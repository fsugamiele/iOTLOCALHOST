# Wanomi 3.0 — Refactorización

**Documento maestro del refactor.**
Versión 0.80 · 2026-07-29 · #52 cerrada · Fase 1 y Fase 2 del paréntesis pre-reunión Claro CERRADAS · Fase 3 (simulador) CERRADA en corpus + código sin push · Fase 4 (siembra) — corpus con lista nominal aterrizada · **DEC-REF-80** (siembra 10 reglas del catálogo #18 con fuente aterrizada, dos packs: cummins-pcc-v1 ampliado + ats-inteliats-v1 NUEVO por BACKLOG-RULE-5 disparador cumplido; F1/F2 desdobladas en sub/sobretensión y sub/sobrefrecuencia; fix inferenceId de cummins-C1→'H1' que libera 'C1' para la nueva de refrigerante, cero riesgo funcional; cita normativa 198-242V/49,5-50,5Hz pendiente de Área 1 sin atribuir en el texto; D4 depende de kit SENSE — no promesa Connect; G2 hereda sensor mentiroso de CR00058, texto advierte) · **DEC-PROC-6** (implementar sobre dirección firmada no es implementar sobre especificación — 9 adendas correctivas en #52 son la medida del hueco; regla: pregunta que la especificación no contesta ⇒ frena bloque + vuelve a diseño) · **DEC-REF-77** (paréntesis pre-reunión Claro: motor+panel+simulador; simulador va PRIMERO por hallazgo genset_running=0 en 55 días; 8 items — alcance, orden, ejercicio semanal, transfer_state real, prefijo cat-, cálculo AL LADO del motor, notificación vs predicción es qué variable se vigila, 4 items fuera con fundamento anti-DEC-PRED-1) · **Adenda al catálogo #18** en bitácora #52 (conteo real 46 no 43, hueco estructural 85/78/65/45% seguridad-baterías-DC-shelter, mismatch cummins-G2 usa 15%=G1, colisión IDs A0/M1/C1 pack↔catálogo) · **DEC-REF-78** (selección de device es preferencia de sesión del usuario, no estado persistente del device; bug destapado por `selectDevice(userId, dId)` filtrando por userId del caller — superadmin desmarca los suyos y no marca los ajenos, `selected:true` desaparece de Mongo y la grilla queda pegada; scope: elimina PUT /device + helper + 3 llamadores; store lee `lastSelectedDId:<userId>` de localStorage con fallback al primero por `_id`; schema `Device.selected` DEPRECADO; 6 items — principio, familia con -75-B(iii), causa raíz verificada en Mongo, alcance, 4 casos raros especificados, deprecación con nota de futuro cleanup) · **Adenda al cierre de #52** en bitácora (registrada en Fase 2 del paréntesis, entrada original intacta): la afirmación "cadena Template→Device→widget→dato vivo PROBADA end-to-end" es PARCIALMENTE FALSA — evidencia por fechas: el ATS de CR00061 no publicó entre 24-jul-02:53Z y 27-jul-01:42Z (gap de 3 días bajo ambos dIds); la prueba de Franco ocurrió en el gap; el valor visto provino de contaminación de suscripciones (`:key="index"` + LiveValue sin watcher, cerrada después en `be9d799`). Queda probado: widget-desde-web + template + vínculo device + widget renderiza. NO quedaba probado: el último eslabón, dato vivo — verificado por primera vez en Fase 2 con 1C RAMA-A (widget muestra 225.1V, Mongo tenía 225.19V a las 12:57 UTC, coincidencia dentro del redondeo por decimalPlaces=1) · **BACKLOG-OPS-3** (consolidación de histórico huérfano del ATS `6z4LN2md`→`59XYsglM` con conflicto de tenencia en TRES userIds — NO ejecutar sin resolver dueño canónico; disparador: solo si alguna vista necesita continuidad histórica) · **DEC-REF-77-A** (adenda al iii — `weekly_exercise` NO puede reusar `mains_failure_ats_transfer` porque bajaría la tensión de red y dispararía alarma cada semana; ejercicio programado se hace con la red presente, requiere escenario propio) · **DEC-REF-79** (parámetros físicos reales de CR00061 para reglas de combustible + ciclo de ejercicio — tanque 250 L rectangular, consumo 3,46 L/h derivado de campo CR00070 Ituzaingó [1410 L en 17 días], grupo de RESPALDO no continuo con 2969,1 h reales, autonomía es alarma con motor corriendo e informativa parado, `fuel_level` en % no litros, contradicción hidrostática↔ultrasónica JSN-SR04T declarada para Área 3 sin bloquear Fase 3, `run_hours` inicial 2969,1 → próximo servicio a 31 h)

---

## 1 · Identidad de Wanomi 3.0

### Qué es

Wanomi 3.0 es la materialización formal del producto enterprise consolidado en las sesiones #8 a #12: una **capa de datos estandarizada y soberana** que opera sobre la infraestructura de torres de telecomunicaciones, detecta y notifica sobre el equipo existente (modo **Connect**), y mide físicamente los puntos ciegos críticos (modo **Sense**). Diseñado para el mercado **enterprise** (telco, infraestructura crítica), con piloto inicial en Claro NEA Argentina.

### Qué NO es

- **No es Wanomi 1.x / 2.x**: las arquitecturas "5 kits independientes", "modelo consumer", "demo descartable" y similares de sesiones previas a #8 quedan archivadas.
- **No es una iteración menor del código existente**: aunque hereda buena parte del stack actual (Node.js, Express, Mongoose, EMQX), las decisiones de arquitectura distribuida (DEC-ARCH-1) y de producto (DEC-PRODUCT-1) imponen una reestructuración que excede una refactorización superficial.
- **No es ML / predicción avanzada day-1**: el alcance honesto es detección Capa 1 (día 1) + predicción Capa 2 (progresiva, madura con baseline propio por sitio). ML auténtico es roadmap, no promesa.

### Pilares no negociables

1. **Marco enterprise** (DEC-STRAT-1): cero deriva hacia consumer durante este ciclo.
2. **Realidad primero** (DEC-STRAT-3): toda decisión se valida contra realidad técnica antes de avanzar.
3. **Edge distribuida** (DEC-ARCH-1): un MongoDB local por Hub, soberanía del dato como argumento de venta.
4. **Capa de agregación NOC** (DEC-ARCH-2): broker MQTT central recibe solo eventos, no telemetría cruda.
5. **Honestidad arquitectónica** (DEC-SENSOR-3, DEC-FORENSIC-2): flag `source: physical | inferred | connect` obligatorio en cada variable. Variables forenses requieren medición física.
6. **Connect+Sense como un único Hub** (DEC-PRODUCT-1): un sitio puede ser Connect, Sense o híbrido. No hay kits separados.

---

## 2 · Decisiones estratégicas heredadas (sesiones #8 a #12)

Estas decisiones son base inalterable de Wanomi 3.0 (DEC-REF-1). Cualquier propuesta de modificación requiere debate explícito en sesión multi-área.

### Estrategia y producto

| ID | Decisión |
|---|---|
| DEC-STRAT-1 | Foco enterprise (telco / infraestructura crítica). Consumer fuera de alcance |
| DEC-STRAT-2 | Demo = herramienta interna viva. Se diseña el producto, no la demo |
| DEC-STRAT-3 | Realidad primero. Toda decisión validada contra realidad técnica |
| DEC-STRAT-4 | **Camino A — "el pilar primero" (formalización, sesión #51, 2026-07-21).** El pilar de Wanomi es ANTICIPAR el evento y RECOMENDAR la solución. Monitoreo e histórico son segundo plano que sostienen el pilar, no el producto en sí. Todo lo que se diseñe/implemente se prioriza por su contribución al pilar. La regla de anticipación es el corazón del pilar y por lo tanto debe ser (a) potente — ya lo es, A8 sellada — y (b) configurable y legible por un operador, no por un ingeniero de backend — objeto de la sesión #51. Decidido por Franco en sesión de estrategia de Área 1 previa a #51; se formaliza aquí porque no tenía registro en corpus (corrección de gap documental, no decisión nueva). Encadena con: F4 acotado del simulador (escenarios de anticipación repetibles) y sesión futura de reglas de degradación/condición. |
| DEC-PRODUCT-1 | Un Hub común, dos modos (Connect software + Sense hardware). Híbrido por sitio |
| DEC-PRODUCT-2 | WN-SITE-CORE = fusión GEN+PWR. SEC integrado como feature del CORE. SURGE y ENV+ como add-ons |
| DEC-SCOPE-1 | MVP de Connect = pata energía (genset+ATS+rectificador). ENV+ adelantado a fase 1-2. AA tonto / batería fina / cámara-AI = fase 2 |
| DEC-SCOPE-2 | Mapa Connect/Sense por sitio NO planificable desde escritorio. Se releva en survey |

### Arquitectura

| ID | Decisión |
|---|---|
| DEC-ARCH-1 | Edge distribuida confirmada. Un MongoDB local por Hub. Sin base central de telemetría cruda |
| DEC-ARCH-2 | NOC recibe eventos, estados y alarmas (vía MQTT central). NO recibe telemetría cruda |
| DEC-STACK-1 | NO migrar Vue 2 / Nuxt 2 ahora. Deuda documentada. Código nuevo escrito migrable (lógica separada de vista) |
| DEC-DASH-1 | Dos dashboards: operador (mapa + alarmas + drill-down) + admin (doble select sitio+plantilla) |
| DEC-DASH-2 | Tres superficies, una verdad: cellowner / técnico campo (display Hub local) / NOC |

### Sensores e inteligencia

| ID | Decisión |
|---|---|
| DEC-SENSOR-1 | Estrategia híbrida físico + soft sensor. Variables forenses → físico. Inferibles → soft |
| DEC-SENSOR-2 | Soft sensors corren en el Hub local |
| DEC-SENSOR-3 | Flag `source: physical | inferred | connect` obligatorio por variable |
| DEC-INTEL-1 | Detección día 1 (reglas, sin baseline) = capacidad inmediata. Predicción = capacidad que madura |
| DEC-PRED-1 | Predictivo Nivel 2 (preventivo basado en condición). ML auténtico = roadmap |
| DEC-FORENSIC-2 | Variables forenses requieren medición física |
| DEC-HMAC-1 | Checkpoints HMAC firmados cada N eventos por Hub; NOC verifica integridad sin revisar la cadena entera |

### Comercial / GTM

| ID | Decisión |
|---|---|
| DEC-GTM-1 | Modelo "managed self-hosted". Hub en sitio, Wanomi opera y mantiene bajo contrato |
| DEC-GTM-2 | Pitch anclado en data propia de Claro: truck rolls evitables + site downs por tanque vacío + cascada de energía. Robo de combustible = capacidad latente, no claim principal |

### Integración

| ID | Decisión |
|---|---|
| DEC-INTEGRATION-1 | Connect = framework multi-driver. Drivers priorizados: GEF (Cummins PCC + ComAp + DSE), ATS (ComAp InteliATS²), Rectificadores (Eltek SmartPack S Modbus TCP — refinado en survey v2.0), AA (Westric Modbus o termostato manual → Sense puro) |

### Hardware (sesiones #10-#12)

| ID | Decisión |
|---|---|
| DEC-HW-1..25 | Diseño del WN-SITE-CORE Rev B. Esquemático congelado, ERC 0/0/0, BOM placa $89 / kit $142 USD. Detalle en `docs/wanomi.md` sesiones #10-#12 |

---

## 3 · Mapa de tracks por área

Orden de ataque de iteración 1 (ruta crítica explícita, definida en sesión #14):

1. **[GATE] Estrategia** — survey express → matriz de controlador por sitio → **1 sitio ComAp confirmado**. Bloquea la convergencia en sitio real. Pre-filtrado de escritorio en T0+1 para detectar riesgo temprano.
2. **[PARALELO] Software** — pipeline e2e contra el simulador (no bloqueado por el survey, gracias a DEC-REF-14).
3. **[PARALELO] Hardware** — Hub funcional + enclosure provisorio.
4. **[TEMPRANO] Marketing** — pitch draft (lo necesita Estrategia para acceder a cellowners) + renders corregidos.

Software y Hardware corren desde T0 sin bloqueo; el simulador reemplaza sitio y lab durante iter 1.

> Detalle de entregables y responsables por área: ver `docsRefactor/<Área>/iteracion_1_alcance.md`.

---

## 4 · Roadmap inicial (fases lógicas)

**MVP de Wanomi 3.0** (mínimo end-to-end, 1 sitio, ~60 días, definido en sesión #14):

**ENTRA:**
1. 1 sitio Tier 1 con **ComAp InteliGen confirmado**.
2. 1 **Hub** (Orange Pi Zero 3 + Modbus RTU/TCP + Mongo local), enclosure provisorio.
3. **Connect**: lectura eléctrica + mecánica + estado (cascada de energía + salud de motor) con `source: connect`.
4. **Motor de reglas** robustecido + RulePacks (~40 reglas día 1, auto-calibradas + cross-equipo).
5. **Notificación**: dashboard + Telegram + evento MQTT al NOC.
6. **`pages/sites/`**: lista + detalle de 1 sitio (mapa + estado vivo + alarmas).
7. **Read-only estricto**.
8. Validación contra **simulador** → instalación en sitio → **30 días** con captura de ≥1 cascada real.
9. **Pitch deck** cerrado con vista en vivo del sitio + renders corregidos.

**FUERA (Fase 1+):** CORE Rev B (track paralelo) · motor SNMP · otras 4 familias GEF · Sense físico (combustible Cummins, vibración) · SURGE/ENV+/sub-nodos · dashboards completos · migración Vue 3 · predictivo más allá de soft sensors · control remoto (escritura).

---

### Fase 0 — Arranque (sesión #14) ✅ COMPLETADA
- Reunión multi-área ejecutada. MVP definido.
- Documentos de alcance iter 1 generados por área (ver `docsRefactor/<Área>/iteracion_1_alcance.md`).
- 10 nuevas DEC-REF registradas (DEC-REF-6..15).

### Fase 1 — Diseño consolidado
- Cada área completa su diseño de iteración 1.
- Integración cruzada: cómo Software consume hardware, cómo Estrategia alimenta Marketing, etc.
- Validación de la arquitectura end-to-end contra Survey Tier 1 (cuando esté disponible).

### Fase 2 — Implementación
- Hardware: Gerbers v2 → fab → prototipo → bench test (ya en curso, track legacy hereda).
- Software: backend Connect+Sense + 3 drivers MVP (ComAp + Cummins + Eltek) + frontend mínimo.
- Firmware: CORE + Hub básico.
- Marketing: pitch + brochure listos.

### Fase 3 — Piloto Claro Tier 1
- Survey Tier 1 ejecutado (5 sites).
- Instalación CORE + Hub en sitios Tier 1.
- 30 días de operación + datos vs. reportes Sytex históricos.
- Reporte ejecutivo + decisión go/no-go para Tier 2.

### Fase 4 — Iteración y escalamiento
- Ajustes derivados del piloto.
- Tier 2 (15 sites adicionales).
- Conversaciones con cuentas post-Claro.

> Las duraciones de Fases 1-4 se definen al inicio de cada fase con criterio realista por área. Fase 0 completada en sesión #14.

---

## 5 · Registro de decisiones del refactor (DEC-REF)

| ID | Fecha | Decisión |
|---|---|---|
| DEC-REF-1 | 2026-05-28 | Wanomi 3.0 hereda como bases inalterables las decisiones estratégicas #8-#12 |
| DEC-REF-2 | 2026-05-28 | Documentación del refactor vive en `docsRefactor/`. `docs/` legacy queda inmutable |
| DEC-REF-3 | 2026-05-28 | Archivo maestro `docsRefactor/WanomiRefactor.md` consolida pilares + DEC-REF-* + roadmap |
| DEC-REF-4 | 2026-05-28 | Sesiones anteriores a #8 NO son base de diseño para 3.0. Histórico solamente |
| DEC-REF-5 | 2026-05-28 | Próxima sesión #14 = reunión multi-área de arranque del refactor |
| DEC-REF-6 | 2026-05-30 | **MVP de Wanomi 3.0 = Connect-first** sobre ComAp InteliGen + Eltek SmartPack S en 1 sitio Tier 1. El WN-SITE-CORE Rev B queda **fuera de la ruta crítica** del MVP (sigue su track de fab en paralelo) |
| DEC-REF-7 | 2026-05-30 | El sitio piloto se elige **por controlador** (criterio eliminatorio: ComAp presente), no por conveniencia geográfica. Gate único del MVP |
| DEC-REF-8 | 2026-05-30 | Connect es framework de **triple-modalidad**: Modbus (TCP+RTU) + SNMP + contacto seco / digital I/O. Amplía DEC-INTEGRATION-1 |
| DEC-REF-9 | 2026-05-30 | Alcance GEF ampliado a **5 familias de controlador**: + PowerWizard 2.1 + SDMO NEXYS/TELYS (evidencia de campo: grupos Wilson y Monte Ralo) |
| DEC-REF-10 | 2026-05-30 | Notificación del MVP = **MQTT event al NOC** (pilar DEC-ARCH-2) + **Telegram** (alerta humana) + **dashboard**. NO se construye capa de consumo del NOC en el MVP |
| DEC-REF-11 | 2026-05-30 | Inteligencia del MVP = **amplitud en detección** (~40 reglas día 1, RulePacks auto-calibradas + cross-equipo), NO predictivo amplio. Honra DEC-INTEL-1 / DEC-PRED-1 |
| DEC-REF-12 | 2026-05-30 | Lectura del MVP = ComAp **InteliGen** (eléctrico + mecánico + estado en una conexión) + Eltek SmartPack S. El dominio mecánico (°C/aceite/RPM/combustible) **es Connect-able** vía controlador de motor |
| DEC-REF-13 | 2026-05-30 | **Read-only estricto** en el MVP. Registros de comando (arranque/parada/E-Stop) deshabilitados en firmware por seguridad. Reabrir solo con decisión explícita + contrato |
| DEC-REF-14 | 2026-05-30 | El simulador (DEC-STRAT-2) se extiende como **banco de pruebas**: emite registros ComAp/Cummins simulados para validar driver + reglas + notificaciones sin equipo físico |
| DEC-REF-15 | 2026-05-30 | El refactor **reutiliza** el modelo `variable→widget→template`; lo extiende con capa **Site** (compone templates por equipo), capa **Driver** (Modbus), **RulePacks** por tipo de equipo y **NotificationRouter**. Device se generaliza a Equipment con `driverConfig` |
| DEC-REF-16 | 2026-05-31 | El MVP Connect adopta **dos drivers día 1**: **ComAp InteliATS PWR** (estado/transferencia/cascada) + **Cummins PowerCommand** (mecánico + eléctrico del grupo). Enmienda DEC-REF-12: el dominio mecánico llega vía PowerCommand, **no se asume InteliGen**. Habilita reglas cross-equipo reales (DEC-REF-11). **La selección de sitio piloto queda abierta** hasta tener más candidatos; el sitio no es bloqueante para el track Software (DEC-REF-14, simulador como banco de pruebas). El rectificador se confirma en survey. |
| DEC-REF-17 | 2026-05-31 | `createSaverRule()` endurecido contra la race condition causa de BACKLOG-SIM-1: (a) `waitForSaverResource` con poll activo hasta `is_alive:true` reemplaza el `setTimeout(EMQX_RESOURCES_DELAY)` fijo; (b) guard ruidoso en `createSaverRule()` (retorna `false` + log de error si el resource no está, en vez de fallar en silencio); (c) `reconcileSaverRules()` al arranque que repara rules faltantes o `enabled=false` (PUT con fallback a recreación), idempotente. Verificado: EMQX 4.2.3 acepta `PUT /api/v4/rules/{id}`. Archivos: `app/api/routes/emqxapi.js`, `app/api/routes/devices.js`. |
| DEC-REF-18 | 2026-06-01 | **Motor de reglas edge** — proceso Node SEPARADO (deployable en Hub Orange Pi). Evalúa stream MQTT local directo en paralelo al saver-webhook; estado del site completo en memoria (~37 KB para CR00061). Al arrancar, hidrata desde Mongo local con `reconstruct` (~250 ms, ~150 lecturas indexadas) — sin ventana ciega ni falsas alarmas tras reinicio. Expresiones cross-equipo ESTRUCTURADAS (árbol JSON, nunca eval). EMQX vuelve a ser broker + persistencia; las saver rules (DEC-REF-17) permanecen intactas. |
| DEC-REF-19 | 2026-06-01 | **Gestión de reglas centralizada (managed self-hosted, DEC-GTM-1)** — catálogo versionado en el NOC = fuente de verdad única. Una regla es un DATO (RuleDefinition en Mongo), no código: agregar regla = insertar registro, sin reinstalar Hub. Bajada por PULL (robusto ante conectividad celular intermitente del BG95-M3); reconciliación idempotente igual que `reconcileSaverRules()` (DEC-REF-17). Subida: solo eventos resumidos (DEC-ARCH-2 intacto). |
| DEC-REF-20 | 2026-06-01 | **Salvaguardas del sync** — anillos `canary → production` con promoción manual (mecanismo listo desde ahora; valor real en Tier 2). Validación obligatoria en el Hub antes de aplicar: regla inaplicable → ignorada con log; regla malformada → rechazada y reportada al NOC como evento. Rollback por versión: el Hub conserva la versión anterior inactiva (rollback instantáneo sin enlace celular). Todo rollback auditado en la forensic chain (Fase 4B). |
| DEC-REF-21 | 2026-06-02 | **NotificationRouter** — 3 severidades: `info` / `warning` / `critical`. Ruteo por consecuencia operativa: warning→dashboard+NOC+Telegram SIEMPRE (DEC-GTM-2, no opcional); critical→tres canales con tono interruptor. `recommendation` vive en RuleDefinition (DEC-REF-18). Formato por canal: Telegram=texto humano; NOC=evento estructurado; dashboard=objeto completo. Evento NOC a `wanomi/noc/{siteId}/event`; bridge NetCool en standby (bloqueado sin credenciales Claro). Deduplicación por correlación de causa raíz declarada en datos: cada regla nombra su evento padre; el router agrupa consecuencias bajo evento padre en ventana de correlación. |
| DEC-REF-22 | 2026-06-02 | **Modelo de eventos unificado** — toda alarma persiste con su valor numérico disparador. Flag `forensic` derivado del `source` de la variable (DEC-SENSOR-3): physical+categoría legal → cadena HMAC (DEC-FORENSIC-2 intacto); resto → forensic:false, solo historial. Predicción usa telemetría cruda Forma A (regresión sobre curva completa, ~43k puntos/mes, 1×/hora en Orange Pi); Forma B descartada por menor acierto. Reusa `forensic_dispatcher.js` + hook alarm-webhook (Fase 4C.2). Retención 30-90d; cooldown extendido a persistencia (anti-tormenta). |
| DEC-REF-23 | 2026-06-03 | **Schema Notification extendido para motor edge** — campos semánticamente correctos agregados a la colección `notifications`: `ruleId`, `inferenceId`, `label`, `variableLabel`, `severity`, `recommendation`, `siteId`, `reason`, `source`. Campos EMQX legacy (`emqxRuleId`, `condition`, `variableFullName`) mantenidos opcionales para compatibilidad con alarmas existentes. Campo `source: 'emqx' \| 'edge-engine'` permite discriminar origen. `variableLabel` (nombre legible, DEC-47) propagado a los 4 canales del NotificationRouter; mapeado a `variableFullName` en Mongo. Motor edge nunca usa shims semánticos sobre campos legacy. Motivado por DEC-STRAT-2 (producto, no demo). |
| DEC-REF-24 | 2026-06-03 | **Estándar de comunicación de alarmas (v1, perfeccionable)** — formato jerárquico con contexto numérico explícito: `[SEVERIDAD] LABEL — Dispositivo: NAME \| Variable: LABEL = VALUE UNIDAD \| Umbral: THRESHOLD (modo MODE) \| → RECOMMENDATION`. Tres niveles operativos: `info`=monitoreo sin acción inmediata; `warning`=intervención planificada; `critical`=acción inmediata. `mode` documenta la fuente del umbral (`calibrated`/`fallback`/`no-ref`/`direct`). Unidades provistas por `variableLabel` (DEC-47). Formato aplica a Telegram, NOC event JSON, y dashboard. v1 deliberadamente simple — perfeccionable con datos reales de operación. |
| DEC-REF-25 | 2026-06-03 | **Driver normaliza, evaluador compara** — separación de responsabilidades entre capa driver y capa evaluador: el driver Modbus (futuro `modbusDriver.js`) lee registros crudos del equipo, aplica `scale`, convierte unidades y publica el setpoint ya normalizado como variable MQTT estándar en siteState. El evaluador tipo C (`typeC.js`) solo hace la comparación numérica — no conoce Modbus, registros ni escalas. Esta separación permite reusar `evaluateD()` como primitiva de comparación y mantener el evaluador sin estado propio. Corolario: `setpointSource.register` y `setpointSource.scale` son metadata del driver, no del evaluador; el evaluador solo usa `setpointSource.variable` (nombre de la key en siteState). |
| DEC-REF-26 | 2026-06-14 | **Escalada temporal del fallback (BACKLOG-EDGE-2)** — cuando una regla tipo C persiste en modo `fallback`/`no-ref` (setpoint ausente) ≥ `escalateAfterMinutes`, el evento INFO de configuración (DEC-REF-24 sub-paso 2b) escala a una transición `warning` (reason `setpoint-unavailable-escalated`), UNA sola vez por episodio (marca idempotente). Opt-in: `escalateAfterMinutes` default `null` (la escalada es dato, DEC-REF-19). Disparo **reactivo** (evaluado en el mensaje entrante, sin `setInterval` — preserva el motor event-driven de DEC-REF-18); el episodio se mide en **tiempo-de-eventos**, no wall-clock. La transición **bypassa** el cooldown del INFO (un cambio de estado es información nueva, no repetición). Reset en silencio al recuperar el setpoint (`mode='calibrated'`): borra el estado del episodio. Sigue siendo mensaje SEPARADO del operativo (DEC-REF-24). Corolario de alcance: el "equipo enmudece del todo" (silencio total del stream) NO lo cubre esta decisión — es heartbeat/staleness, BACKLOG-EDGE-4. Parámetros (`escalateAfterMinutes`, cooldowns) calibrables en producción; el mecanismo queda cerrado. Validado E2E en CR00061 (escalada idempotente, reset, no-regresión). |
| DEC-REF-27 | 2026-06-17 | **Estado del pin del mapa = status server-side.** El color del pin de cada site (`pages/sites/`) refleja la peor severidad *vigente* entre sus notificaciones, calculada en el backend (`GET /sites/status`) con una agregación única para N sites (escalable, DEC-ARCH-2 — el NOC consume estado resumido, no telemetría cruda). "Vigente" = notif dentro de una ventana de 15 min (2× `cooldownSec` operativo default + margen; calibrable). Prioridad: critical→rojo, warning→amarillo, sin alarma vigente→verde. **INFO no pinta el pin** (opción A) — vive solo en el feed del detalle. Auto-resolución por silencio: el motor re-emite cada `cooldownSec` mientras la condición persiste; al cesar, el pin vuelve a verde solo. **Abierto a futuro (validar con Claro):** elevar INFO a color propio (celeste) post-MVP. **Desacuerdo registrado:** Diseño operador prefería pin derivado del `siteState` vivo del motor; resuelto por coherencia pin↔feed y por no construir un canal de estado que el blueprint §6 no pide. Revisable con datos reales de los 30 días. Commits: ea3ea26, 9290a95, 1f61a57, bd1ff55. |
| DEC-REF-28 | 2026-06-17 | **Modelo de tenancy jerárquico de 4 niveles.** La propiedad/visibilidad se modela como árbol `Operator → Zone → Site → Equipment`. `Operator` = tenant raíz (aísla entre clientes: Claro vs futuro Movistar). `Zone` = agrupación geográfica de sites con responsable (NEA, NOA...). `Site` (ya existente) gana `operatorId` + `zoneId`; el campo `cellOwner: String` actual se migra a relación `zoneId`. `Equipment` (Device actual) cuelga del Site. **Modelo completo desde día 1, implementación colapsada a 1-1-1 en el MVP** (Claro/NEA/CR00061). Greenfield: el código actual no tiene roles ni tenancy (User = name/email/password; todo cuelga de `userId` plano). Reemplaza el `cellOwner` string libre (DEC-DASH-2 lo definía conceptualmente, sin modelo de datos). |
| DEC-REF-29 | 2026-06-17 | **Autorización por grants {rol, alcance}.** Un `User` tiene uno o más grants, cada uno = {rol, alcance en el árbol}. Cubre uniformemente todos los actores sin tipos rígidos: cellowner = grant {responsable, zoneId}; gerente de zona = múltiples grants {responsable, zoneA/B/...} (un gerente supervisa cellowners de N zonas — confirmado por Franco como realidad operativa); NOC = grant {supervisor, operatorId}; Wanomi = grant {superadmin, *} (Wanomi opera bajo contrato, DEC-GTM-1, ve todos los operadores). Elegido sobre `role` simple porque el gerente multi-zona rompe el modelo de etiqueta fija desde el día 1, y reescribir permisos es superficie de fuga entre tenants. MVP: 1 usuario (Franco) con grant superadmin; sistema de invitación/alta de usuarios diferido a backlog hijo. |
| DEC-REF-30 | 2026-06-17 | **Topología de topics híbrida.** Dos clases de dato viajan distinto, alineado a DEC-ARCH-2 (crudo en el edge, resumido al centro). (1) **Telemetría operativa intra-site** sigue en `{userId}/{dId}/{variable}/sdata` — NO se toca; es local al Hub, no cruza al centro, no necesita jerarquía. (2) **Eventos/alarmas** (suben al NOC) y **distribución de packs** (bajan al Hub) usan la capa jerárquica `{operatorId}/{zoneId}/{siteId}/event`. Los grants (DEC-REF-29) se traducen a suscripciones ACL sobre esta capa: cellowner→`{op}/{zona}/#`, gerente→múltiples ramas, NOC→`{op}/#`, Wanomi→`#`. Aislamiento estructural por rama (un cellowner físicamente carece de ACL fuera de su zona). El namespace `{userId}/#` actual (ya existente en `getWebUserMqttCredentials`) se conserva para telemetría; la capa jerárquica es aditiva. Migración de telemetría al namespace jerárquico: NO se hace (innecesaria — el crudo es local por DEC-ARCH-2). |
| DEC-REF-31 | 2026-06-17 | **Distribución de RulePacks por el árbol de tenancy (implementa DEC-REF-19/20 sobre DEC-REF-28).** Catálogo de packs a nivel `Operator` (catálogo maestro del cliente) + `overrides` por `Zone`. El Hub de cada site pulea (DEC-REF-19, pull idempotente) lo que le corresponde según su rama: catálogo del operador + overrides de su zona. La misma jerarquía gobierna los dos flujos que cruzan el site: packs bajan, eventos suben (DEC-REF-30); la telemetría intra-site no se distribuye. MVP: schema del catálogo + el pack `cummins-pcc-v1` actual colgado de Claro; la *implementación del mecanismo de pull* (DEC-REF-19/20) sigue diferida — hoy es seed manual. |
| DEC-REF-32 | 2026-06-18 | **Frescura de grants en el frontend = `GET /me`, no JWT.** Continuación operativa de DEC-REF-29 (autorización por grants): el frontend obtiene los grants vía `GET /me` (lectura fresca de DB en cada carga de la app), NO del JWT firmado ni del response body del `/login`. Elegido sobre la **Opción A** (cachear los grants en el cliente al login): con `/me`, un cambio de rol se refleja en la UI sin necesidad de re-login — la credencial visual del frontend deja de quedar congelada hasta el próximo login. La autorización REAL ya es server-side e inmediata (28.x.1: el middleware lee grants frescos de DB en cada request); esto alinea la frescura de la UI con la frescura ya vigente del backend. El response body de `/login` no se toca (queda como retrocompat para los consumidores actuales del campo `userData`). Estado: el endpoint `/me` existe (28.x.1, commit pendiente); el consumo desde el frontend queda diferido a BACKLOG-TENANT-1 (no hay todavía UI que distinga por rol). |
| DEC-REF-33 | 2026-06-18 | **Autorización en el gate del Site, derivados confían en el gate (punto único centralizado).** En los endpoints multi-colección (forensic, `GET /site/:siteCode/full`), la autorización por grants se evalúa UNA vez en el gate del Site (`Site.findOne` compuesto con `buildReadFilter`); los queries derivados (Device, Template, ForensicEvent, Notification) sueltan el `userId` y filtran solo por `siteId`/`siteCode`, confiando en que el gate ya autorizó. Razón: los registros derivados de un site tienen el `userId` del dueño (ej. CR00061 → cuenta personal), no el del usuario autorizado por grant; mantener `userId` en los derivados rompería el acceso del cellowner que SÍ debe verlos por su zona. Elegido sobre defensa en profundidad (re-validar scope en cada query derivada) porque esta última repite el `buildReadFilter` —con su query a Sites— por cada colección, pagando latencia para proteger contra un bug del gate que hoy no existe (premature hardening). **Condición de seguridad del punto único:** el gate se evalúa SIEMPRE vía el helper centralizado `buildReadFilter`, NUNCA reimplementado a mano por endpoint — así el punto único es deliberado y auditable (un solo lugar donde vive la autorización), no frágil por duplicación desincronizada. Gatillo de revisión: si un refactor futuro afloja o duplica el gate, esta decisión deja de sostenerse y la defensa en profundidad vuelve a la mesa. Alcance: solo READS (28.x.3); las escrituras (POST/PUT/DELETE) son backlog (BACKLOG-TENANT-3) y hoy quedan protegidas por accidente (el filtro `userId` no matchea para el cellowner). |
| DEC-REF-34 | 2026-06-19 | **ARCH-3 — acceso a históricos gobernado por grants.** El gate de los endpoints de históricos (`/get-last-data`, `/get-small-charts-data`) usa `resolveScopedDIds` (puro scope de grants), reemplazando el filtro por `userId`. Consecuencia consciente: un dato huérfano (Device borrado, Data sobreviviente) deja de ser visible para el ex-dueño en el endpoint de históricos — el dato NO se borra (persiste para análisis y cadena forense), solo no figura en el drill-down del aparato inexistente. Coherente con grants como fuente única de verdad de visibilidad (DEC-REF-33). La separación de roles de 28.3 se vuelve efectiva: la cuenta personal sin grant deja de ver Claro por ownership. |
| DEC-REF-35 | 2026-06-21 | **Fuente de selectores de variables = `template.widgets`**, NUNCA slugs crudos de la colección `data`. El selector usa `variableFullName` para mostrar y `variable` (slug) para consultar; `variableType` filtra qué entra al selector según el contexto (gráficos numéricos excluyen `bool` y `categorical`). Aplica a toda vista que liste variables al usuario (históricos en `pages/history.vue`, futuros export y análisis). Coherente con DEC-47 (nombres legibles, NO slugs) y DEC-REF-24 (la plantilla es el contrato curated del dispositivo, no la colección cruda). Trade-off aceptado: una variable declarada en la plantilla pero sin datos aparece en el selector y se grafica vacía — lo cubre el cartel "Sin datos en este rango" del componente chart, sin agregar endpoint nuevo ni fetch en cadena. Registrada en #32 sobre código ya implementado en 31.4c (formaliza el invariante; los próximos vistas lo heredan). |
| DEC-REF-36 | 2026-06-22 | **TENANT-4 ejecutada — `userId` de sites+devices Claro NEA migrado a cuenta de servicio.** Migrados 4 sites + 10 devices de la cuenta personal de Franco a `operator-claro@wanomi.platform` (cuenta de sistema SIN login: password = hash de secreto random descartado en runtime; ve por ownership vía `buildReadFilter` pero no se autentica). Separación de roles de 28.3 ahora efectiva sobre dato REAL: la cuenta personal deja de ver Claro por ownership, el cellowner sigue viendo por grant. Validado E2E por la app sobre el gate de Site en ambas direcciones — `GET /api/site` (cell 4 / personal 0 / super 4); `forensic-events?siteCode=CR00061` y `site/CR00061/full` (cell 200 / personal **404 "site not found"** / super 200), confirmando el before/after de 28.x (Franco-personal sacaba el forense por ownership, ya no). **Honestidad de validación:** el path histórico por dId (`resolveScopedDIds`, DEC-REF-34) NO quedó validado en positivo — `get-last-data` dio 0/0/0 simétrico por falta de ingest (`data` sin muestras, simulador apagado); su validación positiva se difiere al paso 4. Alcance CERRADO a sites+devices; `emqxauthrules` diferido (BACKLOG-TENANT-6). Scripts: `seeds/seed_operator_claro.js` (`483797e`), `seeds/migrate_tenant4_userid.js` (`ce53c6e`) — idempotente, backup JSON pre-migración (`seeds/_dev/`, gitignored) + modo `--restore` ensayado (restore = pre-estado exacto, re-migrate = estado final). Registrada post-ejecución (precedente: DEC-REF-35). |
| DEC-REF-37 | 2026-06-23 | **La rama de ownership (`scope===null → {userId}`) NO aplica a derivados sin tenencia propia.** Extiende DEC-REF-33. Para los modelos cuya visibilidad es DERIVADA de un Site y que no pueden ser legítimamente propios de un usuario sin operador (`Notification`: la genera el motor de alarmas de un sitio; `Template`: catálogo por tipo de equipo, DEC-REF-15/BACKLOG-TENANT-8; `ForensicEvent`: dato forense del motor edge), un usuario sin grants ve NADA: `scopeFilterFor` devuelve un centinela `DENY` (`Symbol`) que `buildReadFilter` materializa como `{ $expr: false }` (0-match), en vez de caer a `{userId}`. Aplica en las dos vías sin alcance: sin grants (guard global vía `hasDenyFallback`) y grant que no alcanza ningún site (`sites=[]`). Motivo: tras TENANT-4 el `userId` de los derivados quedó pegado al dueño viejo (no migrado a propósito, DEC-REF-36); la rama `{userId}` lo exponía por ownership a la cuenta personal sin grant — fugas confirmadas E2E (#33: `GET /notifications` 24 docs, `GET /template` 4 docs visibles a Franco-personal). `Site` y `Device` CONSERVAN la rama `{userId}` (raíz de 'lo propio'; un B2C podría tener sites/devices propios legítimos — caso hoy inexistente pero estructuralmente válido: `device.siteId` no es required, `POST /template` no exige device). `ForensicEvent` va a DENY por defensa en profundidad (sus endpoints gatean por Site previo hoy, pero DENY lo blinda contra un endpoint standalone futuro; costo nulo, dato sensible). `resolveScopedDIds` NO se tocó (Device/Data quedan en ownership; hoy SAFE por composición — Franco resuelve 0 dIds post-TENANT-4). E2E sobre dato real: franco `/notifications` 24→0, `/template` 4→0; cell preserva 18 notifs + 4 templates por grant; site/device sin regresión (4/0/4, 10/0/10). Commit `00b397a`. Coherente con DEC-STRAT-2: el criterio de visibilidad refleja la naturaleza del dato, no un artefacto de migración. |
| DEC-REF-38 | 2026-06-24 | **Autorización de suscripción MQTT bajo tenancy = ACL B-narrow por dId del scope (equivalente broker de DEC-REF-37).** La ACL MQTT del web-user (`EmqxAuthRule` type=user, generada en `getWebUserMqttCredentials`) deja de ser `[{userId}/#]` (solo el namespace propio) y pasa a autorizar exactamente los devices que el grant del usuario alcanza, en el namespace donde el dato realmente vive (post-TENANT-4, `SERVICE`, no el del caller). Granularidad **α-estricta** (única que es B-narrow real): por cada dId del scope, subscribe a `{owner}/{dId}/+/sdata` + `/+/notif` + `/+/actdata`, y publish SOLO a `{owner}/{dId}/+/actdata`; más `{userId}/#` propio. Descartadas: **β** (`{owner}/{dId}/#`) porque permitiría publish a `config` → el cellowner podría reconfigurar el device (fuga de CONTROL, no solo de datos); **γ/B-broad** (`{owner}/#`) porque expondría todo el namespace del operador → fuga de tenancy al entrar 2º operador/zona (mismo patrón que DEC-REF-37 en la API). Implementación: helper nuevo `resolveScopedDIdsWithOwner(req)` en `scope.js` (devuelve `[{dId, userId}]`, reusa `scopeFilterFor('Site')`, no toca `resolveScopedDIds`); ambos paths de `getWebUserMqttCredentials` (CREATE+UPDATE) reescriben publish/subscribe en cada llamada → scope fresco en cada login (coherente con DEC-REF-29/32). Multi-namespace por diseño (DEC-REF-28: N operadores, N service accounts; el helper `(dId,userId)` lo cubre sin hardcodear SERVICE). Regeneración natural: el frontend pide credenciales en cada carga (`default.vue`), sin endpoint ni mecanismo extra. Complemento necesario: el frontend debe suscribirse a los namespaces de los devices visibles, no a `{auth_userId}/#` (pieza 3, cambio en `default.vue`). La migración de las ACL device PERSONAL→SERVICE (pieza 1) resultó innecesaria: `getDeviceMqttCredentials` crea la ACL bajo SERVICE en el primer bootstrap (self-heal, a verificar al prender el simulador); las 10 ACL device viejas bajo PERSONAL quedan como limpieza diferida (RISK-SEC-1). |
| DEC-REF-39 | 2026-06-28 | **Gobernanza de reglas Wanomi-managed (esta fase).** El catálogo lo cura Wanomi (DEC-REF-19). El operador NO edita ni crea reglas; sub-catálogos / ajuste por cellowner = roadmap. La consola de reglas es superficie superadmin (DEC-REF-29). |
| DEC-REF-40 | 2026-06-28 | **Predicción y auto-baselining = roadmap, fuera de MVP.** Capa de predicción (regresión sobre histórico → variable inferida → regla que la vigila, DEC-PRED-1) y auto-ajuste de umbrales por baseline histórico fuera de MVP/demo. Salvaguarda: el auto-baselining nunca debe naturalizar una condición de falla (un sitio que corre mal no debe auto-subir su umbral y esconder el problema). |
| DEC-REF-41 | 2026-06-28 | **Vista de cascada dedicada.** La cascada energética tiene vista propia: timeline disparo raíz → causa → consecuencias encadenadas → recomendación, sobre el agrupamiento padre-hijo de DEC-REF-21 (`correlationParent`, ya en schema). En scope de demo. |
| DEC-REF-42 | 2026-06-28 | **Consola de reglas superadmin (central en esta fase).** CRUD de reglas para superadmin (DEC-REF-29), escribe al mismo Mongo que lee el motor, sin pull/canary/reconcile (eso recién con Hub separado, post-demo). v1 incluye constructor visual de reglas cross-equipo (`crossExpr`). Construcción en paralelo: la cascada de la demo se siembra por RulePack, NO depende de la consola. |
| DEC-REF-43 | 2026-06-28 | **Feed de alarmas del detalle de site.** Contenido = colección `notifications` filtrada por `siteId`, gateada por `buildReadFilter('Notification')` + DENY (DEC-REF-37), en handler dedicado (DEC-DASH-2). El endpoint del feed devuelve además un mapa `{variable → peor severidad vigente en ventana 15 min}` para el coloreo, con la misma lógica windowed de `/sites/status` (DEC-REF-27) pero agrupada por variable — evita la incoherencia variable-roja-eterna vs pin apagado. |
| DEC-REF-44 | 2026-06-28 | **Real-time-lite del feed.** El handler de `notif` en `default.vue` reemite al bus de Nuxt (`$emit`), que hoy NO hace (solo toast + badge). Una vista de site abierta escucha y dispara re-fetch acotado del feed + recoloreo. Usa la ACL del browser ya existente (DEC-REF-38); no abre `wanomi/noc/{siteId}/event`. |
| DEC-REF-45 | 2026-06-28 | **Coloreo de variables por severidad.** Server-side, derivado del mapa de DEC-REF-43, consistente con el pin (DEC-REF-27). El ACK NO apaga el color: color = condición activa (se apaga por silencio/resolución); ACK = atención humana. Estados separados. |
| DEC-REF-46 | 2026-06-28 | **ACK de alarmas con autoría auditable.** Se agregan `acknowledgedBy` (userId) + `ackAt` (timestamp) al schema `notifications`, separados de `readed`. El endpoint de ACK exige write-authorization (BACKLOG-TENANT-3): el grant del actor debe alcanzar el `siteId` de la notif — espejo de escritura del gate de lectura `buildReadFilter`. |
| DEC-REF-47 | 2026-06-29 | **`crossExpr` = árbol AND/OR sobre estados de equipo + operador de suma entre equipos ("B plus").** El árbol de la regla cross-equipo es estructurado (DEC-REF-18: dato, nunca texto evaluable), profundidad acotada. Nodos lógicos `AND`/`OR`; hojas de dos clases: (a) condición sobre un equipo `{ deviceType, variable, condition }` resuelta leyendo `siteState` por `_deviceType` (sin hardcodear `dId`); (b) hoja aritmética de suma entre equipos `{ sum: [{deviceType, variable}...], condition }` para carga combinada (power plant Eltek de Claro). Comparación reusa primitiva de typeD (DEC-REF-25). Fuera de alcance (roadmap): secuencias ordenadas y tendencias temporales multi-equipo (estas últimas → capa de predicción, DEC-REF-40). Implementación por capas (DEC-REF-28): se diseña el árbol contemplando la suma; se implementa primero AND/OR (cascada energética), la hoja de suma se agrega sin reescribir. |
| DEC-REF-48 | 2026-06-29 | **Temporizador de cascada reactivo, sin reloj propio.** El "no arrancó en N s" se evalúa con el patrón de DEC-REF-26: marca `:start` al cumplirse el evento gatillo (pérdida de AC); en el próximo mensaje entrante, si pasaron ≥ `graceSec` en tiempo-de-eventos Y la condición de falla persiste, dispara una vez (idempotente). Resolución por éxito: si la condición se satisface antes del plazo (gen arranca), se limpia `:start`. `graceSec` es dato de la regla (ajustable por sitio/grupo), no hardcoded. Preserva el modelo event-driven (sin `setInterval`). Limitación heredada: si el equipo que cierra la condición enmudece → heartbeat/staleness (BACKLOG-EDGE-4), otra alarma. |
| DEC-REF-49 | 2026-06-29 | **Validación de cascada con el simulador como producto, no con scripts.** La cascada se valida con el simulador simulándola de verdad (DEC-REF-14, "producto no demo"). Requiere: (a) arreglar el `sharedState` del simulador (ver BUG-SIM-1); (b) un escenario de cascada real coordinado (corte AC en ATS → gen no arranca → consecuencias). Se descarta la orquestación por script externo por ser atajo de demo. |
| DEC-REF-50 | 2026-06-29 | **`correlationParent` persistido + convención padre-hijo.** El evento raíz (pérdida de AC) se emite como alarma propia; cada consecuencia nombra a su padre vía `correlationParent` (DEC-REF-21). Se agrega `correlationParent` a `saveToMongo` (hoy solo va al evento NOC, no se persiste) para que la vista de cascada (DEC-REF-41) tenga el dato de agrupación. |
| DEC-REF-51 | 2026-07-02 | **Despacho de reglas cross por bypass del gate.** Las reglas `type='cross'` se evalúan en cada mensaje entrante del site contra `siteState` completo (requisito de DEC-REF-48: el timer se resuelve con el próximo mensaje, venga del equipo que venga — cubre el caso "equipo gatillante enmudece, otro sigue hablando"). Gate `deviceType`+`variable` intacto para D/C/S. Costo medido en contexto Hub: ~1 msg/2-3s por site, evaluación sub-ms sobre `Map` en RAM. Descartado despacho por "variables participantes del árbol" = hardening especulativo (nota de escala futura). Anti-tormenta ya cubierto por `cooldownSec` + marca `:start` idempotente (DEC-REF-48). Decidido por Franco, sesión #39. Incluye dos aplicaciones de forma surgidas del recon #39: (a) threading explícito de `eventTs` (sellado único en el handler del mensaje, opción A elegida por Franco) como aplicación literal del tiempo-de-eventos de DEC-REF-48; (b) extensión del enum `NotificationRO.mode` con `'cross'` — misma clase de bug de persistencia silenciosa que `mode:'window'` (#37), esta vez cazado en recon previo. |
| DEC-REF-52 | 2026-07-05 | **BACKLOG-TENANT-9 vía self-heal del producto (Variante B).** Causa raíz DOBLE (verificada #41-R2): las reglas EMQX del Rule Engine se perdieron ~2026-06-01 (GET `/api/v4/rules` = `data:[]`, 10 `emqxRuleId` de Mongo → 404, causa probable: ciclo de vida del contenedor sin volumen persistente para rules), Y las 10 saverrules en Mongo quedaron con `userId=PERSONAL` tras TENANT-4 (mismatch que las habría roto igual al recrearse). Decisión: **script migra SOLO `saverrules.userId` PERSONAL→SERVICE**; el create+relink de las reglas EMQX lo hace `reconcileSaverRules` (`emqxapi.js:257-320`, patrón ya existente `:308-313`) al reiniciar el backend. Coherente con DEC-STRAT-2 (producto, no demo: se ejercita el path de reconciliación real). **Veredicto A (#41-R3):** el reconcile lee `rule.userId` del doc Mongo en dos puntos que reproducen la geometría original de la creación — `emqxapi.js:296` (FROM del SQL, `topic = rule.userId + "/" + rule.dId + "/+/sdata"`) y `:304` (`payload_tmpl`, `"userId":"' + rule.userId + '"'`). Migrar el doc primero ⇒ EMQX queda con SERVICE en ambos lugares. **Orden obligatorio:** script → `docker restart node`; un restart previo recrearía las 10 reglas con `userId=PERSONAL` (Fuga a evitar, no accidente teórico: `initEmqxResources` se dispara top-level en el `require` del router, `emqxapi.js:534`). **Backup+`--restore`:** JSON pre-migración con los 10 docs completos en `seeds/_dev/` (gitignored, precedente TENANT-4). Restore reproduce el estado observado pre-migración: `saverrules` con `userId=PERSONAL` + `emqxRuleId` huérfanos + EMQX Rule Engine en 0. Scope: 10 docs cuyos `dId` pertenecen a devices con `userId=SERVICE`. Exclusiones (consistente DEC-REF-36/37, sellado #41-R2 B5): `data` histórico (465 docs, lectura por `dId` sin `userId` en `historyProvider.js:14`), `notifications` (24, gateadas por Site vía DEC-REF-37 DENY), `templates` (4, BACKLOG-TENANT-8 decisión de producto). |
| DEC-REF-52-A | 2026-07-05 | **Adenda #41-R4/R5 — causa raíz refinada y fix del self-heal.** La ejecución del script (R4) migró Mongo limpio (10/10 saverrules PERSONAL→SERVICE), pero el `docker restart node` posterior NO gatilló `reconcileRules()`: `waitForSaverResource` (`emqxapi.js:527-531`) hizo timeout (120s, 38 intentos × 3s) porque los 3 resources (`rule/alarm/saver-webhook`) estaban persistidos en EMQX con `url=http://localhost:3001/...` — desde el contenedor `emqx` esa URL apunta al propio `emqx` que no tiene backend, `is_alive:false/None`. Causa raíz refinada: pérdida de estado EMQX ~2026-06-01 (BACKLOG-OPS-1) + `WEBHOOKS_HOST=localhost` en `app/.env` (deriva vs. CLAUDE.md que documenta `WEBHOOKS_HOST=node`, hostname del contenedor node en la red docker) → recreación silenciosa de resources con URL inalcanzable → self-heal bloqueado. Fix aplicado en R5: (a) `app/.env` corregido `WEBHOOKS_HOST=localhost → node`; (b) DELETE de los 3 resources muertos vía API EMQX (`emqxapi.js:78-88` es create-if-missing puro; no UPDATE de URLs de resources existentes; sin DELETE el bootstrap saltea y persiste el bug); (c) `docker restart node` → bootstrap recrea los 3 resources con URL `http://node:3001/...` → resources vivos → `reconcileRules()` ejecuta → 10 rules SERVICE con relink de `emqxRuleId`. El tratamiento a los 3 resources (no solo saver) fue aprobado por Franco: es el mismo fix, mismo self-heal, corrige de una vez toda la deriva. |
| DEC-REF-53 | 2026-07-06 | **Reconciliación RulePack productivo `cummins-pcc-v1` (A4, sesión #42).** Cierra BACKLOG-RULE-3 con cuatro decisiones de sala. **D1 (clase connect):** rename en el pack productivo — `oil_pressure_psi → oil_pressure` y `fuel_level_pct → fuel_level` (opción (a) de las tres registradas en RULE-3; blast radius acotado por recon #42-R1 B1: solo `seeds/cummins_pcc_v1.js` + 1 archivo dev, cero uso en frontend/motor/sim de runtime). Incluye corrección de la incoherencia de unidad en A1: unidad canónica **Bar** (no `kPa` con nombre `_psi`), umbrales según biblioteca de campo — warning `<2.0 Bar`, crítico `<1.0 Bar`. Opciones (b) mapping en bridge Modbus y (c) reglas duales transicionales **descartadas**: infraestructura especulativa y doble mantenimiento respectivamente. **D2 (clase inferred):** reglas D1 `cummins-D1-service-due-soon` y D2 `cummins-D2-service-overdue` se **RETIRAN del pack productivo** — referencian `hours_to_next_service_250`, variable derivada que no existe en template/sim/edge (recon #42-R1 B1). Diseño preservado en BACKLOG-RULE-4. Alternativa evaluada y descartada por scope: publicar `run_hours` en sim + derivación en edge (mantenimiento predictivo demo) — queda recuperable vía RULE-4. **D3 (graceSec):** formalizar `graceSec` en el schema `rule_definition.js` **ANTES** de sumar reglas cross por vía canónica del seed productivo (`findOneAndUpdate`/mongoose silenciaría el campo por strict:true, bug latente clase `mode:'window'` #37 — recon #42-R1 B2). Sin migración: todas las reglas actuales tienen `graceSec:undefined`, la formalización solo abre el path futuro. **D4 (alcance cross):** el pack productivo suma **UNA regla cross** — `mains-loss → gen-no-start` (única cadena validada E2E en A3.3 con escenario `mains_failure_gen_no_start` existente, DEC-REF-49). Cascada energética completa **DIFERIDA** — bloqueada por BACKLOG-SIM-4 (dominio real de `transfer_state`, criterio Área 1/3). |
| DEC-REF-53-A | 2026-07-06 | **Adenda #42-R4/R5 — refinamientos de sala posteriores al registro original. Registrada como fila propia por convención (la fila 53 fue editada retroactivamente en R5 por error de proceso, corregido en R6).** Al bajar el diff, sala refinó dos puntos sobre R3: **(i) A0 warning** — A1 crítico `<1.0 Bar` se acompaña de nueva regla warning `cummins-A0-oil-pressure-low` (`oil_pressure < 2.0 Bar`, cooldown 300s propuesto, defensa en profundidad). Verificado en `typeD.js` (leído #42-R2) que no hay estado compartido entre reglas — cada regla evalúa contra su propia condition, cooldown propio por `rule.ruleId` en `fireAlarm` (`ruleEngine.js:131`). Cuando `oil_pressure < 1.0` disparan ambas, sin interferencia. **(ii) Opción (b) de D4 — madre M1** — la regla cross C1 lleva madre para que la vista de cascada (DEC-REF-41/50) tenga `correlationParent` real. Se agrega alarma D `cummins-M1-mains-loss` sobre el ATS (base fixture E2E `e2e-mains-loss-root` de A3.3, bitácora #40-R7). Geometría: regla D con `deviceType:'ATS'` dentro de pack `deviceType:'cummins-pcc'`. Verificado estáticamente contra `ruleEngine.js:29-30` (recon #42-R4 B4): el gate usa `rule.deviceType`, NO `pack.deviceType`; el motor recorre todos los packs y todas las reglas por mensaje entrante, sin filtrar packs por su `pack.deviceType`. Geometría válida por construcción. **(iii) `C1.correlationParent = 'cummins-M1-mains-loss'`** — reemplaza el `null` del R3, cadena madre→hija persistida. Migración futura de M1 a pack ATS propio: **BACKLOG-RULE-5**. **Pack final aprobado**: 5 reglas — A0 (warning oil), A1 (critical oil), G2 (fuel), M1 (madre mains-loss), C1 (cross hija con parentesco M1). Version 2. Validado E2E en R5 (bitácora #42): M1 a T0+31ms, C1 a T0+99.4s con `correlationParent:"cummins-M1-mains-loss"` persistido, cooldown 300s sostenido, restore limpio. |
| DEC-REF-54 | 2026-07-06 | **Bloque #43 A5-A9: alcance total sin diferimientos (directiva Franco, sesión #43).** Los cuatro frentes que el recon R1 identificó como candidatos a A5-A8 (feed de alarmas por site, ACK auditable, consola de reglas superadmin, endpoints de sites) se absorben junto a los backlog vigentes relacionados como bloque A5-A9 de la misma sesión, sin backlog residual. **A5 = feed de alarmas del detalle de site (DEC-REF-43):** handler dedicado `GET /site/:siteCode/alarms` en `sites.js`, cursor por `time`, mapa `{variable → peor severidad 15 min}` con la MISMA ventana calibrable de `/sites/status` (DEC-REF-27, reusa la misma fuente, no hardcodea), response con `correlationParent`/`mode` crudos; la agrupación visual madre→hija de DEC-REF-50 la resuelve el frontend a partir de esos campos. **A6 = write-authorization + ACK auditable + cierre TENANT-3:** `buildWriteFilter` genérico en el mismo módulo que `buildReadFilter` (espejo exacto de semántica de grants, DENY por defecto para derivados sin tenencia propia — coherente con DEC-REF-37); campos `acknowledgedBy` (userId) + `ackAt` (timestamp) al schema `notifications` en ambos lados (app + edge), separados de `readed` (DEC-REF-45/46); endpoint `PUT /site/:siteCode/alarms/:notificationId/ack` idempotente (primera atención humana es la que vale); notifs preexistentes sin migración (`ackAt: null`); writes de Site/Device/Notification pasan a estar gateados por `buildWriteFilter` (fin de "protegidas por accidente" de DEC-REF-33). **A7 = Zone CRUD gateado + real-time-lite (DEC-REF-44):** ruta `zones.js` nueva con CRUD gateado por el mismo criterio jerárquico; en el frontend `layouts/default.vue` el handler de `notif` reemite `wanomi:notif` al bus de Nuxt (además del toast/badge existentes, sin quitar nada) y la vista de detalle de site escucha para disparar re-fetch acotado del feed + recoloreo, sin abrir tópicos MQTT nuevos (usa la ACL browser de DEC-REF-38). **A8 = consola superadmin (DEC-REF-42) absorbiendo BACKLOG-RULE-2, BACKLOG-EDGE-2 y DEC-REF-47:** CRUD HTTP de RulePack + página frontend con constructor visual de `crossExpr` (árbol AND/OR/comparaciones + hoja de suma DEC-REF-47), reload del motor edge (BACKLOG-EDGE-2 = hot-reload), catálogo↔tenancy (BACKLOG-RULE-2 = `operatorId`/overrides por Zone). RBAC vía rol `superadmin` (ya en `user.js` enum). **A9 = OPS-1/OPS-2/API-1** (durabilidad reglas EMQX + deriva `.env` ↔ CLAUDE.md + hardening enrichments `GET /device`) como pull operacional único. **RISK-SEC-1/2 mantiene trigger registrado** (rotación diferida al despliegue a producción, sin cambio). Convención: los backlog absorbidos NO se editan (sus filas quedan como precedente inmutable); se agregan notas de absorción en `>` blockquote debajo de la tabla respectiva (patrón `> BACKLOG-TENANT-7 — RESUELTO en #34` de §5a). |

| DEC-REF-55 | 2026-07-07 | **Fix RTL — cierre de la regresión DEC-REF-38 vs publishers de notif (Franco, sesión #43/R7).** GATE 3.c de #43 rojo por causa raíz confirmada en R6: publishers de notif (edge `notificationRouter.js:51` + legacy `webhooks.js:369, 431`) publican a `${userId}/dummy-did/dummy-var/notif` — formato pre-DEC-REF-38 no migrado. La ACL B-narrow del cellowner (DEC-REF-38, `emqxauthrules` reescrita post-#33) autoriza `${owner}/${realDid}/+/notif` — `dummy-did` en pos 2 no matchea ningún dId real → EMQX bloquea la entrega, browser nunca ve el mensaje, toast + `$emit('wanomi:notif')` no disparan. Fix: **tópico pasa a `${owner}/${dId}/alarm/notif`** — segmento 3 FIJO literal `alarm` (la variable viaja en el payload, NO en el tópico; esquiva la ruptura por variables con `/` como `n/a` que usa C1 al no tener variable de disparo propia). Payload migra de texto plano a **JSON**: `{siteId, severity, ruleId, variable, message, time, correlationParent, mode}`. `message` = el texto humano que hoy es todo el payload (toast no pierde legibilidad). Paridad **obligatoria** en los TRES publishers (edge + los dos webhooks legacy) — mantener un canal en formato viejo revive el bug. ACL sin cambios (`${owner}/${realDid}/+/notif` ya cubre `alarm` en pos 3 vía el `+`). Frontend: `default.vue` parsea JSON con try/catch (fallback: string crudo → `message` para robustez ante mensajes en tránsito en formato viejo durante el rollout); `$emit('wanomi:notif', payload)` con OBJETO en lugar de string. `_siteCode.vue` gana filtro `if (payload && payload.siteId === this.siteCode) this.loadAlarms()` — fin del re-fetch ciego (evita re-fetch cuando la notif es de otro site del scope del cellowner). |
| DEC-REF-56 | 2026-07-07 | **Inhibición de alarmas de aceite con motor parado (Franco, sesión #43/R7).** A0 (warning) y A1 (critical) del pack `cummins-pcc-v1` (DEC-REF-53, `oil_pressure < 2.0 Bar` / `< 1.0 Bar`) pasan de condición simple a condición **compuesta**: `<motor corriendo> AND <oil_pressure < umbral>`. Fundamento de campo: un genset detenido reporta `oil_pressure=0` por física (bomba de aceite depende del cigüeñal); la alarma real de aceite solo tiene sentido con motor en marcha. Sin inhibición, A1 es **falso-positivo cada cooldown** en cualquier sitio con red estable (el sim lo evidenció durante #43/R6 — feed inundado, `variableSeverity:oil_pressure:critical` sostenido — pero el bug es de **producto**, no del sim: en un genset real con AC estable, oil_pressure también leería 0 con motor apagado). La variable/mecánica exacta de "motor corriendo" (`rpm > umbral` del propio CUMMINS, o `gen_status='RUNNING'` del ATS vía cross) se resuelve en el recon micro de Fase 2 y se registra como adenda **DEC-REF-56-A** con la evidencia de disponibilidad y rango. Pack `cummins-pcc-v1` bump **version 3** (canónico por seed). M1/C1 NO se tocan (concern separado). |
| DEC-REF-56-A | 2026-07-07 | **Adenda DEC-REF-56 — variable elegida `rpm` del propio CUMMINS + geometría compound-single-device (recon Fase 2, #43/R7).** Recon read-only: (i) `typeD.js:10-26` acepta una sola condición op+value → NO expresa compound sin código nuevo (punto de frenado del prompt evitado). (ii) `typeCross.js:16-119` (DEC-REF-47) tiene AST AND/OR con hojas `{deviceType, variable, condition}` — SÍ expresa compound tal como está. Ambas hojas apuntan al MISMO device (`deviceType: cummins-pcc`); `findDeviceByType(siteState, 'cummins-pcc', siteCode)` (typeCross.js:53) devuelve el único CUMMINS del sitio, mismo devState para ambas leaves — funciona por construcción. (iii) `ruleEngine.js:14-27` evalúa cross en cada mensaje (bypass del gate `rule.deviceType !== deviceType` de línea 29) — la regla dispara cuando la compound activa, no cuando la variable primaria publica. Compound-single-device es geometría válida en typeCross (no requiere que las hojas estén en devices distintos). (iv) Variable de "motor corriendo": **`rpm` del propio CUMMINS** — el sim publica `rpm=0` en reposo (`sensor-engine.js:59` initial) y target `1500` con `sharedState.gen_running=true` (`:124`), ramp ±100/tick; umbral `rpm > 300` separa "coasting/off" de "arrancado" sin ambigüedad (idle real Cummins ≈ 1500 rpm nominal). Descarté `gen_status='RUNNING'` del ATS por cross-device — introduciría un salto de device sin necesidad, mismo device es más simple. **Diseño final de regla**: `type: 'cross'`, `graceSec: 0` (compound-activa dispara inmediato; el cooldown por-ruleId protege contra fires excesivos por flapping), `deviceType/variable` top-level `cummins-pcc/oil_pressure` (metadata para coloreo — cross bypasea el gate igual), `condition: null` (compound vive en crossExpr), `correlationParent: null` (no forma cadena — no cascada), `crossExpr = AND(rpm > 300, oil_pressure < umbral)`. **Notas cosméticas**: `mode:'cross'` en la notif (fireAlarm en ruleEngine.js:17-25 lo hardcodea para cross; enum del schema ya lo acepta); `value:null` en la notif (el fire path cross pasa `value:null` — el frontend puede resolver el valor primario por otras vías si necesita). Semántica de mode:'cross' se amplía de "cross-device cascade" a "evaluado por AST cross" — cascade se distingue con `correlationParent != null`. |
| DEC-REF-57 | 2026-07-07 | **A8: alcance y orden de sub-frentes.** A8 se ejecuta en 5 sub-frentes: SF-1 CRUD HTTP RulePack · SF-2 RBAC/tenancy · SF-3 hot-reload edge · SF-4 resolve events · SF-5 consola + constructor visual. Orden aprobado: SF-1 → SF-3 → SF-5 (en paralelo con SF-3) → SF-4 → SF-2 mínimo (absorbido en SF-1, ver DEC-REF-60). Nota terminológica: en A8 y en adelante, BACKLOG-EDGE-2 = hot-reload de packs en el motor edge; el uso previo de la etiqueta (escalada temporal del fallback, dentro de DEC-REF-26) está implementado y cerrado. El doble uso queda tombstoneado aquí. |
| DEC-REF-58 | 2026-07-07 | **Hot-reload del motor edge (SF-3).** Canal: tópico MQTT de control `wanomi/edge/${SITE_ID}/reload` (segundo subscribe del edge). Requiere línea de ACL en EMQX — cae bajo el paraguas de BACKLOG-OPS-1 (durabilidad de config). Criterio: producto-real (DEC-STRAT-2) — en topología de Hub remoto detrás de NAT, MQTT es el único canal de control que sobrevive; HTTP en el edge y polling delta descartados. Política de estado en reload: regla eliminada → limpiar todas sus keys (cooldownState/windowState/crossState); regla editada (contenido distinto) → limpiar sus keys, la definición nueva rige desde cero; regla intacta → preservar todo, el reload no perturba lo que no cambió. |
| DEC-REF-59 | 2026-07-07 | **Resolve events (SF-4).** El motor emite evento de resolución al detectar la transición activa→inactiva de una regla. Persistencia: campo nuevo `kind: 'fire' | 'resolve'` en `notifications`, default `'fire'` — las 985 notifs históricas se leen como `fire` sin migración; `mode` conserva intacta su semántica de método de evaluación. El payload MQTT de DEC-REF-55 se extiende con el mismo campo (aditivo, no rompe consumers). UX: toast de resolución visible y visualmente inconfundible respecto del de falla (paleta verde/azul, texto tipo "Resuelto: …"). Decisión reversible con criterio explícito registrado: si en pruebas de uso real resulta ruido, se suprime el toast y queda solo el re-fetch silencioso. |
| DEC-REF-60 | 2026-07-07 | **RBAC RulePack mínimo fail-close (SF-2 mínimo).** `scopeFilterFor` gana rama `'RulePack'`: superadmin → `{}` (match all); cualquier otro rol → DENY fail-close en lectura y escritura. Tenancy completa (`operatorId` + overrides por Zone) diferida hasta la incorporación de un 2º operador — no-hardening especulativo, coherente con DEC-REF-33/-37: con un solo operador el aislamiento no es validable contra realidad. Deuda registrada en BACKLOG-RULE-6. |
| DEC-REF-60-A | 2026-07-07 | **Adenda DEC-REF-60 — patrón "upsert + DENY_FILTER = creación, no bloqueo" (ratificación de sala, #44/R3).** Durante SF-1 el agente detectó que gatear PUT/DELETE con `buildWriteFilter` (letra del prompt R2/B3) habría abierto un hueco: en Mongo, `findOneAndUpdate` con `upsert:true` y un filtro que no matchea (`DENY_FILTER = {$expr:false}`) **no bloquea — crea** un documento nuevo con los campos del update. Regla general que esta adenda fija para toda ruta futura: **RBAC de escritura sobre endpoints con upsert va por chequeo explícito de rol ANTES del write, nunca por filtro**. Se ratifica también la segunda decisión del agente: la rama `'RulePack'` de `scopeFilterFor` retorna `DENY` (Symbol) explícito y no `null` — con `null`, el 0-match sería coincidencia de schema (RulePack sin `userId`), no diseño, y la aparición futura de `userId` abriría el candado en silencio. Ambas desviaciones del prompt fueron mejoras verificadas en disco, ratificadas por sala y Franco en GATE 2. |
| DEC-REF-61 | 2026-07-07 | **Diseño fino SF-3: hot-reload del motor edge (sala + Franco, #44/R4→R5).** Cierra las cinco decisiones abiertas del recon R4 sobre el marco de DEC-REF-58: (a) ACL del canal de control: statu quo — `wanomi/edge/+/reload` queda alcanzable solo por la credencial `superiotix` (única con publish wildcard; verificado en R4 que browsers/devices/sim no llegan). Usuario dedicado `edge-control` con ACL acotada al canal: diferido al checklist de despliegue a producción junto a RISK-SEC-1/2, con nota explícita: el Hub físico portará la credencial en campo — un Hub comprometido con `superiotix` tiene el broker entero; la llave acotada se fabrica antes de esa mudanza. (b) Trigger: automático post-write — PUT/DELETE de `rulepacks.js` publican el reload al confirmar la escritura (cliente `global.mqttClient` existente, permiso verificado). Guardar = aplicar; botón manual recuperable en SF-5 si la consola lo pide. (c) Payload: señal de "recargar todo" (contenido del mensaje ignorado). El costo de releer el catálogo completo es despreciable en volumen actual y proyectado; la selectividad por pack agregaría complejidad sin beneficio. (d) Detección de regla editada (materializa D3 de DEC-REF-58): huella SHA-256 por regla (`JSON.stringify` de la definición), snapshot `Map<ruleId, hash>` en memoria del motor. En reload: hash igual → preservar estado; distinto → limpiar keys de esa regla; ruleId ausente → limpiar; ruleId nuevo → entra de cero. Descartados `version`/`updatedAt` del pack como proxy: violan D3 (invalidarían reglas intactas). (e) Estructura: `loadPacks()` se parte en dos funciones — carga/filtrado de packs e hidratación de siteState — porque la hidratación re-ejecutada pisaría valores vivos de los devices con datos históricos (motor ciego post-reload, hallazgo R4/B3.8). Boot llama ambas en secuencia (comportamiento idéntico); el reload llama solo la primera. Sin ramas condicionales tipo "si es reload, saltear". |
| DEC-REF-61-A | 2026-07-07 | **Adenda DEC-REF-61 — tópico broadcast de reload (ratificación, #44/R5).** El auto-publish post-write (61.b) publica a `wanomi/edge/all/reload` (broadcast), no al tópico por-site: los packs son globales por `deviceType` y el writer no conoce qué sites los usan — iterar sites acoplaría el CRUD al modelo Site sin necesidad. Cada edge suscribe **ambos** tópicos: `wanomi/edge/${SITE_ID}/reload` (dirigido, reservado para reload manual por-edge desde SF-5) y el broadcast. La ACL vigente (`wanomi/edge/+/reload` bajo `superiotix`) cubre ambos sin cambio. Resolución del agente ratificada por sala y Franco. |
| DEC-REF-62 | 2026-07-08 | **Diseño fino SF-5: consola superadmin (sala + Franco, #44/R6→R7).** Cierra los seis puntos abiertos del recon R6: (a) Grants en frontend: se consume `state.auth.userData.grants` — ya viaja en el response de login y sobrevive F5 vía localStorage (verificado R6/B1, cero cambio backend, shape-additive: ningún consumidor existente lee grants hoy). Al montar la consola se revalida el rol con `GET /me` (grants frescos de DB) — defensa contra grant revocado post-login. El doble chequeo aplica solo a la consola, no a menú ni navegación. (b) Middleware de rol: array `middleware: ['authenticated', 'superadmin']` — primer uso del patrón array en el codebase (Nuxt 2.14.7 lo soporta), un concern por middleware. `superadmin.js` lee grants post-rehidratación de `readToken`; sin rol → redirect a `/dashboard`. (c) Sidebar: un ítem nuevo condicionado por computed `isSuperadmin` — primer patrón de visibilidad por rol en el layout, mínimamente invasivo. (d) Arquitectura: `pages/rulepacks/index.vue` (listado, BaseTable con acciones por slot) + `pages/rulepacks/_packId.vue` (detalle/edición con editor), espejando `sites/*`. El editor no va en modal. (e) Editor crossExpr: componente recursivo `<CrossExprNode>` (primer patrón recursivo del codebase; atención a `key` en `v-for`). v1 activa AND/OR + hoja equipo. La hoja de suma NO queda en backlog: se activa en SF-6 (DEC-REF-63) — el componente se diseña desde v1 contemplando el tipo de hoja adicional. Límite de profundidad (8) avisado client-side pre-submit. Errores 400 mostrados con patrón `$notify` vigente con razón textual. (f) Blindaje cuenta demo: checklist E2E manual con ambas cuentas ejecutada por Franco al cierre de cada capa que toque navegación. Sin tests automatizados (sin infraestructura de testing en el proyecto; si se incorpora, será decisión propia). Nota operativa: frontend en producción — cada capa requiere `nuxt build` completo. |
| DEC-REF-62-A | 2026-07-08 | **Adenda DEC-REF-62 — revalidación /me se extiende al detalle en Capa 3 (#44/R8→R9).** En Capa 2 la página de detalle no repetía `GET /me` por ser read-only (ratificado en GATE 7); al ganar el editor en Capa 3, `/rulepacks/:packId` pasa a ser superficie de escritura y adopta la misma revalidación fresca al mount que el índice. El criterio de DEC-REF-62.a se precisa: doble chequeo en **toda superficie de escritura de la consola**, no solo el índice. Nota operativa registrada: log del edge en `/tmp/edge_r5bis.log` (volátil) — migrar a ubicación estable en el próximo relanzamiento planificado del motor (SF-4). |
| DEC-REF-62-B | 2026-07-08 | **Adenda DEC-REF-62 — ratificaciones de alcance Capa 3 + SF-7 (GATE intermedio, #44/R9→R9-bis).** Franco ratificó tres decisiones del agente, con corrección sobre la primera: (i) tipos C y S quedan read-only **en la Capa 3** — su edición requiere mini-forms de config propia (setpointSource/EDGE-2 para C; window para S) y el pack productivo vigente es 100% D+cross. Por criterio de Franco (mínimo backlog salvo dependencia de equipo físico, y coherencia con el objetivo de A8 de eliminar el workflow de seeds — una consola que edita 2 de los 4 tipos que el motor evalúa obligaría a volver a seeds para los otros 2), la edición completa de C/S **no queda en roadmap difuso: se incorpora como sub-frente SF-7 de A8**. Orden actualizado: SF-5 → SF-4 → SF-6 → SF-7. (ii) `version` del pack se auto-incrementa client-side en cada guardado de reglas — sin interferencia con SF-3: el diff del motor compara hash POR REGLA y version es campo del pack; reglas intactas siguen reconociéndose intactas. (iii) Borrado de regla individual con confirmación simple (recuperable re-agregándola desde el mismo editor); la fricción de tipeo exacto queda reservada al borrado de pack completo — gradación de fricción proporcional al daño. |
| DEC-REF-64-A | 2026-07-09 | **Adenda DEC-REF-64 — pulido post-GATE 10 (Franco, #44/R11→R12).** Franco validó el motor de resolve events (GATE 10 verde sobre pipeline: fire → resolve-by-edit → coloreo híbrido) y relevó tres ítems de pulido que integran el cierre de SF-4: (i) Toast por severidad: el toast de fire deja de ser rojo fijo — mapa completo: critical → rojo (danger), warning → amarillo (warning), info → celeste/azul (info), resolve → verde (success). Coherente con la semántica de emojis ya vigente en Telegram. (ii) Pin de /sites en tiempo real: la página de listado/mapa gana el oyente del bus wanomi:notif con re-fetch del status (espejo del patrón DEC-REF-44 que _siteCode.vue ya tiene) — el color del pin cede ante fires y resolves sin refrescar. (iii) Telegram activo: corrección de dato — las credenciales existen en app/.env; el motor reportaba OFF porque los relanzamientos replicaron un entorno que nunca las incluyó (drift app/.env vs entorno de proceso, primo del incidente EMQX de #41). Se verifican los nombres de variables esperados contra los existentes, se relanzan con ellas, y se valida el ciclo completo fire→resolve en Telegram real. Corrección de honestidad al reporte R11: la rama Telegram NO estaba "verificada en tests unitarios internos" (el proyecto no tiene infraestructura de tests, DEC-REF-62.f) — estaba verificada solo por lectura de código; esta ronda la valida de verdad. |
| DEC-REF-64 | 2026-07-09 | **Diseño fino SF-4: resolve events (sala + Franco, #44/R10→R11).** Cierra los cinco puntos del recon R10 sobre el marco de DEC-REF-59: (a) Resolve por edición: una regla ACTIVA (fire vigente en `activeState`) que un reload detecta como editada o eliminada (D3) emite resolve al limpiarse, con `mode: 'resolve-by-edit'` en el payload para distinguirlo del resolve por condición. Principio: ninguna alarma abierta muere en silencio — todo RAISE tiene CLEAR visible que dice por qué. `cleanupStateForRules` se extiende para cubrir `activeState`. (b) Telegram: los resolves viajan por `notify()` a los cuatro canales, con rama de formato en `sendTelegram` (emoji verde, prefijo "Resuelto:"). Criterio de Franco (D5): medir en uso real antes de suprimir; reversible — si es ruido, se filtra (p.ej. solo critical). (c) Coloreo: híbrido en `/sites/status` y `variableSeverity` — `kind:'fire'` en el match + un resolve más reciente que el último fire de esa ruleId dentro de la ventana apaga el color al instante; la ventana de 15 min queda como red de seguridad para silencios sin resolve. (d) Escenario E2E: pack `_test-sf4` (canary:false) con `_sf4_d1` typeD `battery_voltage lt 20` (fire controlado) y `_sf4_x1` cross AND(mains_voltage > 100, battery_voltage > 5) (resolve natural vía `mains_failure` + `mains_restore`). Cubre: fire real → estado real en Maps → edición con `keys borradas > 0` enumeradas (cierra deuda D3 de R5-bis) → resolve-by-edit → resolve-by-condition → toast verde (validación visual de Franco) → `kind` en Mongo. (e) Log estable: el edge migra de `/tmp/edge_r5bis.log` a `logs/edge-${SITE_ID}.log` bajo el repo, `logs/` en `.gitignore`, en el relanzamiento planificado de esta ronda. Rotación: pendiente operativo menor, no bloqueante. Implementación: `kind: {enum:['fire','resolve'], default:'fire'}` en AMBOS schemas (paridad notifications.js + inline de notificationRouter.js); `activeState` Map en el closure de `start()`; `fireResolve` simétrico a `fireAlarm` reusando `notify()`; ganchos en ruleEngine.js (typeD/S `!triggered` con `activeState.has`) y typeCross.js (boolean de `delete(firedKey)`); `default.vue` condiciona toast por `payload.kind` (success verde + "Resuelto: "); `_siteCode.vue` sin cambio. Limpieza incidental autorizada: case 'cross' muerto de ruleEngine.js:131-133. |
| DEC-REF-63 | 2026-07-08 | **SF-6: hoja de suma cross-equipo entra al alcance de A8 (Franco, #44/R6).** Por criterio de Franco — mínimo backlog salvo dependencia estricta de equipo físico — la capa 2 de DEC-REF-47 (hoja aritmética `{sum:[...], condition}` para carga combinada, caso real power plant Eltek de Claro) deja de estar diferida sin fecha y se incorpora como sub-frente SF-6 de A8. Es puro software: (i) el evaluador cross resuelve la hoja de suma (la validación de forma ya la reconoce; hoy retorna `sum-pending`), (ii) se retira el 400 `sum-pending` del CRUD, (iii) se activa el botón en `<CrossExprNode>`. Prerequisito a verificar por recon (no asumir): que el sim publique las variables a sumar (cargas Eltek); si no, el ajuste del sim es parte de SF-6 (el sim es producto). Orden actualizado de A8: SF-5 → SF-4 (incluye validación D3 heredada) → SF-6. Auditoría de diferimientos bajo el criterio de Franco en bitácora #44/R6: tenancy full sigue diferida (falta un 2º operador real, no software); llave dedicada del canal de control sigue en checklist de producción; BACKLOG-RULE-4 (mantenimiento por horas) marcado para re-auditoría al adaptar el sim (alternativa sim-publica-run_hours recuperable bajo el criterio nuevo). |
| DEC-REF-65-A | 2026-07-11 | **Adenda DEC-REF-65 — cierre presentación GATE 13 (Franco, #44/R17→R18).** Franco ejecutó la checklist visual de R16 y levantó dos anomalías que frenaron GATE 13: (i) los ALARM de la regla de suma llegan con `value:null` y `thresholdUsed:null` en toast, Telegram y Mongo — el total sumado y el umbral quedan invisibles al operador. R17 diagnosticó causa raíz: `evaluateSum` calcula el total pero solo devuelve el boolean tri-state; `evaluateCross` no lo propaga; `ruleEngine.js` hardcodea `value:null, thresholdUsed:null` al llamar `fireAlarm` para el path cross (heredado de DEC-REF-56-A antes de que existiera la hoja sum, único punto con "total" natural). La nota honesta al reporte que R16 opt-in-eó ("si Franco quiere el total en el toast, se agrega como commit adicional post-GATE 13") queda invalidada por criterio de Franco: sin el número visible el operador no puede juzgar la severidad — no es cosmética, es información crítica. Fix: `evaluateSum` retorna `{val, total}` preservando el tri-state true/false/null; `evaluateNode` propaga el total de la hoja que dispara; `evaluateCross` expone `{fired, resolved, sumTotal, thresholdUsed}`; `ruleEngine.js` pasa esos valores a `fireAlarm` en el path cross-con-sum en lugar de null. Reglas cross tipo tree (AND/OR de hojas equipo, sin sum) mantienen comportamiento actual — no hay agregado natural para representar. `sendMqttNotif` y `sendTelegram` no necesitan cambio de rama: reusan el `alarm.value`/`alarm.thresholdUsed` que ya consumen, y el `message` sale "[SEV] label | site | 267.4 A | umbral: 200 A" en lugar de "... | null" — camino más simple que agregar rama por mode. (ii) El pin del mapa en la página de detalle `/sites/:siteCode` no cambia color al recibir notif — se queda con el status de la primera carga. R17 verificó causa: el commit `722069a` de R13 tocó exclusivamente `pages/sites/index.vue` (`iconForStatus` + `setIcon` in-place); `_siteCode.vue` quedó fuera del alcance por diseño explícito de R13 ("`loadAlarms` ya era silencioso"), pero el pin del detalle depende de `this.status` que solo se setea en `loadDetail()`. Fix: replicar el patrón R13 en el detalle — el `_notifHandler` existente gana un refresh silencioso del status del site (`/sites/status` con lookup del siteCode, sin `loading`) y actualiza el marker vía `setIcon` sin re-crear el mapa. Corrección de honestidad al alcance R13: la sala/agente asumieron que "detalle sin skeleton" cubría todo el detalle; en realidad cubría el feed de alarmas pero no el pin del propio site — brecha que emergió recién cuando Franco tuvo abierta la pestaña del detalle durante un fire, no del listado. Ejecución en una ronda: R18 = fix motor (B) + reinicio planificado del edge (captura previa PID 49423) + fix frontend (C) + `nuxt build` + restart node + limpieza del residuo `_test-sf4-visual` heredado de la checklist GATE 10-bis (R12) + E2E técnico del total con evidencia Mongo/log/Telegram (D) + re-checklist para Franco (E). No introduce DEC-REF nueva sobre el diseño de SF-6 — solo salda dos brechas de presentación de decisiones ya vigentes (DEC-REF-65.a-e + DEC-REF-64-A ii). Autorización adicional de Franco (#44/R18): push a `origin/feature/telco-support` con auditoría previa de los 51 commits locales sin pushear (el remoto quedó rezagado desde el commit #44/R6 documentando "push #44/R6", ver bitácora `34dc4e0` — ninguna ronda posterior lo materializó porque no hubo orden expresa). |
| DEC-REF-65 | 2026-07-09 | **Diseño fino SF-6: hoja de suma cross-equipo (sala + Franco, #44/R14→R15).** Cierra los cinco puntos del recon R14 sobre el marco de DEC-REF-63/47: (a) Semántica de la suma — por deviceType: cada renglón `{deviceType, variable}` de la hoja sum se expande a todos los devices de ese deviceType presentes en siteState al momento de evaluar; el total es la suma de todas las expansiones. Un módulo nuevo en el site entra a la suma solo, sin editar la regla — se elimina la regla-fantasma inversa (inventario desactualizado que falsea el total en silencio). La exclusión de un device puntual no se soporta en v1 (caso operativo raro; si aparece, se diseña entonces). (b) Frescura obligatoria de sumandos: cada valor en siteState lleva timestamp de última lectura; si algún sumando expandido es más viejo que la ventana de frescura (proporcional a la cadencia de publicación del deviceType, criterio ≥2-3× cadencia — lección BUG-SIM-6), la hoja de suma no se evalúa ese ciclo y el motor loguea sumando, deviceId y antigüedad. Nunca totales con valores congelados; nunca ausente-como-cero. El "device que no reporta" es problema de las reglas de silencio (M1), no de la suma. (c) Simulador — ajuste dentro de SF-6: la familia Eltek del sim pasa a modelar N módulos rectificadores del mismo deviceType publicando carga individual con valores realistas y mecanismo de control para mover la carga durante pruebas (que el cruce de umbral sea provocable, no azaroso). Nombres de variables del protocolo real Eltek según el informe de equipamiento Claro del proyecto — no inventados. Cadencia definida y registrada (insumo de la ventana de frescura). (d) Validación API: se retira el 400 sum-pending de ruleValidation.js; la hoja sum se acepta con: array sum no vacío, cada renglón con deviceType y variable presentes, condition con op del set vigente y value numérico. Mismo camino de 400-con-razón-legible validado en Capa 3. (e) Editor: se activa el botón "hoja de suma" en `<CrossExprNode>` con mini-form (lista dinámica de renglones deviceType/variable + condition del total) — el modelo del componente la contempla desde DEC-REF-62.e; el render read-only existente pasa a editable. Va en R16 con validación visual de Franco. Ejecución en dos rondas: R15 = (a)+(b) motor, (c) sim, (d) API, E2E técnico (STOP GATE 12); R16 = (e) editor + E2E visual de Franco (GATE 13, cierra SF-6). Incluye reinicios planificados de sim (primero de la sesión — captura previa de cmdline/environ/cwd) y edge. |
| DEC-REF-66 | 2026-07-13 | **Diseño fino SF-7: edición de reglas C y S en consola (sala + Franco, #44/R20→R21).** Cierra los cuatro puntos del recon R20, con dos correcciones de Franco sobre la recomendación original de la sala: **(a) Mini-form C:** expone los campos que el evaluador consume — `setpointSource.variable`, `fallbackToD`, `on_missing_ref`, `condition {op, value}` (umbral de respaldo) — más `escalateAfterMinutes`, que por corrección de Franco deja de ser perilla desconectada: EDGE-2 se cablea en el motor dentro de SF-7 (typeC consume el campo: N minutos consecutivos sin setpoint → escalada de severity INFO→warning del aviso de no-ref; la lógica está diseñada desde el registro de EDGE-2). `setpointSource.register` y `.scale` quedan FUERA del form: son metadata del driver Modbus que consumirá el Hub físico contra el equipo real — diferimiento legítimo por dependencia estricta de fierros (criterio de Franco), documentado formalmente: trigger = integración del driver Modbus real (Área 3 / diseño del Hub físico); al llegar el fierro, los campos se exponen en un colapsable "avanzado" del mismo mini-form, sin rediseño. **(b) Mini-form S:** los tres campos de la ventana — `window.durationSec`, `window.countThreshold`, `window.matchCondition {op, value}`. `reset_behavior` queda fuera: no es perilla, es la punta de una feature completa sin diseñar — el reconocimiento de alarmas (ACK): botón en el feed, autoría y timestamp, estado nuevo en el ciclo RAISE→ACK→CLEAR, interacción con los resolves de SF-4. Se registra como candidata formal al bloque A9, con diseño propio y opinión del Asesor Telco (en telco el ACK es estándar de NOC). **(c) Regla dura de campos nuevos:** todo campo nuevo entra al schema ANTES que a la UI, en commit aparte, con smoke `db.rulepacks.findOne()` post-PUT verificando llegada a Mongo. Sin excepción (lección graceSec/DEC-REF-53 y `mode:'window'` de #37, ahora regla escrita). **(d) E2E de C — camino completo (Franco):** el sim aprende a publicar setpoints como lo haría el Cummins real (variables `setpoint_*` periódicas con valores realistas), cubriendo el camino calibrated sin fricción manual y para siempre — coherente con "el sim es producto" (DEC-REF-63). El E2E de S reusa `genset_no_start` + las reglas positiva/negativa del seed canónico de #37, sin cambios de sim. **Validación backend (cierre de la puerta fantasma):** `ruleValidation.js` gana `validateC` y `validateS` espejo del patrón sum — C: `condition` presente si `fallbackToD:true` u `on_missing_ref:'alarm'`; op en enum; value numérico si op aritmético; advertencia (no rechazo) por config semánticamente muerta (`setpointSource.variable` vacío queriendo calibrated, o no-ref+ignore que silencia siempre). S: `durationSec > 0`, `countThreshold ≥ 1`, `matchCondition` presente con op en enum y value numérico si aritmético — hoy una S malformada se descarta con warning mudo en el log (regla fantasma clase `siteState:29-43`). Se agrega también `validateD` mínimo (op en enum, value numérico si aritmético) — observación colateral del recon, mismo patrón, costo marginal. |
| DEC-REF-66-A | 2026-07-14 | **Adenda DEC-REF-66 — corrección de premisa (#44/R21):** `escalateAfterMinutes` NO estaba desconectado — lo consume `ruleEngine.js` (DEC-REF-26, #24, validado E2E) aunque no `typeC.js`; el recon R20 grepeó solo el evaluador. El mini-form C lo EXPONE como campo ya cableado (con tooltip explicando la semántica de DEC-REF-26: escalada del INFO de configuración, no de la alarma operativa). Fase B de R21 queda VACIADA — sin cambio de motor. El corpus advirtió esto en DEC-REF-57 ("BACKLOG-EDGE-2 tombstoneado — hot-reload; la escalada del fallback de DEC-REF-26 está implementada y cerrada"); error de sala registrado sin suavizar (clase DEC-PROC-2, recon con `grep` insuficiente que no cruzó la implementación con la letra canónica). Impacto en R21: se preserva la premisa de DEC-REF-66.a en lo semántico (el mini-form expone el campo) pero se cancela el trabajo de motor. E2E paso (3) de escalada verifica la implementación EXISTENTE — evidencia: `reason:'setpoint-unavailable-escalated'` en Mongo. |
| DEC-REF-66-B | 2026-07-14 | **Adenda DEC-REF-26/66 — el reset de EDGE-2 emite resolve (Franco, #44/R21→#45/R22).** Hallazgo de Franco en el E2E de R21 (primera ejercitación viva de EDGE-2 desde #24): el "reset en silencio" de DEC-REF-26 es pre-SF-4 — al recuperarse el setpoint tras una escalada, el motor limpia sus keys sin emitir resolve, y el pin queda warning hasta que la ventana de 15 min expire. Gap de integración entre DEC-REF-26 y DEC-REF-64.a ("ninguna alarma abierta muere en silencio"). Cambio semántico registrado: si `escalatedKey` estaba set al momento del reset, `ruleEngine.js` emite `fireResolve` con `mode:'resolve-by-setpoint-recovered'` antes de borrar las keys del episodio; el INFO de configuración (que no pinta pin, DEC-REF-27) sigue cerrando en silencio. DEC-REF-26 queda enmendada solo en ese punto; el resto (reactivo, tiempo-de-eventos, idempotencia, opt-in) intacto. |
| DEC-REF-67 | 2026-07-15 | **Fiabilidad del canal Telegram — retry acotado en `sendTelegram` (sala + Franco, #45/R22, cierre GATE Hallazgo 2).** Diagnóstico C1 (read-only): el resolve de `_sf7_s_pos` perdido (L3339) y un warning repetido perdido (L3330) son **ETIMEDOUT por-request** sobre un canal fire-and-forget sin reintento — NO formato, NO rate-limit, NO bug de la rama resolve. Evidencia decisiva: la huella es *timeout* (falla de red), no *rechazo* (falla de contenido) → descarta que el resolve venga malformado; ~30 ETIMEDOUT históricos en el log = fragilidad **estructural** del canal, no incidente aislado. Se atiende la duda de Franco ("¿por qué solo en recuperación?"): en la ventana hubo **dos** pérdidas, una sobre un warning y otra sobre el resolve — no es exclusivo del resolve; la muestra chica (2 fallas) creó esa impresión. El resolve queda más expuesto solo por **posición** (llega al final de la ráfaga del ciclo; el último de una tanda rápida es el más propenso al timeout), no por ser un mensaje distinto. **Fix Fase 1:** `sendTelegram` gana reintento acotado —3 intentos, backoff exponencial base 2s (2/4/8s) + jitter— **async/no-bloqueante** (`setTimeout`), preserva fire-and-forget hacia `ruleEngine` (la cadena de evaluación no espera al canal). Timeout explícito de request (`req.setTimeout` ~5s) para que el reintento sea útil. **Simétrico fire + resolve** (un fire perdido es peor que un resolve perdido — DEC-REF-64.a). **SIN dedup y SIN persistencia** en esta fase, aceptado y documentado: posible duplicado si el intento 1 llegó pero la respuesta expiró (Telegram no tiene idempotency-key); mensaje perdido si el edge muere entre intentos — gap que cierra BACKLOG-NOTIF-1. El reintento cumple **doble función (fix + diagnóstico)**: si la pérdida persistiera con 3 reintentos, la causa sería específica del resolve y se reabre investigación. Severidad acotada: el resolve NO se pierde del sistema, solo del canal Telegram; MQTT/Mongo cierra el episodio y el pin del browser resuelve por DEC-REF-64.c. **Telegram = best-effort en MVP; Mongo/MQTT = autoritativo** (queda documentado). **Alternativa evaluada y NO elegida (registro por pedido de Franco, #45/R22):** Opción 3 — solo `req.setTimeout` sin retry ni outbox: reduce cuelgues largos pero NO recupera el mensaje perdido → no cierra Hallazgo 2. Descartada como solución; conservada como **fallback de degradación**: si el reintento introdujera efectos no deseados en operación real (p.ej. duplicados molestos), se degrada quitando el bloque de reintento y conservando solo el `req.setTimeout`. |
| DEC-REF-66-C | 2026-07-15 | **Adenda DEC-REF-66 punto (c) — "campo nuevo → schema primero" se generaliza a valores de enum (sala + Franco, #46).** La regresión de #45/R22 (el fix del Hallazgo 1 agregó `mode:'resolve-by-setpoint-recovered'` sin extender los enums de `NotificationRO` [edge] y `Notification` [app]) mostró que el punto (c) de DEC-REF-66, escrito solo para "campos", dejaba fuera el **valor nuevo de un enum existente**. Regla generalizada: **todo campo nuevo O valor nuevo de enum entra al schema ANTES que a la UI/motor, en commit aparte, con paridad edge↔app obligatoria (mismo valor en `NotificationRO` y en `Notification`) y smoke post-cambio verificando llegada a Mongo.** El fix ya está en origin (`42255c1`, taggeado a DEC-REF-66-B); esta adenda es **doc-only**: escribe la regla que ese fix ya honró. Sin cambio de código. |
| DEC-REF-66-D | 2026-07-18 | **SF-7 CERRADO — E2E DEC-REF-66.d completo, A8 SELLADA (sala + Franco, #48).** Cierre formal del sub-frente SF-7 (diseño DEC-REF-66) con E2E producto sobre PROD post-fix OPS-1 recurrencia 3 (fix DEC-REF-52-A aplicado — ver adenda #48/R3 de BACKLOG-OPS-1). **División de roles del E2E:** Franco creó la regla C `_e2e66d_c1` desde el browser en `:3000/rulepacks/cummins-pcc-v1` — **los mini-forms de R23 (DEC-REF-66.a/.b, en origin desde #47 `fdea14e/30bc694`) validados con uso real**, no solo por PUT. Yo custodié hot-reload SF-3 y digestión del pack en el edge. Parámetros: `condition{lt,50}`, `setpointSource.variable:'coolant_temp_setpoint'`, `fallbackToD:true`, `on_missing_ref:'ignore'`, `escalateAfterMinutes:2`, `cooldownSec:60`, `severity:warning`. **Evidencia atómica de las 4 fases con timestamps y Δs:** (1) CALIBRATED 2026-07-18T12:48:39.405Z · `kind:fire mode:'calibrated' thresholdUsed:94.5 reason:'threshold-calibrated'` (setpoint real del sim, jitter alrededor de 95.0); (2a) FALLBACK 12:54:07.613Z · `kind:fire mode:'fallback' thresholdUsed:50 reason:'threshold-fallback'` (Δ trigger→transición = 94s, coincide cadencia setpoint@60s + procesamiento); (2b) INFO 2b 12:54:07.616Z · `kind:fire mode:'fallback' sev:'info' reason:'setpoint-unavailable'` (DEC-REF-24 en el mismo tick); (3) ESCALADA EDGE-2 12:56:12.049Z · `sev:'warning' reason:'setpoint-unavailable-escalated'` **Δ INFO 2b→escalada = 124.4s** (`escalateAfterMinutes:2` = 120s + un tick del sim, match exacto con DEC-REF-26 EDGE-2); (4) RECOVERY 12:58:38.766Z · `kind:'resolve' mode:'resolve-by-setpoint-recovered' reason:'setpoint-recovered' thresholdUsed:null` (Δ trigger→resolve = 93s), con **pin `/sites/status: CR00061 ok` sin esperar la ventana de 15 min** — cierre visible del DEC-REF-66-B fix Hallazgo 1 en producción real. Recommendation custom del fix aplicada: `'Resuelto: setpoint de "coolant_temp" recuperado.'`. Checklist visual de Franco (browser + Telegram + pin) confirmó las 4 fases. **Cleanup por PUT canónico** (5 reglas del seed `seeds/cummins_pcc_v1.js`, `version:34`), hot-reload SF-3 digirió el delete: `eliminadas: 1 [ _e2e66d_c1  ] · intactas: 5 · keys estado borradas: 1` (el motor limpió el episodio EDGE-2 completo). **D3 de DEC-REF-64.a probado sobre episodio EDGE-2 real — cierra el círculo DEC-REF-26 en producto**: la escalada quedó registrada como pin warning, el resolve por setpoint recuperado lo cerró trazablemente, ningún fire quedó "muriendo en silencio". `wanomi-edge` intacto todo el E2E (Up 41h · `restarts=1` sin cambio — los fixes de #46/#47 vigentes: DEC-REF-67 retry Telegram, DEC-REF-68 arranque autónomo, DEC-REF-66-B fix Hallazgo 1). **A8 SELLADA:** SF-1 (CRUD HTTP RulePack + candado superadmin, DEC-REF-58/60/60-A) · SF-3 (hot-reload edge SF-3, DEC-REF-61) · SF-4 (resolve events, DEC-REF-59/64/64-A) · SF-5 (frontend RulePack editor Capa 1, DEC-REF-62) · SF-6 (hoja de suma cross-equipo, DEC-REF-63/65/65-A) · SF-7 (edición reglas C y S con E2E producto, DEC-REF-66/66-A/-B/-C/-D). El bloque de A8 declarado en DEC-REF-57 (5 sub-frentes originales + SF-6 y SF-7 agregados en curso) cierra completo. **Hallazgos colaterales registrados (no bloquean el sello):** (a) los mini-forms de R23 no aplican `.trim()` al input de `ruleId` (Franco pegó `" _e2e66d_c1  "` con espacios accidentales de copy-paste; el motor evaluó igual — es artefacto UX) → **subitem de BACKLOG-UI-8**; (b) el mini-form C no expone `variableLabel`/`unit`/`recommendation` en la UI → los messages del edge muestran `coolant_temp` raw en lugar de "Temp. refrigerante" → **subitem de BACKLOG-UI-8**; (c) el handler `POST /api/saver-webhook` (webhooks.js) responde HTTP 500 ante payload `{}` sin `topic/payload` en lugar de validar → observación menor, **candidato al bloque A9** (hardening de webhooks bajo pull operacional); (d) el flip 404→401 de `alarms/rules` post-rebuild que #48/R2 vio y no se explicó — **no se manifestó en este E2E** con el estado post-fix DEC-REF-52-A; sigue etiquetado como "no resuelto, sin teoría" hasta que vuelva a aparecer. **Sello de A8** habilita el siguiente tramo del plan de #32: **UI-3/reports** con seeds estáticos (declarado como "siguiente", NO arrancado en #48). |
| DEC-REF-68 | 2026-07-15 | **Arranque autónomo del edge — crash-only containerizado en el compose de producción (sala + Franco, #46, Opción A).** Requisito duro de Franco: el Hub en campo se reinicia solo tras corte de energía, sin técnico. **Corrección de premisa por recon (DEC-PROC-2):** el carry-over atribuía la muerte a `ECONNREFUSED :1883` (EMQX); FALSO — 82 `ECONNREFUSED :1883` históricos, todos recuperados: `mqtt.connect` usa defaults de mqtt.js (`reconnectPeriod:1000`) y `on('error')` solo loguea. El asesino es **Mongo al arranque**: `mongoose.connect` rechaza → `.catch()` de `start()` con `process.exit(1)` pelado (`index.js:205-208`) → sin supervisor, muerto para siempre. Agravante: sin `unhandledRejection`/`uncaughtException` → muerte muda sin rastro. Pata (a) de #45 incompleta (append de 4 vars a `app/.env`, arranque aún dependiente de CWD e inline). **Topología (Opción A, decisión de Franco):** el edge se **containeriza** y entra al `docker_compose_production.yml` junto a mongo/emqx/node (los tres ya `restart: always`). El Hub de campo correrá Docker con Mongo local containerizado → el edge como servicio del mismo stack es la topología coherente (un solo `compose up`, una imagen que se versiona/envía; O&M de appliance). Elimina `nohup` y **cierra de raíz la deriva localhost** (hostnames de red `mongo`/`emqx`, misma clase de bug que DEC-REF-52-A/BACKLOG-OPS-2). **Modelo:** crash-only. En el arranque no hay estado en memoria que perder → morir-y-revivir ES correcto. **(a) env fuente única:** archivo dedicado `edge-engine/.env.edge` (gitignored; `.env.edge.example` sin secretos al repo) + `env_file:` en el servicio (equivalente compose de `EnvironmentFile=`) + `dotenv.config({path:<abs>})` para corridas manuales → cero dependencia de CWD; `MQTT_HOST=mqtt://emqx:1883` y `MONGODB_URI=mongodb://…@mongo:27017/iotix?authSource=admin` (CORRIGE el default del código, que apunta a DB `wanomi` sin auth — DB equivocada). **(b) supervisión = servicio `wanomi-edge` en el compose de PROD:** `restart: always` (consistente con los 3 vecinos; NUNCA `on-failure:N` — trampa Docker equivalente al `StartLimitIntervalSec=0` de systemd: haría rendirse al guardián). `depends_on: {mongo: service_healthy, emqx: service_healthy}` **como cinturón del cold-boot vía `compose up`, NO como sustituto** — documentado: `depends_on` NO se re-evalúa en reinicios por política ni en reboot del host (el daemon relanza sin re-esperar salud), justo el escenario power-cycle; ahí el que cierra la carrera es (c)+`restart`. Bind-mount del repo (`./app` + `./edge-engine`) como el servicio `node` hoy; `WORKDIR`+command para que `dotenv` y `require('../app/...')` resuelvan; `NODE_PATH=…/app/node_modules`; base `node:14`; bind-mount `./logs` (preserva log per-site, DEC-REF-64.e); auth con creds superuser (EMQX `ALLOW_ANONYMOUS=false`/`NOMATCH=deny`). **(c) resiliencia mínima en código — AMBAS obligatorias:** `process.on('unhandledRejection')` + `process.on('uncaughtException')` que loguean con detalle y LUEGO `exit(1)` (muerte muda → registrada; el guardián revive) + `serverSelectionTimeoutMS:5000` explícito en `mongoose.connect`. **El timeout NO es opcional** (corrección a la síntesis del recon): como `depends_on` no cubre reinicios por política ni reboot del host, en esos caminos el edge puede relanzarse con Mongo aún no listo; el fast-fail 5s hace ágil el ciclo morir-revivir (5s vs ~30s default). MQTT intacto. **Alternativa evaluada y NO elegida:** retry in-process para que el edge SOBREVIVA un blip de Mongo runtime sin reiniciar (preservar `siteState`/`activeState`/hashes DEC-REF-61). Descartada: 0 blips de Mongo runtime en el log; una conexión mongoose ya establecida tolera el blip por diseño; el `.catch` fatal es SOLO el connect inicial; los handlers de (c) convierten el caso raro en reinicio registrado. Reabre si aparece blip de Mongo runtime en el log. **Supuesto con disparador de hardware (sin backlog nuevo, minimum-backlog #46):** imagen de campo para el Orange Pi Zero 3 (base ARM, empaquetado final del appliance, deuda transversal `node:14` EOL) acoplada al despliegue; diseño y E2E de #46 corren sobre WSL2 x86 (Docker activo). Secretos reales bajo RISK-SEC-1. **Sin BACKLOG residual:** (a)+(b)+(c) implementadas y validadas E2E esta sesión. |
| DEC-REF-69 | 2026-07-18 | **Dashboard NOC operador multi-site — DEC-DASH-1a, R0 de #49 (sala + Franco).** Ejecución del split de dashboards heredado en la bitácora (DEC-DASH-1): `/dashboard` pasa a ser dashboard **operador NOC multi-site** scoped por tenancy (`buildReadFilter('Site')` + `resolveScopedDIds` — primitivas ya vigentes en `middlewares/scope.js`, recon §6 #49); el dashboard actual (**DEC-DASH-1b: doble select sitio+plantilla, configuración/debugging interno de plataforma**) se **renombra a `/dashboard-admin`**, protegido con `middleware/superadmin.js` y con link de sidebar visible solo para rol superadmin. Aterrizaje de todos los logins a `/dashboard` (NOC) — el recon §2 #49 (verificado con `grep -rn` separando redirects vs nav) fija el conteo real: **6 redirects preservan el path `/dashboard`** (`pages/index.vue:10` · `pages/login.vue:102` · `middleware/notAuthenticated.js:7` · `middleware/superadmin.js:26` · `pages/rulepacks/index.vue:191` · `pages/rulepacks/_packId.vue:508`) y **2 links del sidebar** (`layouts/default.vue:15` · `components/Layout/DashboardLayout.vue:14`) siguen apuntando a `/dashboard`. Los redirects NO se tocan (el destino sigue siendo el NOC); el sidebar gana un segundo link `/dashboard-admin` **condicionado a rol superadmin**. **Cards con labels en castellano + subtítulo con definición operativa (fija el "qué significa exactamente" para el NOC, evita ambigüedades post-Claro):** (i) **Sitios Online** — "transmitiendo dentro de cadencia" (cadencia declarada por template, no heurística); (ii) **Diésel 24h** — "Δ nivel promedio · %" (litros absolutos diferidos hasta que exista `tankCapacity` en metadata del site/device — decisión Confiabilidad); (iii) **Alertas** — "activas ahora" con ventana DEC-REF-64.c (15 min de red de seguridad + regla lastKind≠resolve); (iv) **Uptime** — "% telemetría esperada recibida · 7d" (label en inglés porque es término técnico consolidado en telco; definición operativa: `msgs_recibidos / msgs_esperados_según_cadencia_de_template`). **Opción A (elegida por Franco sobre B) — ALCANCE v1:** (1) franja KPI fija en la parte superior con los 4 cards; (2) chart de tendencia debajo con **selector de variable derivado de templates de devices visibles ∩ presencia real en `db.data`** (intersección — nada hardcodeado; si no hay dato, no aparece en el selector); (3) card del chart muestra Δ/rango de la variable seleccionada; (4) **drill-down por navegación a `/sites/:siteCode` existente**: click en pin del mapa → navega al detalle del site correspondiente; click en fila de la tabla de estado → mismo destino. **Semántica de agregación resuelta en BACKEND y transportada por el contrato del endpoint `/trend` como campo `aggregation: 'avg' | 'range'` — `avg` para niveles/porcentajes (fuel_level), `range` (min–max entre sites) para magnitudes físicas por site (temperaturas, tensiones). El frontend renderiza según contrato, sin mapeo propio (requisito explícito de Confiabilidad, #49). Clasificación futura vía metadata de template queda como nota, NO como scope de esta ronda.** **Endpoints (dos, separados por concern y por perfil de índice usado):** `GET /dashboard/noc?window=<24h|7d|30d>` para el agregador de KPIs + tablero por site con status y last-values (fuel/temp/mains) + histograma 7d × severity (regla "dos épocas" pre/post DEC-REF-59 para `kind`); `GET /dashboard/noc/trend?variable=<>&window=<>` para la serie de tendencia + el `aggregation` ya calculado (frontend NO re-agrega). **Stack:** Highcharts existente (`nuxt-highcharts@1.0.3`, recon §3 #49 lo confirma como único consumidor productivo — chart.js/d3 quedan en el template Black Dashboard sin uso operativo). **Cero dependencias nuevas** (DEC-STACK-1). Componentes KPI reutilizables scope red (multi-site) y scope site (single-site) — habilitan la reutilización futura en la vista `/sites/:siteCode` (**DEC-DASH-2**, site-page futura). **La premisa "seeds estáticos" del plan UI-3 de #32 queda SUPERADA:** el recon §1 #49 mostró 46.9 días de `db.data` reales (1.57M docs) con 5 curvas de `fuel_level` cubiertas + 2225 notificaciones históricas — se ejecuta sobre dato real, no sobre fixtures. **Índice nuevo autorizado por Franco para R1:** `db.notifications.createIndex({time: -1, severity: 1})` — cubre "notifs últimos 7d agrupadas por severity" que hoy hace COLLSCAN sobre 2225 docs (recon §5 #49). **Track de continuación: `BACKLOG-UI-9` — track del dashboard NOC y sus refinamientos:** resumen por-site diferido (agregado por siteId dentro de `/sites/:siteCode` — `_siteCode.vue` — **no dentro del dashboard**, decisión 4 de Franco #49), site-page / Hub display (**vista dedicada del display local del Hub instalado — Orange Pi existente — DEC-DASH-2 ya firmada, sesión dedicada futura comprometida**), filtros avanzados (por operador, por zona, por tipo de site). Alcance a definir cuando el dashboard NOC v1 esté vivo. |
| DEC-REF-70 | 2026-07-20 | **Arquitectura de navegación y limpieza de plataforma post-auditoría #50 (mandato Franco #49 → sala + Franco #50).** Cierra la auditoría GATE 1 de #50 con decisiones sobre navegación, kills, y refactor de duplicaciones. **(a) Sidebar operador final (orden fijo):** Panel (`/dashboard`) · Sitios (`/sites`, link NUEVO) · Histórico (`/history`, link NUEVO — cierra BACKLOG-UI-4) · Devices (`/devices`) · Templates (`/templates`) · Reglas de monitoreo (`/rulepacks`, `v-if="isSuperadmin"`) · Simulador (`/demo/simulator`, link NUEVO `v-if="isSuperadmin"` — mismo patrón que Reglas de monitoreo). **(b) KILLS confirmados por Franco sobre GATE 1:** `pages/rules.vue` (`/rules`) y `pages/alarms.vue` (`/alarms`) — reemplazadas por el motor edge/rulepacks; sidebar-items retirados. **Los routes backend `/rule`, `/alarm-rule`, `/rule-webhook`, `/alarm-webhook` NO se tocan** (webhooks EMQX consumidos por el broker, siguen siendo interfaz oficial de dispositivos EMQX-legacy). Verificación pre-kill en Paso 2 (dispositivos vivos + rules count + grep de consumers) es CONDICIÓN de ejecución. **(c) `pages/dashboard-admin.vue` KEEP sin link de sidebar** — ruta con guard `superadmin` intacta, revisión final diferida a la sesión dedicada de **DEC-DASH-2** (Hub display + site-page). **El `<el-select>` de device del navbar global sale de `DashboardNavbar.vue` y se muda dentro de `pages/dashboard-admin.vue`** — único cliente real hoy (verificado GATE 1). Misma lógica sobre `$store.state.devices` + `PUT /device`, scope local a esa página. **(d) `register.vue` — DECISIÓN DE DISEÑO SIN kill ejecutado:** el registro público queda marcado para cierre (alta futura por admin/CLI). Se registra **RISK-SEC-4** nuevo (blockquote debajo de la tabla DEC-REF): signup público abierto es exposición en demo a cliente. **Trigger de ejecución del kill: antes de la demo a Claro O antes del deployment productivo, lo que llegue primero.** **(e) `ContentFooter.vue` reemplazado por footer mínimo `"Wanomi · v{package.json.version}"`** — se lee la versión en build vía `require('../../package.json').version` o equivalente Nuxt, sin hardcodear. **(f) CSS `.site-pin` extraído a `app/assets/sass/dashboard/custom/_leaflet-pins.scss`** e importado desde `black-dashboard.scss`; los 3 bloques duplicados en `pages/sites/index.vue`, `pages/sites/_siteCode.vue` y `components/Noc/NocSiteBoard.vue` se eliminan dejando un comentario de una línea apuntando al archivo global. Cierra la convención frágil de "no-scoped en cada archivo con comentario" que ya generó el bug estructural de #49/R7 (pines invisibles en el Panel). **(g) `SidebarSharePlugin` — poda, no kill (decisión Franco):** (i) se REMUEVE del plugin el toggle dark/light — su lógica (`document.body.classList` toggle `.white-content`) se MUDA al navbar como botón sol/luna visible en `DashboardNavbar.vue`, mismo mecanismo, mismos consumers (observer en `pages/dashboard.vue` + prop `isLight` a los 4 componentes `Noc*` — cero cambio en el patrón #49/R3); (ii) el selector de color de fondo del sidebar QUEDA en el plugin; (iii) **color primario del sidebar = AZUL** por defecto, key `'blue'` del set del template (verificado en `SidebarSharePlugin.vue:83` — swatch `{ color: 'info', value: 'blue' }` — mapea vía CSS `.badge-info` al hex `$info=#1d8cf8` de `_variables.scss:105`; el `sidebarBackground` prop del `<side-bar>` acepta `blue|vue|orange|green|red|primary`); default actual `"primary"` (rosa/magenta `#e14eca`, `layouts/default.vue:140`) → **default nuevo `"blue"`** en el layout Y en el `active:true` del swatch del plugin. **Identidad visual — nota para Área 4 (branding, arquitecto Marketing):** el sidebar azul queda como color primario de plataforma, referencia para los docs de presentación y branding. **BACKLOG-UI-10 nuevo:** rediseño de `/demo/simulator` como consola de demo — diseño en sesión dedicada; en #50 la página solo gana link de sidebar superadmin sobre la implementación existente. **Cambios NO ejecutados en #50 (registrados pero diferidos):** kill de `register.vue` (RISK-SEC-4 con trigger propio); refactor UI de `/demo/simulator` (BACKLOG-UI-10); merge visual de la vista `/sites` con el mini-mapa del Panel (nota para DEC-DASH-2 — hoy `/sites` KEEP como vista full-screen del mapa, gana link de sidebar). **Convención de commits para FASE 2:** un commit atómico por concern (docs · kills template · kills alarms/rules · navegación · CSS), verificación de consumers previa a cada kill (grep + globalComponents), build único + `docker restart node` al final (autorizado explícitamente por Franco para el Paso 7). |
| DEC-REF-70-A | 2026-07-20 | **Adenda DEC-REF-70 punto (g) — refinamiento G del cierre #50 (corrección de Franco durante FASE 2).** El diseño original de DEC-REF-70 (g) declaraba: *"se REMUEVE del plugin el toggle dark/light — su lógica se MUDA al navbar como botón sol/luna visible en DashboardNavbar.vue"*. La ejecución del commit `6c2a0e6` (Paso 5, navegación) implementó eso al pie de la letra: `BaseSwitch` + `import BaseSwitch` + método `toggleMode` removidos del `SidebarSharePlugin.vue`; nuevo botón sol/luna con observer local agregado en `DashboardNavbar.vue`. **Franco corrigió el diseño en dos rondas visuales sucesivas post-Paso 7:** (1ª) *"regresa el BaseSwitch de dark/light y el import a SidebarSharePlugin, no tiene sentido sacar el switch si dejamos el select de colores en el toogle. Y como el select de dark/light no tiene ícono o no se observa vuelve a insertarlo en el toogle como te solicité antes"* — restaurar el switch en el plugin, argumento de coherencia visual (los controles de tema van juntos con el selector de colores). (2ª) Al preguntar si mantener también el sol/luna en el navbar, la respuesta fue *"No, quita el sol y la luna"* + *"agrega leyenda 'mqtt conectado' al lado de la luz"* + *"en minúscula mqtt"*. **Ejecución final (commit `22ef9ab`, refinamiento G):** (i) `SidebarSharePlugin.vue` — `BaseSwitch`, `import BaseSwitch` y método `toggleMode(isDarkMode)` RESTAURADOS, con sync inicial en `mounted()` contra `body.classList.contains('white-content')` para que la posición del switch refleje el tema real al montar (mejora vs el original — cubre el caso de tema persistido); (ii) `DashboardNavbar.vue` — sol/luna REMOVIDO, observer local del theme y estado `isLight` REMOVIDOS (el navbar ya no consume el tema); (iii) `DashboardNavbar.vue` — **leyenda `"mqtt conectado"` / `"mqtt desconectado"`** agregada al lado de la luz indicadora, en minúsculas, verde/rojo condicional, tipografía 0.82rem con tono muted (`.mqtt-status-label`). **Mecanismo intacto:** el observer en `pages/dashboard.vue` sigue detectando `body.white-content` y bajando `isLight` como prop a los 4 componentes `Noc*` — el patrón DEC-REF-69 R3 no cambia, sólo cambia QUIÉN toggeaba el body (ahora vuelve a ser el switch del plugin, no el sol/luna del navbar). **Motivo del error de diseño (sin suavizar, clase menor):** la sala firmó el punto (g) por analogía con "un lugar único de control" (mudar todo lo interactivo al navbar), sin cruzar la decisión con el resto del contenido del plugin (el selector de colores del sidebar YA vivía en el plugin y no se movía). Al quedar la mitad de los controles visuales en el plugin y la otra mitad en el navbar, el conjunto perdió coherencia — Franco lo percibió en el checklist visual y corrigió. **Familia de errores:** decisión de diseño tomada por analogía sin auditar la vecindad completa del componente afectado (misma raíz que DEC-PROC-2, versión light — no había recon técnico faltante, había recon UX faltante). Sin nueva DEC-PROC porque el fix visual es reversible y trivial; queda como lección explícita del cierre: **las decisiones visuales del corpus deben ser cruzadas contra el conjunto de controles del componente afectado antes de firmarse, no sólo contra su propia lógica**. Fila DEC-REF-70 queda intacta (append-only); esta fila reemplaza el punto (g) del original. |
| DEC-REF-71 | 2026-07-21 | **Cadena de autoría Template → Device → Regla como columna vertebral de configurabilidad (Franco, apertura #51).** El vocabulario del sistema (variables, labels legibles, unidades, tipos, widgets) se define UNA sola vez en el Template; el Device se asocia a un Template y hereda su vocabulario; la Regla se escribe SOBRE ese vocabulario heredado. Fuente única de verdad: ningún campo de vocabulario se tipea manualmente en la autoría de reglas — se hereda del template (labels/units read-only en los mini-forms). Corolario DEC-STRAT-2 ("Se diseña el producto, no la demo"): ningún eslabón de la cadena puede ser seed-only en el producto final — hoy los Templates se cargan hardcodeados por seed y la página de Templates no cubre su autoría completa, lo cual queda declarado formalmente como DEUDA DE PRODUCTO. Alcance de #51 sobre esta DEC: recon del estado real de la cadena (R1) + diseño de qué le falta al Template (schema + página) para ser fuente única + diseño de la herencia hacia los mini-forms de reglas. La IMPLEMENTACIÓN (CRUD de Templates en UI, asociación Device→Template en producto) es bloque propio post-#51, con backlog formal a registrar al cierre de la sesión — NO se implementa en #51. |
| DEC-REF-71-A | 2026-07-21 | **Adenda DEC-REF-71 — el estado real es peor que el declarado (evidencia R1 #51).** La fila madre declara "los Templates se cargan hardcodeados por seed". El recon R1 demostró que NO existe seed versionado que los inserte: grep sobre seeds/ sin hits para los 5 templates productivos (WN-SITE-SEC v2, WN-SITE-GEN v2, WN-ATS-InteliATS-PWR, WN-GEN-Cummins-PowerCommand, WN-ELTEK-SmartpackS — 42 widgets totales en db.templates de la DB iotix); origen no versionado (script one-off o import de otra instancia). Agravantes verificados: (i) NO existe PUT /template en backend — templates.js expone solo GET/POST/DELETE (:11,43,86); la edición no es un faltante de UI, es un faltante de ruta; (ii) el schema Template declara widgets como Array libre sin sub-schema (template.js:6-11) — los 42 widgets almacenados tienen un shape de 4 keys (variable, variableFullName, variableSendFreq, variableType) que la UI de templates.vue NI SIQUIERA PRODUCE (su shape rico incluye widget/unit/icon/class/column/etc.); (iii) divergencia de unidad EN PRODUCCIÓN: widget "Presión aceite (psi)" del template Cummins vs unit:"Bar" en la regla cummins-A1 — misma variable, dos unidades según la vista, sin ninguna línea de código que enforce consistencia; (iv) el mini-form de reglas usa variable como TEXTO LIBRE (_packId.vue:171) sin dropdown desde template — grep templateId en pages/rulepacks/ → 0 hits; el corte de la cadena es total; (v) correlationParent (cascada DEC-REF-53-A) tampoco es editable por UI — se declara SUBITEM del futuro backlog de implementación de la cadena (decisión Franco GATE 1.d), misma familia de deuda. Fila madre intacta (append-only). |
| DEC-REF-72 | 2026-07-21 | **Diseño del estándar de legibilidad + fuente única de vocabulario (sala + Franco, #51/R2, GATE 2). Cinco decisiones, implementación diferida a bloque propio post-#51.** Verificación previa R2.0 (read-only): el shape productivo de widget = 4 keys (variable/variableFullName/variableSendFreq/variableType) es contrato de facto — 11 consumidores sanos; el path productivo (site page → LiveValue.vue:36-37) fue escrito A CONCIENCIA del shape pobre (parsea unit del label con regex /\(([^)]+)\)/, hardcodea decimalPlaces por tipo); dashboard-admin.vue roto silencioso (legado template, KEEP sin link DEC-REF-70c). Dos vocabularios paralelos sin reconciliar; psi/Bar es su síntoma. **(D1) Sub-schema formal de widget:** núcleo obligatorio variable/variableFullName/variableType (enum float\|int\|bool\|categorical, superset DevicePanel) /variableSendFreq; `unit` asciende a campo formal (mata el regex de LiveValue); opcionales de presentación widget/icon/class/column/decimalPlaces/enumValues/tasmotaPath. Migración legacy: backfill MÍNIMO solo de `unit` (mismo regex que el runtime ya usa + limpieza del paréntesis en variableFullName) — corrige psi/Bar en la migración, no espera save espontáneo. **(D2) PUT /template canónico** (patrón PUT RulePack; hoy templates.js solo GET/POST/DELETE): advierte, NO bloquea — al guardar, cruza variables del template contra reglas de packs de deviceType coincidente y devuelve warnings[] (canal DEC-REF-66/R23) por regla huérfana. El diccionario no queda rehén de sus usos. Rename asistido en cascada = refinamiento futuro, no v1. **(D3) Herencia hacia mini-form de reglas:** `variable` deja de ser texto libre → selector poblado desde templates del deviceType; variableFullName + unit read-only heredados con señal de origen; el selector muestra label legible con variable técnica en secundario (Camila). Fallback legacy sin match: modo texto con badge "no encontrada en template" — no rompe, no oculta. **(D4, decisión de fondo) Fuente única de unidad = template, resolución en el PUT del pack:** backend resuelve variableLabel+unit desde el template al guardar la regla y los persiste DENORMALIZADOS en RuleDefinition. Edge sigue CIEGO a templates (cero cambio de motor, sorpresa 8 intacta, topología crash-only DEC-REF-68 intacta). Costo honesto: template que cambia unidad → reglas viejas hasta próximo save; mitigación: PUT /template (D2) incluye en warnings[] las reglas denormalizadas desactualizadas. Alternativa descartada: resolución runtime en el edge (obligaría al motor a conocer templates + dependencia de red en el camino de evaluación). **(D5) recommendation v2** (sube DEC-REF-24 de "v1 perfeccionable" a v2): campo EDITABLE en los 4 mini-forms (textarea — es conocimiento operativo, NO vocabulario, no se hereda). Estándar por severidad: info→verbo de verificación sin urgencia; warning→acción preventiva + plazo; critical→imperativo + a quién escalar. Largo máx 200 chars (holgado Telegram, corto pantalla). Materialización: placeholder dinámico por severidad + tooltip; backend advierte (no rechaza) si excede. **Colateral registrado:** FE-5 dashboard-admin roto silencioso → nota para sesión DEC-DASH-2, NO se toca en #51. **Alcance #51 sobre esta DEC = DISEÑO FIRMADO. La implementación (sub-schema, migración, PUT /template, selector heredado, denormalización, recommendation v2) es bloque propio con backlog formal a registrar al cierre de #51 — NO se implementa en #51.** |
| DEC-REF-73 | 2026-07-21 | **Capa de autoría por intención — asistente de creación de reglas (sala + Franco, #51/R3, GATE 3). DISEÑO FIRMADO, implementación diferida.** Dirección de producto de Franco: el asistente es **CAPA DE ENTRADA sobre los mini-forms de R23, no reemplazo** — reusa lo construido, no lo tira. **Insumo de arranque (Asesor Telco NOC):** 8 reglas verbalizadas como las piensa un operador de guardia, mapeadas a clase de regla — generador que no arranca al cortar la red (cross, = C1 productiva) · tanque que baja y no se recupera (SIN TIPO que la implemente hoy = "la babosa") · temperatura sobre umbral (D) · presión bajo el límite que el propio equipo declara (C) · tres intentos de arranque fallidos (S, = genset_no_start del sim) · rectificador caído con red presente (cross) · "decime qué hacer" (= recommendation, D5 de DEC-REF-72) · "no me llenes de mensajes" (= cooldown/anti-flapping). **Hallazgo de UX que ordena el diseño (Camila):** el operador NUNCA piensa en "tipo de regla" — piensa en situaciones; D/C/S/cross no existen en su cabeza. El tipo técnico debe ser RESULTADO deducido, jamás pregunta de entrada. **Armazón de 4 pasos:** (1) "¿Qué querés vigilar?" — elige equipo (deviceType) y variable de una lista en criollo heredada del template (cablea directo DEC-REF-72/D3: elegir trae label+unidad solos); (2) "¿Qué situación te preocupa?" — CINCO opciones en lenguaje de situación que deducen el tipo: "se pasa de un límite que yo pongo"→D · "se pasa del límite que el propio equipo considera seguro"→C · "pasa varias veces seguidas en poco tiempo"→S · "dos cosas malas que pasan juntas"→cross · "se va degradando y no se recupera"→babosa (pantalla "próximamente", DERIVA a la sesión de reglas de degradación — declarada, NO diseñada en #51 por decisión de alcance); (3) aterrizaje en el mini-form R23 del tipo deducido, con vocabulario ya heredado y etiquetas en lenguaje de intención ("Avisame cuando supere…" con unidad al lado, NO `condition.op`) — mismos campos que R23 ya persiste; (4) "¿Qué hay que hacer cuando salte?" — recommendation con placeholder dinámico por severidad (DEC-REF-72/D5), pegada a la alarma como la piensa el operador. **Modo simple vs avanzado:** simple = intención pura con defaults sensatos (cooldownSec/graceSec/escalateAfterMinutes); colapsable "Ajustes avanzados" para quien sabe. **`correlationParent` se resuelve DENTRO del avanzado del tipo cross** (decisión Franco GATE 1.d) — cierra la deuda que destapó R1 (cascada DEC-REF-53-A sin UI) sin inventarle pantalla propia. **Anti-flapping:** no es paso del asistente — default invisible en el avanzado; el armazón lo pone bien de entrada. **Límites explícitos del alcance:** el asistente es SOLO para CREAR (se suma como "Crear regla guiada" junto al alta actual); EDITAR una regla existente sigue cayendo directo en el mini-form R23 (la intención importa al crear, no al editar). NO reemplaza la consola. NO diseña la babosa. **Implementación diferida a BACKLOG-CHAIN-1.** |
| DEC-REF-74 | 2026-07-21 | **Catálogo de widgets del template — diseño de las dos familias (sala ampliada + Franco, #51/R4, GATE 4).** Ronda abierta por señalamiento de Franco tras el cierre de diseño de R3: *"solicité una auditoría para que entiendan realmente el funcionamiento de la app… ninguno advirtió de dónde saldrán los widgets necesarios que llenarán el select en template"*. Corrección aceptada por la sala: la evidencia estaba en el propio reporte R1 (sorpresas 4 y 5 — `variableType` hardcodeado en `configXxx` de templates.vue:470,488,501,521; `unit` solo presente en el widget numberchart) y fue clasificada como asimetría menor en lugar de hueco estructural. **Distinción que ordena el diseño (precisión de Franco):** las INSTANCIAS de widget sí se preconfiguran en el template (variable, label, unidad, frecuencia — viven en Mongo); los TIPOS de widget están hardcodeados — el select ofrece 4 opciones fijas (numberchart, switch, button, indicator) heredadas del template IoTiX/Now-UI, cada una con su método `configXxx` escrito a mano. Agregar un tipo NO es cargar un dato: es construir el componente. **Catálogo aprobado: 11 widgets nuevos en DOS FAMILIAS.** **Familia A — widgets de variable** (se configuran eligiendo UNA variable del template; encajan en el modelo actual): (1) **valor con estado** — valor + unidad + luz de color por umbral; base = `LiveValue.vue` ya productivo en el site page, se promueve a widget de catálogo; será el más usado del NOC; (2) **nivel de tanque** — visual de llenado + % + absoluto; (3) **contador acumulado** — valores monótonos crecientes (horas de motor, kWh, arranques) con variación del período; (4) **estado múltiple** — variable con N estados excluyentes (posición ATS: red/grupo/transferencia) — hoy forzada a switch; (5) **alarmas del equipo** — bitmaps del controlador decodificados, solo las activas, con severidad; (6) **mini tendencia** — valor actual + sparkline de ventana corta (anticipación de un vistazo); (8) **frescura del dato** — última lectura vs cadencia esperada; nace de BACKLOG-EDGE-4 (silencio del stream no detectado): hoy un dato viejo se ve idéntico a uno fresco; (12) **booleano con tiempo en estado** — variante de (1) donde el dato principal es hace-cuánto (puerta de shelter abierta hace 40 min ≠ abierta); voz de Seguridad física, Área 1. **Familia B — widgets de sitio** (se configuran eligiendo QUÉ EQUIPOS participan; NO encajan en el modelo "un widget = una variable" — leen varios equipos, calculan derivadas, o leen alarmas en vez de telemetría): (7) **autonomía proyectada** — horas restantes desde nivel + consumo; regresión sobre histórico, NO ML (coherente DEC-PRED-1); **depende de BACKLOG-INTEL-1**; (9) **recomendación activa** — alarma vigente + su `recommendation` + escalamiento; cierra la mitad olvidada del pilar (hoy la recomendación llega por Telegram pero NO existe como widget: quien mira la pantalla no ve qué hacer); (10) **planta DC** — tensión + corriente + estado por rectificador (el template Eltek tiene solo 3 widgets para un SmartPack que expone mucho más); (11) **cascada de energía del sitio** — red → ATS → grupo → rectificador → carga con estado por eslabón; el MVP se definió como "cascada de energía + salud de motor" (DEC-REF-6) y ningún widget la representaba. **ALCANCE REAL POR WIDGET — confirmado por Franco, registrado para no subdimensionar:** cada widget son TRES piezas, no una — (i) el componente que se renderiza, (ii) el mini-form de configuración dentro de la página de templates (mismo patrón que los mini-forms por tipo de regla de A8/SF-7), (iii) el campo correspondiente en el sub-schema de widget (DEC-REF-72/D1). Once widgets = once tríos. **Metadata nueva que el catálogo exige al sub-schema** (por eso catálogo y sub-schema se diseñan JUNTOS, no en secuencia): umbrales por severidad (widget 1), `tankCapacity` (widget 2 — ya declarado faltante en DEC-REF-69), `enumValues` (widget 4), diccionario de bitmaps del controlador (widget 5), cadencia esperada (widget 8). **Decisión de Franco sobre los 42 widgets legacy:** los 5 templates productivos NO tienen el campo de tipo de widget (causa verificada del render roto de dashboard-admin, R2.0/FE-5) — **se arreglan asignando tipo EN LA MIGRACIÓN**, no rearmándolos a mano desde la página nueva (rearmar 42 widgets es exactamente la fricción que la cadena vino a matar). **Orden de construcción:** Familia A primero (cimiento, desbloquea el sub-schema), Familia B después (requiere vocabulario ordenado; autonomía además depende de INTEL-1). Nota de Área 4: cascada de energía (11) y recomendación activa (9) son los de mayor impacto en demo Claro — cuentan la historia del producto en una pantalla — y también los más caros; NO van en la primera tanda. **Identidad visual (Área 4):** tema oscuro de plataforma con azul `#1d8cf8` como primario (DEC-REF-70 g); verde/ámbar/rojo RESERVADOS a estado operativo, nunca decorativos. **Implementación: punto (a) de BACKLOG-CHAIN-1 se amplía a "sub-schema + catálogo de widgets de Familia A" como frente único.** |
| DEC-REF-74-A | 2026-07-22 | **Adenda DEC-REF-74 — corrección de conteo del catálogo (apertura #52).** La fila madre declara *"11 widgets"* y *"once tríos"* tres veces, pero **enumera doce ítems sin huecos de numeración**: **Familia A** = (1)(2)(3)(4)(5)(6)(8)(12) → **ocho**; **Familia B** = (7)(9)(10)(11) → **cuatro**. El ítem (12) *"booleano con tiempo en estado"*, descrito en la fila madre como *"variante de (1)"*, **tiene número propio, trío propio y mini-form propio: cuenta como widget**. **Alcance real corregido: 12 widgets = 36 piezas, no 33.** **Numeración congelada:** rige la del corpus (1–12); queda anulada la renumeración 1–8 de Familia A usada en la apertura de #52, que hacía colisionar el (8). Fila madre intacta (append-only). |
| DEC-REF-75 | 2026-07-22 | **Diseño de implementación del sub-schema, la migración y el widget valueStatus (sala + Franco, apertura #52). Ejecuta DEC-REF-72/D1 + la parte de migración de DEC-REF-74. Alcance de #52: sub-schema al modelo + migración de los 42 legacy + el trío completo del widget (1) valueStatus.** **§1 · Sub-schema formal de widget.** Sub-schema propio con `{ _id: false }` — sin esto Mongoose inyecta un `_id` a cada uno de los 42 widgets en la primera escritura. `strict` explícito según RAMA-1a/1b de R1.1 (nunca heredado por default); veredicto de R1.1 = **RAMA-1a (`strict: true`)** por smoke empírico con doc enriquecido en Mongoose 5.13.15. **Grupos de campos:** (i) **Núcleo obligatorio:** `variable` · `variableFullName` · `variableType` (enum `float\|int\|bool\|categorical`) · `variableSendFreq`. (ii) **Formal (asciende de facto a schema):** `unit` (String, default `''`) · **campo de tipo de widget** (nombre resuelto por P3 de R1.1 = `widget`, enum del catálogo). (iii) **Presentación opcional:** `icon` · `class` · `column` · `decimalPlaces` · `tasmotaPath`. (iv) **Metadata de catálogo:** `thresholds{criticalLow, warningLow, warningHigh, criticalHigh}` (w1) · `tankCapacity` + `tankUnit` (w2, cierra DEC-REF-69) · `enumValues[{value,label,severity}]` (w4) · `bitmapDictionary[{bit,label,severity}]` (w5) · `cadenceExpected` (w8). **Enum del campo de tipo:** los 4 heredados (`numberchart`, `switch`, `button`, `indicator`) se conservan por compatibilidad con templates nacidos de la UI, más los 12 del catálogo en inglés (DEC-47): `valueStatus`, `tankLevel`, `counter`, `multiState`, `equipmentAlarms`, `miniTrend`, `projectedAutonomy`, `dataFreshness`, `activeRecommendation`, `dcPlant`, `powerCascade`, `booleanDwell`. **Nota anti-doble-fuente:** `thresholds` es **presentación, no alarma**. El motor de reglas sigue siendo dueño único del disparo. Un umbral de widget pinta una luz; no notifica, no escala, no persiste alarma. Sin esto nace un segundo vocabulario de alarmas y se repite el bug que la cadena vino a matar. **§2 · Mapeo determinístico legacy → tipo (los 42, sin confirmación fila por fila).** `variableType === 'bool'` (los 9, sin sub-casos) → `booleanDwell` · `variableType === 'categorical'` (los 3) → `multiState` · `int` + nombre `bitmap_*` (los 4) → `equipmentAlarms` · `int` + nombre `*_count` / `*_failed` / `*_attempts` → `counter` · `int` resto (`rpm`, `fault_code`) → `valueStatus` · `float` + `*_level` con unidad `%` → `tankLevel` · `float` + `*_hours` / `*_kwh` / `run_*` → `counter` · `float` resto (la mayoría de los 22) → `valueStatus`. El bucket *"confianza media"* de R1 desaparece: **todo `bool` va a `booleanDwell`**, que es superset de *"valor con estado"* para booleanos (muestra el estado y hace cuánto). Cero adivinación, cero ida y vuelta. **Backfill de `unit`:** regex `/\(([^)]+)\)$/` **anclado al final**, aplicado solo si `variableType === 'float'`. Todo otro tipo → `unit: ''` explícito. El anclaje es **desvío deliberado** respecto de *"mismo regex que el runtime"* (DEC-REF-72/D1): el del runtime no está anclado y captura `(42100)` de los bitmaps como si fuera unidad. Se registra el desvío, no se cuela. **Tipos asignados sin componente construido:** la migración asigna el tipo definitivo a los 42, pero en #52 solo existe `valueStatus`. El **resolver del frontend mapea todo tipo sin componente → `valueStatus` como fallback**, sin error y sin crash. El dato queda correcto desde el día uno; la UI lo alcanza en #53+. Esto evita una segunda migración de renombre y es coherente con DEC-STRAT-2: el dato no se maquilla para que la UI de hoy no se queje. **§3 · Widget (1) `valueStatus` — el trío.** **Componente:** valor numérico + `unit` + luz de estado. `decimalPlaces` por default según `variableType` (`float`→1, `int`→0) y overrideable por el mini-form. **Estados:** sin dato (`null`/`undefined` → gris neutro, texto *"sin dato"*, nunca `0`), `ok`, `warning`, `critical`. **Evaluación:** primero los bordes críticos, después los de warning, si no cae en ninguno → `ok`. Si no hay ningún umbral cargado → sin color, valor neutro. Azul `#1d8cf8` para cromo y tipografía; verde/ámbar/rojo reservados a estado operativo (DEC-REF-70 g). **La frescura del dato no es responsabilidad de este widget** — es el widget (8), no se adelanta acá. **Mini-form en `templates.vue`:** mismo patrón que los mini-forms por tipo de regla (A8/SF-7). Campos: `variable`, `variableFullName`, `unit`, `variableSendFreq`, `decimalPlaces`, los cuatro umbrales (todos opcionales), `icon`, `column`. El shape exacto se ajusta al molde real que devuelva P4 de R1.1. **Campo de sub-schema:** ya entra en C1: `thresholds` + `unit` + `decimalPlaces`. |
| DEC-REF-75-A | 2026-07-22 | **Adenda DEC-REF-75 — correcciones al §1 durante C1/C1-BIS (#52).** **(i) Enum de `variableType` retirado del sub-schema.** El §1 declaraba enum `float\|int\|bool\|categorical` para `variableType`, pero P4 de R1.1 verificó que la UI actual produce `variableType: 'input'` (widgets `numberchart`/`indicator`) o `variableType: 'output'` (widgets `switch`/`button`) en los `configXxx` de `templates.vue:470/488/501/521`. Con el enum activo, el primer `POST /template` desde la UI post-restart node fallaría con *"not a valid enum value"*. **El campo queda `type: String` sin enum**; la validación del enum del catálogo pertenece al PUT /template de CHAIN-1(b) y a los mini-forms, no al schema base. Enum de `widget` **se conserva** — sus 16 valores incluyen los 4 heredados (`numberchart`/`switch`/`button`/`indicator`) que la UI produce hoy. **(ii) Veredicto real de la rama strict — RAMA-1a sostenida por evidencia rigurosa.** El smoke original de P1 usó un doc scratch con 8 keys (4 núcleo + `widget`/`unit`/`class`/`column`) que resultaron ser TODAS declaradas por el sub-schema propuesto — cero keys ejercieron `strict:true`. El veredicto anterior era correcto por accidente, no por evidencia. Nuevo smoke (C1-BIS/P1-real) con 5 keys realmente no-declaradas (`demo`, `message`, `chartTimeAgo`, `text`, `selectedDevice` — las que la UI arma pero el sub-schema no lista): **PRESERVADAS al hidratar Y al persistir** (round-trip completo `insertOne raw → findById → toObject → save → find raw`). Mongoose 5.13.15 en DocumentArray con `strict:true` no descarta keys no declaradas; el veredicto anterior se sostiene por la razón correcta. **(iii) Nota de método:** un smoke de strict debe cargar deliberadamente keys que el schema NO conoce; verificar contra el propio sub-schema antes de correrlo. Lección de C1-BIS: no basta con "insertar keys extras" — hay que confirmar que esas keys efectivamente NO están declaradas. Fila madre DEC-REF-75 intacta (append-only). |
| DEC-REF-75-B | 2026-07-22 | **Adenda DEC-REF-75 — falsación del punto (ii) de DEC-REF-75-A durante C1-TER (#52). El write-path del constructor Mongoose DESCARTA silenciosamente keys no declaradas.** **(i) Falsación con evidencia.** El TEST-CREATE de C1-TER ejerció el path REAL del `POST /template`: `Template.create({...widgets:[configXxx]...})` — es decir, el constructor de Mongoose que la ruta backend usa. Resultado sobre los 4 `configXxx` de `templates.vue:465-529`: **numberchart** perdió `chartTimeAgo`, `demo`, `selectedDevice`, `userId`; **button** perdió `message`, `text`, `selectedDevice`, `userId`; **switch**/**indicator** perdieron `selectedDevice`, `userId`. La afirmación de DEC-REF-75-A(ii) — *"PRESERVADAS al hidratar Y al persistir"* — sólo vale para el flujo `insertOne raw → Mongoose lee` (read-path). El write-path del constructor con `strict:true` **descarta las keys no declaradas ANTES de serializar** a Mongo. Los smokes P1 y P1-real ejercieron el read-path, nunca el write-path del constructor; el veredicto de rama estaba mal certificado por el método. **(ii) Fix aplicado — declarar las 3 keys críticas del write-path.** Al grupo de presentación opcional del sub-schema se suman: `message: {type:String}`, `text: {type:String}`, `chartTimeAgo: {type:Number}`. `strict: true` se mantiene; enum de `widget` se mantiene. Justificación por key: `chartTimeAgo` lo consume `Rtnumberchart.vue:187` para la ventana del sparkline en el request de históricos; `message` es el payload del actuador que envía `Iotbutton.vue:42` al topic `actdata`; `text` es el label visible del botón (`Iotbutton.vue:5`). Sin persistirlos, cada widget legacy creado desde la UI post-rebuild nace mudo. **(iii) Decisión explícita de NO persistir `selectedDevice`, `userId` (dentro del widget), `demo`.** GREP verificado (C1-TER paso 1): (a) `selectedDevice` — leído por 4 widgets legacy en runtime **y** sobrescrito por `dashboard-admin.vue:111-112` (`widgetCopy.selectedDevice.dId/.name = $store.state.selectedDevice.*`); `_siteCode.vue` NO lo consume (arma dId propio). El valor persistido es placeholder del editor (`{name:'Home',dId:'8888'}`) siempre pisado en runtime; **contradice DEC-REF-33 ("Template = molde compartido sin device")**. (b) `userId` dentro del widget — leído por 3 widgets legacy en runtime **y** sobrescrito por `dashboard-admin.vue:113` (`widgetCopy.userId = $store.state.selectedDevice.userId`); `LiveValue.vue` lo consume del `config` construido por `_siteCode.vue.liveConfig()`, no del widget crudo. Valor persistido literal (`"sampleuserid"`/`"userid"`) siempre pisado; es placeholder duplicado. (c) `demo` — flag de preview del template editor (`Rtnumberchart.vue:176` para modo simulado), `dashboard-admin.vue:116` lo pisa a `false` siempre. En runtime productivo su valor persistido no importa. **(iv) Efecto colateral confirmado (no rompe).** El constructor Mongoose inicializa arrays declarados; los widgets creados post-rebuild traen `enumValues:[]` y `bitmapDictionary:[]` desde el nacimiento — coherente con el shape declarado del sub-schema, cero impacto operativo. Cast confirmado: `variableSendFreq:'30'` (string) → `30` (number). **(v) Lección de método explicitada.** Un smoke de `strict` debe ejercer **los dos paths** (read + write via constructor); usar `insertOne` raw prueba lectura, no escritura. Corresponde extender la nota (iii) de DEC-REF-75-A: verificar keys no declaradas contra el schema **y** ejercer el constructor path si el consumidor real es un `.create()`/`.save()` desde payload. Filas madre DEC-REF-75 y DEC-REF-75-A intactas (append-only). |
| DEC-REF-75-C | 2026-07-23 | **Adenda DEC-REF-75 — alcance de C4 ampliado: `/history` es consumidor del label como fuente de unidad, hay que pasarle `widget.unit` explícito (mismo patrón que `liveConfig` en C3). Registro ANTES del código.** **(i) Recon R-C4-a.** Auditados los 2 consumidores citados en el mandato: (a) `app/pages/history.vue:38` — `<el-option :label="w.variableFullName">` del selector de variables (única fuente de UI del texto "Presión aceite (psi)" al elegir qué graficar); (b) `app/pages/history.vue:57` — pasa `variableFullName` como prop a `<HistoryChart>` (`app/components/HistoryChart.vue:30/56`) que lo usa como `chartOptions.series[0].name` — el título de la serie del gráfico. En ambos casos **el label es la única fuente de unidad**: `variablesForDevice` (history.vue:113-118) toma widgets crudos del template pero el consumidor solo lee `variableFullName`, ignorando `widget.unit` post-C2. **RAMA-A para `/history`.** El tercer consumidor citado, `app/components/Layout/DashboardNavbar.vue:68` (`notification.variableFullName`), **NO se ve afectado por C4** — su fuente es `notification.variableFullName` = `alarm.variableLabel` (`edge-engine/notificationRouter.js:148`) = `rule.variableLabel` denormalizado en `RuleDefinition` (`edge-engine/ruleEngine.js:87/117/222/261` + `app/api/models/rule_definition.js:12`). Los labels del RulePack tienen sus propios paréntesis, redactados a mano, independientes de la migración del template. **(ii) Recon R-C4-b — cobertura y guarda de idempotencia.** Censo empírico sobre `db.templates`: 22/22 widgets con `unit !== ''` tienen su `variableFullName` terminando exactamente en `(<unit>)`. Cero mismatches. La migración toca los 22 sin dejar afuera a ninguno; guarda por sufijo exacto es segura. **(iii) Fix frontend acompañante en C4 (bloque único de código, no dos commits).** `history.vue`: (a) el label del `<el-option>` se construye concatenando `w.variableFullName` + ` (` + `w.unit` + `)` cuando `w.unit`; (b) se pasa una nueva prop `unit` a `<HistoryChart>` desde `selectedVariableObj?.unit`. `HistoryChart.vue`: (a) declara la prop `unit` (String, default `''`); (b) el `series[0].name` concatena `variableFullName` + ` (${unit})` cuando `unit`. Sin fallback al regex (mismo criterio que C3). **(iv) Alcance del script C4.** Migración destructiva del label: `variableFullName ← variableFullName.slice(0, -`(<unit>)`.length).trim()`. Toca sólo los 22 con `unit !== ''`; los 20 restantes (4 `bitmap_*`, 3 bool con paréntesis no-unidad — PIR/corte-golpe, 1 categorical con FFT, 8 int, 3 bool sin paréntesis final) quedan intactos por construcción de la guarda. **Idempotencia por sufijo:** solo escribe si `label.endsWith('(<unit>)')`; segunda corrida = 0 cambios. **Trim final del label** — cierra subitem (a) de BACKLOG-UI-8. **Backup PROPIO** a `seeds/_dev/backup_labels_${timestamp}.json`; `--restore` documenta explícito que **revierte SOLO C4** (para revertir C2 también hay que aplicar el backup de C2 después). **Filas madre DEC-REF-75 / DEC-REF-75-A / DEC-REF-75-B intactas (append-only).** |
| DEC-REF-75-C-A | 2026-07-23 | **Adenda DEC-REF-75-C — corrección de dos errores de la -C (#52/C4 pre-apply).** **(i) Falsación de la afirmación "el trim final del label cierra subitem (a) de BACKLOG-UI-8".** La fila -C afirma que el `.trim()` de C4 sobre `variableFullName` cierra un subitem de BACKLOG-UI-8. Verificado contra el corpus (`docsRefactor/WanomiRefactor.md:305`): BACKLOG-UI-8 es *"Pasada de consistencia visual — consola de Reglas de monitoreo (#47)"* — auditoría visual de la página de Reglas contra el patrón de Dashboard/Devices/Alarms/Templates. **No enumera subitems (a)/(b)** en el corpus (no hay "(a)" declarado), y el GATE 0 de apertura #52 dejó fuera de alcance a UI-8 por completo. Aunque existiera un subitem (a) hipotético sobre `.trim()` en algún mini-form de reglas, sería sobre `ruleId` u otro campo de la consola de Reglas — **NO sobre `variableFullName` del widget del template**. Otra página, otro campo, otro concern. **BACKLOG-UI-8 sigue ABIERTO** con su alcance original (auditoría visual de la consola de Reglas), sin verse tocado por C4. **(ii) Naturaleza real del `.trim()` de C4.** Es **higiene del label del widget**: elimina espacios colgantes que podrían quedar cuando la migración recorta el sufijo `(<unit>)` de un label que tuviera espacio entre el texto y el paréntesis (p. ej. `"Presión aceite (psi)"` → `"Presión aceite "` sin `.trim()`). Sin relación con la consola de Reglas ni con UI-8. Se registra como parte del contrato del script `seeds/migrate_widget_labels.js`. **(iii) Fecha de la -C.** La fila DEC-REF-75-C está fechada 2026-07-23. Corresponde a la realidad: la sesión #52 cruzó medianoche (los commits desde `dd0d462` hasta `843e2ce` son 2026-07-22; a partir de `bb732fb` la sesión operó 2026-07-23, verificable con `git log --format='%ci' bb732fb`). No es error de reloj; se anota explícito para trazabilidad. Filas madre DEC-REF-75 / -A / -B / -C intactas (append-only). |
| DEC-REF-75-D | 2026-07-23 | **Adenda DEC-REF-75 — alcance final de C4: `unit` viaja en el contrato del endpoint `/dashboard/noc/trend*`, no se deriva del label en el frontend (#52/C4 apply).** **(i) Recon R-C4-c/-d completo: 12 consumidores auditados.** Un consumidor crítico afectado (`app/api/routes/dashboard_noc.js:64-88` — `computeTrendVariables[FromTemplates]` construye `label: labelByVar.get(v)` con `w.variableFullName` **crudo del widget** para el selector del gráfico de tendencia del NOC operador — DEC-REF-69, R7 #49); dos cosméticos (`Rtnumberchart.vue:12` card title — unit sigue visible en la serie L134/L170; `templates.vue:337` columna "Variable" — editor); el resto (`webhooks.js:378/453/483`, `alarms.js:181/205/206`, `rules.js:144/164`, `emqxapi.js:370/430`, `DashboardNavbar.vue:68`) lee `rule.variableLabel`/`notification.variableFullName` de RuleDefinition (independiente del template), o son editores/receivers del request. R-C4-d: `tools/device_simulator/seed.js:42-94` declara labels con paréntesis pero NO parsea; `ensureTemplate` skipea si ya existe → los 5 productivos post-C4 no serán regenerados (ambiente reset regeneraría con paréntesis — deuda menor anotada; no bloquea). **(ii) DECISIÓN: `unit` viaja en el contrato del endpoint, no se deriva del label en el frontend. NO es ampliación de alcance de C4** — es la aplicación del principio ya firmado en DEC-REF-69: la **semántica de agregación** viaja en el contrato (`aggregation: 'avg'|'range'`) y el frontend renderiza sin mapeo propio. Extender esa disciplina a `unit` es coherente, no novedad. Contrato: `trendVariables[] : {variable, label, aggregation}` → `{variable, label, unit, aggregation}`. El frontend consumidor (si existe) concatena `label + ' (' + unit + ')'` cuando `unit`, mismo patrón que `HistoryChart` (DEC-REF-75-C, C3). **(iii) Los 2 cosméticos NO entran en C4, con fundamento:** (a) `Rtnumberchart.vue:12` — la unidad **sigue visible** en `series[0].name` (L134/L170: `config.variableFullName + " " + config.unit`), el card title puede quedar sin el sufijo `(°C)` sin que se pierda información operativa; (b) `templates.vue:337` — es el editor; **C5 da a `unit` campo propio en el mini-form** de `valueStatus` y desbloqueará hacer visible la unidad en la tabla como columna aparte, lo que haría descartable cualquier fix hecho ahora sobre `variableFullName`. Ambos → **BACKLOG-UI-12**. **(iv) Orden de ejecución de C4 y por qué NO se invierte: apply (labels) → rebuild (consumidores).** Al revés, los consumidores concatenarían `unit` sobre labels que todavía lo tienen (`"Temperatura shelter (°C) (°C)"`). La ventana intermedia (apply hecho, rebuild pendiente) muestra falta de unidad, no unidad duplicada — recuperable, no chocante. Filas madre DEC-REF-75 / -A / -B / -C / -C-A intactas (append-only). |
| DEC-REF-76 | 2026-07-23 | **Widget (1) `valueStatus` — arquitectura del trío + resolver tipo→componente + regla de tipo en el mini-form (#52/C5, precisa DEC-REF-75 §3).** **(i) DOS componentes, no uno — `ValueStatus.vue` (presentación pura) + `LiveValue.vue` (fuente MQTT wrapper).** `ValueStatus.vue` recibe `props: {value, config}`, sin MQTT, sin store, sin fetch, sin `mounted`/`beforeDestroy`. `LiveValue.vue` conserva su rol y su contrato con `_siteCode.vue` (suscripción MQTT + estado `mqttConnected` + presentación de "esperando dato / sin señal"), y su `<template>` pasa a delegar la presentación del dato en `<ValueStatus :value="value" :config="config" />`. **Fundamento:** el widget de catálogo debe renderizar donde NO hay stream vivo (preview de `templates.vue`, `dashboard-admin.vue`, la vista de tipos futura); copiar `LiveValue` como base productiva crearía dos componentes que divergen al primer fix. Con este split, hay UN componente de presentación (`ValueStatus`) reutilizable con o sin fuente MQTT, y `LiveValue` queda como el único acoplado al bus. Sin regresión visible: el site page debe verse idéntico. **(ii) `variableType` es un SELECT del dominio en el mini-form de `valueStatus`, NO hereda `'input'`/`'output'` de los `configXxx` legacy.** El dominio real de `variableType` es el enum de DEC-REF-72/D1: `float\|int\|bool\|categorical` (retirado como enum del sub-schema en DEC-REF-75-A por compatibilidad con los 4 `configXxx` legacy, pero SIEMPRE fue el dominio semántico correcto). Los widgets legacy (numberchart/switch/button/indicator) heredaron `variableType: 'input'`/`'output'` hardcodeado en `templates.vue:470/488/501/521` porque nacieron con criterio de "dirección del canal MQTT", no "tipo del dato". **En `valueStatus` el criterio correcto es dominio del dato**: sin esto, (a) `ValueStatus` no sabe formatear (`decimalPlaces` por default depende de `variableType`); (b) `dashboard_noc.js:70-73` filtra `trendVariables` por `variableType === 'float' \|\| 'int'` y excluiría los `valueStatus` con tipo `'input'` del selector de tendencia del NOC; (c) una regla futura escrita sobre el widget no tiene tipo real. **Los 42 legacy y los 4 tipos viejos conservan lo suyo** (retrocompatibilidad); los widgets `valueStatus` nuevos nacen con el tipo correcto. **(iii) Reglas de estado de `ValueStatus`** (precisan DEC-REF-75 §3): **sin dato = `value === null` o `value === undefined` únicamente** — `0` es dato válido, NO "sin dato" (contraejemplo real: `oil_pressure = 0` con motor parado es lectura correcta del sensor, no ausencia de dato); **sin umbrales cargados = SIN color** (no verde: afirmar "ok" sin criterio es reportar algo que no se sabe; el widget se ve neutro con el valor); **con umbrales: evaluar críticos primero, después warning, si no cae en ninguno → ok**; **la luz de estado NO informa frescura** — la frescura es responsabilidad del widget (8) `dataFreshness` (DEC-REF-74), no se adelanta acá; el gris de "sin dato" cubre solo el caso `null`, no "dato viejo". Cromo/tipografía en azul `#1d8cf8`; verde/ámbar/rojo reservados a estado operativo (DEC-REF-70 g). **(iv) Resolver tipo→componente.** El mapa de resolución vive en un helper compartido (ubicación concreta a decidir en C5.1 — candidato: `app/components/Widgets/resolver.js`) que aplica el fallback firmado en DEC-REF-75 §2 (*"todo tipo sin componente → `valueStatus`"*): los 4 legacy (`numberchart`/`switch`/`button`/`indicator`) mapean a sus componentes actuales intactos; los tipos del catálogo con componente construido mapean a ese componente (en #52: solo `valueStatus`); todo tipo del catálogo SIN componente todavía mapea a `valueStatus` como fallback. **Se aplica en `templates.vue:172-175/233-236` (preview + lista) y en `dashboard-admin.vue:35-52` (grilla del super-admin)**, reemplazando los `v-if` hardcodeados por un componente dinámico (`:is="resolveWidget(widget.widget)"`) o equivalente. **Consecuencia esperada:** cierra el render en blanco de los 42 legacy migrados en ambas páginas — evidencia visible del cierre del ciclo C1→C5. Filas madre DEC-REF-75 / -A / -B / -C / -C-A / -D intactas (append-only). |
| DEC-REF-76-A | 2026-07-23 | **Adenda DEC-REF-76 — el resolver debe distinguir CONTEXTO y la cáscara es pieza aparte del widget (#52/C5.1-BIS).** **(i) Falsación de DEC-REF-76 (iv).** El resolver mapea `type → componente` sin distinguir dónde se renderiza. En `dashboard-admin.vue` (contexto operativo con dispositivo seleccionado y stream vivo) `ValueStatus` recibe `value=null` y afirma *"sin dato"* mientras el equipo publica; en `templates.vue` (contexto editor, sin device, sin fuente) hace lo mismo aunque no exista fuente. **Evidencia:** checklist visual de C5.1 aprobado por Franco. **Mismo criterio que DEC-REF-76 (iii) — "sin umbrales cargados → sin color, porque afirmar 'ok' sin criterio es reportar algo que no se sabe"**: en editor no hay device, no hay fuente, no hay valor posible; renderizar *"sin dato"* miente. El fallback correcto para editor es *no aplica* (guión neutro), semánticamente distinto de *sin dato* (hay fuente, aún no llegó publish) y de *esperando* (hay fuente activa, primer publish pendiente). **(ii) Resolver con CONTEXTO.** Firma nueva: `resolveWidget(type, { context })` con `context ∈ {'live', 'editor'}`. **'live'** = grilla operativa con dispositivo y stream (dashboard-admin, futuras vistas de site que usen el resolver); resuelve al componente **wrapped en la fuente MQTT** (LiveValue como wrapper del presenter). **'editor'** = template editor sin device y sin fuente (templates.vue preview + lista); resuelve al componente **presentacional puro con `context='editor'`** (sin wrapper MQTT, sin "sin dato"). Los 4 legacy (`numberchart`/`switch`/`button`/`indicator`) son agnósticos al contexto (se resuelven al mismo componente en los dos casos) porque su modelo interno maneja su propio ciclo. **(iii) CÁSCARA COMO PIEZA APARTE, no dentro del widget.** El widget puro (ValueStatus y sus futuros hermanos del catálogo) **no lleva `<card>` ni encabezado con nombre — es solo el DATO**. La cáscara vive en un componente aparte `WidgetShell.vue` (reutilizable por los 12 widgets del catálogo, DEC-REF-74/-74-A) que provee card + encabezado con `config.variableFullName`. **Fundamento:** el site page ya pone la cáscara desde `_siteCode.vue` (validado visualmente por Franco, C3) y los 4 legacy se auto-encascaran (p. ej. `Rtnumberchart.vue:4` `<card type="chart">`); si `ValueStatus` metiera su propio `<card>`, el resultado en site page sería doble card y en dashboard-admin doble card sobre los 4 legacy. La cáscara la aplica el componente compuesto **por tipo del catálogo** (`ValueStatusLive.vue`, `ValueStatusEditor.vue`), no el widget puro. **Precisa DEC-REF-75 §3 que no mencionaba el encabezado con nombre.** **(iv) TRES estados de `ValueStatus`, no dos.** Prop nueva `context` que ajusta el estado "sin valor": (a) **`esperando`** (`context='live'` + hay fuente, aún no llegó publish) — spinner MQTT-específico, lo maneja el wrapper `LiveValue`, no `ValueStatus`; (b) **`sin dato`** (`context='live'` + valor recibido pero es `null`/`undefined`) — gris con texto *"sin dato"*, lo maneja `ValueStatus` (comportamiento actual); (c) **`no aplica`** (`context='editor'` — no hay fuente por definición del contexto) — **guión neutro `—`**, gris, sin texto de "sin dato" (afirmar que no hay dato cuando no hay fuente miente). `0` sigue siendo dato válido en los tres (DEC-REF-76 iii). **(v) Consecuencias esperadas registradas para no leerse como falla:** (a) los 42 widgets legacy migrados en C2 no tienen `thresholds` en Mongo (no se cargó ninguno) → renderizan sin color por DEC-REF-76 (iii); es lo declarado, no un bug; (b) sin la siembra de BACKLOG-UI-7 (viable HOY con TENANT-9 cerrado, verificado empírico R4 · `db.data` con datos de 0 min ago para dIds reales) `dashboard-admin` muestra los valores recién al primer publish MQTT (30-60s según `variableSendFreq`). No se implementa en C5.1-BIS; queda como track UI-7 explícito. **(vi) Molde generalizado según R1 — RAMA-A confirmada.** `LiveValue.vue` es la fuente MQTT genérica del molde: mantiene `mounted/beforeDestroy/onData/topic/mqttConnected` (agnósticas), acepta prop `presenter` (referencia a componente) y delega la presentación del dato en `<component :is="presenter" :value="value" :config="config" />`. Para cada tipo del catálogo se crean dos composiciones explícitas `TipoLive.vue` (WidgetShell + LiveValue con presenter) y `TipoEditor.vue` (WidgetShell + presenter con `context='editor'`) — en #52 solo `ValueStatusLive`/`ValueStatusEditor`, con fallback DEC-REF-75 §2 para los tipos no construidos aún. **Nota UI-6 (post-C5.1):** el corpus L306 describe la grilla vacía de `pages/dashboard.vue`, que migró a `/dashboard-admin` (DEC-DASH-1b, DEC-REF-69); C5.1 cerró uno de sus dos síntomas (los widgets ahora renderizan); el segundo (select de devices trabado en un preestablecido) y el hipotético `SyntaxError` **quedan fuera de C5.1-BIS** — se registra que UI-6 aplica hoy a `dashboard-admin` como puntero. Filas madre DEC-REF-75 / -A / -B / -C / -C-A / -D / -76 intactas (append-only). |
| DEC-REF-76-B | 2026-07-23 | **Adenda DEC-REF-76 — mini-form de `valueStatus`: `variable` editable, chequeo de duplicados por `variable`, validación de alta, `column` en el form, degradación de config (#52/C5.2, cierra el trío).** **(i) `variable` es campo EDITABLE del mini-form de `valueStatus`; el `makeid(10)` NO se aplica.** `templates.vue:708-723` (`addNewWidget()`) asigna `config.variable = this.makeid(10)` — identificador aleatorio de 10 chars. Correcto en el modelo IoTiX heredado (la plataforma inventa el id y el dispositivo se configura contra él), **INVÁLIDO en Wanomi**: el nombre de la variable lo fija el mapa Modbus del equipo (`oil_pressure`, `fuel_level`, `bitmap_42100`). **Evidencia:** los 42 widgets legacy productivos tienen nombres reales del mapa; ninguno nació de esta UI (ver DEC-REF-71-A: no existe seed versionado, shape hardcodeado). **Consecuencias de un widget con `variable` aleatoria:** (a) nunca matchea la telemetría del broker → muestra "sin dato" para siempre; (b) el edge no lo evalúa (gate por `variable` en `ruleEngine.js`); (c) `dashboard_noc.js:70-73` lo excluye del selector de tendencia (`Data.distinct("variable", ...)` no lo encuentra); (d) una regla escrita sobre él nunca dispara. **Es la causa concreta de la deuda declarada en DEC-REF-71** ("el vocabulario del sistema se define UNA sola vez en el Template"): sin nombre real, el vocabulario nace roto. **Fix ACOTADO a `valueStatus`:** en el mini-form, `variable` es `<base-input>` editable, requerido; `addNewWidget()` **no pisa `config.variable`** cuando `widgetType === 'valueStatus'`. Los 4 tipos legacy conservan `makeid` (retrocompatibilidad, mismo criterio de DEC-REF-76 ii "los legacy conservan lo suyo, los nuevos nacen bien"). **(ii) Chequeo de duplicados por `variable`, no por `variableFullName.trim()`.** `addNewWidget:710` hoy chequea `this.widgets.some(w => w.variableFullName.trim() === varName)`. La clave real de unicidad es `variable` — dos widgets con etiquetas distintas pueden apuntar a la misma variable y hoy pasan sin advertencia (bug latente). **Para `valueStatus` el chequeo es por `variable`**; en los 4 legacy el criterio actual queda intacto (siguen con `variableFullName.trim()`, retrocompatibilidad). Si se detecta un caso legacy con dos widgets sobre la misma `variable`, se corrige por edición manual — no se migra ahora. **(iii) Validación de alta.** `canAddWidget` hoy sólo exige `variableFullName.trim()`. **Para `valueStatus` exige además `variable` no vacía** — un widget sin variable nace muerto (nunca matchea). Para los 4 legacy la regla actual permanece (el `makeid` garantiza `variable` no vacío por construcción; agregar el chequeo sería redundante). **(iv) Degradación del `previewConfig` + prop `config` de `ValueStatus`.** `previewConfig()` (`templates.vue`, computed) mapea sólo los 4 legacy; al sumar `valueStatus` al select devolvería `undefined` para ese tipo, y `<component :is="resolveWidget('valueStatus', 'editor')" :config="undefined" />` reventaría en el sub-schema del prop (`config: {type: Object, required: true}`). **Fix (a)** se suma un 5° config (`valueStatusConfig`) al `data()` de templates.vue con el shape del mini-form (incluyendo `thresholds: { criticalLow:null, warningLow:null, warningHigh:null, criticalHigh:null }`) y `previewConfig` lo mapea. **Fix (b) defensivo:** el prop `config` de `ValueStatus.vue` (y `ValueStatusEditor.vue`, `ValueStatusLive.vue`, `WidgetShell.vue`) pasa de `required: true` a `default: () => ({})` — degrada sin romper si algún consumidor futuro instancia sin config, y muestra el guión neutro. Cero regresión sobre los consumidores actuales. **(v) `column` entra al mini-form.** Los 42 legacy no tienen `column` (shape de 4 keys) y por eso los cards se apilan a lo ancho en la grilla de `dashboard-admin` (síntoma visible en el visual de C5.1-BIS). Los widgets `valueStatus` nuevos nacen con `column` seleccionable (col-3 a col-12) en el mini-form; los widgets se corrigen cuando se editen por UI — **no se migra ahora** (mismo criterio DEC-STRAT-2: se implementa el producto, no se maquilla el dato viejo). **(vi) Convención del label: SIN unidad entre paréntesis.** La unidad es su propio campo desde C2/C4 (`unit`); escribir `variableFullName: "Presión aceite (psi)"` regenera el drift que C4 vino a matar. El label es solo el nombre operativo (`"Presión aceite"`); la unidad se ingresa en el campo `unit` del mismo form. Coherente con DEC-REF-72/D1 ("`unit` asciende a campo formal"). Filas madre DEC-REF-75 / -A / -B / -C / -C-A / -D / -76 / -76-A intactas (append-only). |
| DEC-REF-76-C | 2026-07-23 | **Adenda DEC-REF-76 — los 4 inputs de umbral SALEN del mini-form de `valueStatus` en #52. La fuente de los umbrales queda diferida a #53 (objeción de Franco).** **(i) Objeción de Franco, aceptada: los umbrales son semántica de ALARMA, no de presentación.** DEC-REF-75 §1 declaró *"`thresholds` es presentación, no alarma. El motor de reglas sigue siendo dueño único del disparo"* como salvaguarda anti-doble-fuente; **la salvaguarda NO alcanza**. Si el operador ingresa `warningHigh=90` como umbral de widget en `templates.vue` **y también** crea una regla `type=D` con `condition: coolant_temp > 90` en la consola de Reglas, hay **dos números para el mismo criterio en dos lugares** — reproduce exactamente la enfermedad de `psi/Bar` (BACKLOG-CHAIN-1 vino a curarla, no a repetirla). Que el motor "sea el único que dispara" no impide que el widget mienta con umbral desincronizado, ni evita el mantenimiento doble. **Objeción ACEPTADA.** **(ii) Los 4 inputs de umbral SALEN del mini-form de `valueStatus` en #52.** Se retiran del `<div v-if="widgetType == 'valueStatus'">`, se retiran de `valueStatusConfig` en `data()`, se retira el bloque de reset. **El campo `thresholds` PERMANECE en el sub-schema de widget (C1, DEC-REF-75 §1)** — no romper el schema por un cambio de dueño de datos; simplemente ningún path lo populará en #52 y `ValueStatus` seguirá viendo `config.thresholds === undefined`. Consecuencia visual: `ValueStatus` renderiza neutro (sin color) porque `hasAnyThreshold` devuelve `false`, que es exactamente lo que DEC-REF-76 (iii) firmó como comportamiento correcto (*"sin umbrales cargados → SIN color; afirmar 'ok' sin criterio es reportar algo que no se sabe"*). **Sin regresión visual.** **(iii) Tres caminos para #53, sin decidir ahora.** **(A)** Umbrales propios del widget (lo que hicimos y estamos retirando): **descartado por doble fuente**. **(B)** El widget lee los umbrales desde las reglas del `deviceType` en el mismo namespace (equivalente a la denormalización de DEC-REF-72/D4 sobre `unit`/`variableLabel`): fuente única, pero los tipos `cross` y `S` (secuenciales) **no exponen un umbral numérico simple** — cross evalúa un árbol AND/OR de condiciones, S una ventana de reintentos; no hay `Number` que pintar en el widget. Cubre bien D/C, mal el resto. **(C)** El color sale del ESTADO DE ALARMA VIGENTE que el motor ya calcula y emite (via `notifications`/`alarm.severity` que viven en `notificationRouter.js` y aterrizan en el navbar): **fuente única**, sirve para los 4 tipos de regla, y **la pantalla coincide con lo que llegó por Telegram** (un widget en rojo cuando el operador tiene en el teléfono una notificación crítica de esa variable). **Recomendación de sala:** (C). Aterriza el widget al patrón operativo real (el operador reacciona al Telegram/notif), cierra la asimetría del pilar (recomendación llega por Telegram pero NO se ve en pantalla — misma raíz que el widget 9 activeRecommendation, DEC-REF-74). **Ronda de diseño propia en #53** con Camila (UX) + Área 2 + Área 4. **(iv) Los FIX 1 y FIX 2 del turno anterior quedan sin objeto en su parte de umbrales** (no hay inputs que normalizar de string a Number, no hay bloque `thresholds` que resetear). **El reset de `variable`/`variableFullName`/`unit`/`decimalPlaces` SIGUE haciendo falta** — sin el reset el próximo widget hereda campos del anterior, que es exactamente el bug que motivó los FIX. Reset ampliado: `variable`, `variableFullName`, `unit`, `decimalPlaces`; se conservan `variableType` (default sano `float`), `icon` y `column` (defaults del form, no del último widget). **(v) `liveConfig()` de `_siteCode.vue` incorpora `decimalPlaces`** — hoy solo pasa `userId`/`dId`/`variable`/`variableType`/`variableFullName`/`unit` a `<LiveValue>` (DEC-REF-75-C fix C3), pero `ValueStatus.decimalPlaces` computed lee `this.config.decimalPlaces` y si el widget lo tiene configurado, el site page lo estaba ignorando (fallback al default por tipo). El fix es aditivo, cero regresión: los widgets legacy sin `decimalPlaces` siguen con el default por tipo. Filas madre DEC-REF-75 / -A / -B / -C / -C-A / -D / -76 / -76-A / -76-B intactas (append-only). |
| DEC-REF-77 | 2026-07-26 | **Paréntesis pre-reunión Claro — motor (siembra reordenada del catálogo) + panel (cascada agrupada, tipo de alarma, eventos por sitio) + simulador (consola + escenarios). Todo es backlog YA registrado (BACKLOG-CHAIN-1 c/e + BACKLOG-UI-10) reordenado por prioridad, NO construido para la ocasión. DEC-STRAT-2 intacto: lo que se muestra es producto funcionando.** **(i) ALCANCE.** Motor: siembra de las inferencias del catálogo sesión #18 que sean sembrables hoy (recon pre-paréntesis: 20 de 46 son SEMBRABLES YA, 16 requieren soft sensor, 8 requieren hardware, 2 requieren variable nueva). Panel: agrupación visual de cascada (DEC-REF-41 diferida a demo, DEC-REF-50 sentó las bases con `correlationParent`; hoy el operador ve N notificaciones sueltas cuando la causa es una), tipo de alarma en la fila (D/C/S/cross visible al operador), eventos por sitio en tarjeta única. Simulador: consola desde la web para disparar escenarios en vivo (hoy es MQTT `simulator/{dId}/control` sin UI), catálogo de escenarios completos (hoy 3 tocan `genset_running`; mínimo 6-8 requeridos para demo de valor). **(ii) HALLAZGO QUE ORDENA EL ORDEN — el simulador va PRIMERO. El grupo simulado NUNCA arrancó en 55 días** (verificado en Mongo: `genset_running=0` en el 100% de las muestras de todos los devices GEN; `run_hours` del Cummins CR00061 (`Z5tKK1rN`) va de 0 a 0.05 — tres minutos de motor en 55 días). **La mayoría de las reglas del catálogo vigilan un motor en operación. Sembrar antes de arreglar el simulador sería sembrar sobre un sitio muerto** — reglas nunca disparadas, cascadas invisibles, valor cero para la demo. Por eso el simulador va PRIMERO, el motor SEGUNDO, el panel TERCERO. **(iii) DECISIÓN FRANCO — ciclo de ejercicio semanal + horas iniciales realistas en el simulador. NO es agregado de demo:** es **práctica estándar de operador telco** (el generador ejercita el motor una vez por semana para mantener aceite lubricado, batería cargada y detectar fallas latentes antes del corte real; genera 5-10 min de `genset_running=1` en cadencia predecible) y **el simulador estaba incompleto** (no fue omisión declarada — nunca se levantó como bug). **Desbloquea:** (a) mantenimiento por horas (reglas D1/D2 del catálogo = `run_hours` cerca de 250h next-service); (b) presión en operación (reglas A1/A2 requieren motor con `rpm > 300`); (c) ciclo de cargador (reglas E4/E5 requieren transición mains→gen→mains); (d) línea base del sitio (D3 requiere patrón de horas para detectar anomalía). Alcance: rutina en el simulador que fuerce `mains_failure_ats_transfer` cada 7 días simulados de wallclock (parametrizable), + valor inicial no-cero en `run_hours` por device (ej. 3200-4800h típicas de sitio Claro maduro). **(iv) DECISIÓN FRANCO — modelar `transfer_state` contra el mapa REAL del ComAp InteliATS PWR (hoy clavado en `'AUTO'`).** Destraba **H2 (cascada completa)** y **F3 (transferencia inconsistente)**, ambas del catálogo. **Requiere que Área 1 defina la máquina de estados contra la biblioteca de campo** (`docsRefactor/_biblioteca_campo/`), NO que la invente Área 2 sobre la marcha (regla DEC-PROC-6: sin especificación, no hay código). **Cierra BACKLOG-SIM-4** ("Modelar dominio real de `transfer_state` del ATS", abierto desde #40-R4). El enum real del breaker state 8455 incluye `AUTO`, `GEN`, `MAINS` + estados intermedios (transferencia en curso, timeout de arranque, bypass) — el detalle exacto lo firma Área 1 con el mapa Modbus abierto. **(v) DECISIÓN FRANCO — prefijo `cat-` para toda regla sembrada del catálogo.** NO se renombra nada productivo (`cummins-C1-mains-loss-gen-no-start` está referenciado por `correlationParent` en cascada + hija tiene sentido operativo; `cummins-M1-mains-loss` idem). Los ruleId son de sola escritura una vez. Convención firme: **`cat-{siteCode}-{inferenceId}`** — ej. `cat-CR00061-A1`, `cat-CR00061-C1`, `cat-CR00061-H2`. La trazabilidad al catálogo va en el campo **`inferenceId` que YA EXISTE en el schema** (`app/api/models/rule_definition.js`, poblado en las 5 reglas productivas). Cero cambio de schema, cero migración. **(vi) ARQUITECTURA DEL CÁLCULO — vive AL LADO del motor, NO adentro.** El motor es **reactivo por diseño** — DEC-REF-18 y DEC-REF-26 lo defendieron explícitamente al implementar EDGE-2 sin `setInterval` (grep confirmado: cero timers de motor en `edge-engine/`; los únicos `setTimeout` son del retry Telegram en `notificationRouter.js`). **Meter cálculo periódico DENTRO del motor rompe una decisión firmada 3 veces.** Diseño correcto: **módulo de cálculo con reloj propio**, corre como proceso Node separado (o dentro del mismo container edge pero módulo independiente), lee `db.data` por ventana temporal (helper reusable en `dashboard_noc.js:506-524` — pipeline de bucketing avg/min/max), calcula la variable derivada (autonomía, discrepancia, promedio móvil), y **PUBLICA la variable derivada al topic estándar** `{userId}/{dId}/{variable}/sdata` con `{value, save:1}`. **El motor la ve como cualquier otra sdata** y una regla D normal la vigila. **Cero cambio de motor.** **Verificado en RECON pre-paréntesis (V5):** (a) el edge se escucha a sí mismo por defecto (suscripción `+/+/+/sdata` sin `nl:true` en `edge-engine/index.js:161`) — RECIBIRÍA su propia publicación derivada, que es exactamente lo que necesita para evaluar la regla D; (b) cero riesgo de lazo (grep `publish.*sdata` en `edge-engine/*.js` = 0 hits — el motor solo publica `alarm/notif` y `wanomi/noc/*/event`, nunca sdata); (c) ACL del edge sin restricción (usuario `superiotix` con `publish:["#"], subscribe:["#"]` en `iotix.emqxauthrules`); (d) volumen estimado ~7,200 muestras/día/site con 5 vars derivadas @60s (~+12% sobre las 58k/día actuales, ASUMIBLE). **(vii) NOTIFICACIÓN vs PREDICCIÓN — la diferencia no está en el motor, está en QUÉ VARIABLE SE VIGILA.** Notificación = variable medida directamente (`fuel_level < 15` → alarma inmediata). Predicción = variable derivada calculada por el módulo (i) — ejemplo: `autonomy_hours = fuel_level / fuel_rate_derivado` publicado como sdata, vigilado por regla D `autonomy_hours < 4` → notificación anticipada de agotamiento antes de que `fuel_level` baje del umbral duro. **Un solo mecanismo de pendiente desbloquea la mitad del catálogo:** G3 (autonomía proyectada) → H4 (sitio caído por combustible) → C2 (degradación gradual de presión de aceite) → E3/E5 (batería y cargador via voltaje derivado en ventana). **(viii) FUERA DEL PARÉNTESIS, con fundamento anti-DEC-PRED-1** (nunca prometer lo que no se puede validar). **C4** (aceite degradado por horas + temperatura): necesita confirmación de mantenimiento (ejercicio del punto iii todavía no genera N ciclos suficientes para modelo Arrhenius realista), función diferida. **D3** (horas anómalas): necesita historia REAL de operación, que **recién EMPIEZA A EXISTIR** con el ciclo de ejercicio del punto iii — no hay 30 días de patrón hoy, cualquier "línea base del sitio" sería estadísticamente ruido (verificado con Mongo). **E4** (cargador no recupera batería): necesita máquina de estados post-marcha del cargador Eltek, mismo bloqueo que iv extendido a rectificador. **I2-I5** (decodificador de bitmaps 42100/01/02/10): es traducción no anticipación — más higiene que valor operativo, se pospone sin bloqueo. **A6/B2-B4** (governor/AVR + rendimiento + combustión + filtro): hipótesis SIN validar (marca 🔵 en el catálogo), DEC-PRED-1 dice que no se prometen en el pitch (mentir es peor que no incluir). **Bump de versión al aterrizar en corpus.** Higiene DEC-PROC-4.a/b en todas las escrituras: verificar ID libre con grep, anclar tras la fila vecina, verificar posición ANTES de escribir. Filas madre DEC-REF-75 / -A / -B / -C / -C-A / -D / -76 / -76-A / -76-B / -76-C intactas (append-only). |
| DEC-REF-77-A | 2026-07-27 | **Adenda DEC-REF-77 (iii) — la rutina de ejercicio del simulador NO puede reusar `mains_failure_ats_transfer` (#53/Fase 3, pre-código).** DEC-REF-77 (iii) firmó *"ciclo de ejercicio semanal"* como práctica estándar de operador telco. El escenario más cercano en el simulador actual es `mains_failure_ats_transfer`, pero ese escenario **BAJA LA TENSIÓN DE RED** — dispararía **alarma de pérdida de energía comercial cada semana**. **Un ejercicio programado NO implica corte:** el ATS arranca el grupo en modo prueba **con la red presente**, típicamente vía relé de test remoto o rutina interna del propio controlador; la tensión comercial se mantiene, el `transfer_state` puede o no cambiar (depende del modo del ATS — "test load" transfiere carga al gen manteniendo mains; "test no-load" arranca el motor sin transferir). **Requiere escenario propio.** Nombre firmado: **`weekly_exercise`**. Cambios respecto de `mains_failure_ats_transfer`: (a) `mains_voltage` NO baja; (b) `mains_freq` NO baja; (c) `genset_running` = true durante la duración parametrizada; (d) `rpm`, `oil_pressure`, `coolant_temp`, `battery_voltage`, `run_hours` se mueven coherentemente (ver DEC-REF-79 (iii) para el trazo esperado); (e) `fuel_level` baja a **1,384 %/h** (= 3,46 L/h ÷ 250 L × 100, ver DEC-REF-79 (ii)); (f) transición: standby → running → cooling → standby, **sin evento de red**. Fila madre DEC-REF-77 intacta (append-only). |
| DEC-REF-78 | 2026-07-27 | **Selección de device es preferencia de sesión del usuario, no estado persistente del device — bug de tenant destapado durante el test decisivo del fix `be9d799` (#53).** **(i) Principio.** La selección de device es PREFERENCIA DE SESIÓN del usuario, no estado del device. Guardarla en el documento del device significa que dos usuarios no pueden mirar equipos distintos al mismo tiempo (el flag `selected: Boolean` es único por-device en el schema, sin partición por-user). **(ii) Familia con DEC-REF-75-B (iii).** Ahí sacamos `selectedDevice`/`userId` de adentro del widget porque *"el Template es molde compartido sin device"*. Esto es lo mismo un nivel más arriba: la SELECCIÓN es propiedad del observador, no del observado. Herencia del modelo IoTiX (un usuario y sus dispositivos, sin cruce) que se rompe cuando el superadmin de Wanomi mira parques ajenos por `buildReadFilter` — modelo que Wanomi extiende, no hereda. **(iii) BUG QUE LO DESTAPÓ.** `selectDevice(userId, dId)` en `app/api/routes/devices.js:290-308` filtra por el userId del CALLER: `Device.updateMany({ userId }, { selected: false })` + `Device.updateOne({ dId, userId }, { selected: true })`. Un superadmin (`admin@wanomi.com`) que selecciona en el select de `dashboard-admin.vue` un device de otro tenant (`operator-claro@wanomi.platform`): (a) desmarca los suyos con updateMany, (b) NO matchea el updateOne del ajeno, (c) retorna `true` engañosamente. Resultado: **ningún device queda con `selected:true`**, el store `getDevices()` no entra al `if(device.selected)`, `selectedDevice` no se refresca, y la grilla queda pegada al último device del propio admin que sí tuvo éxito (típicamente un ELTEK del admin). **Verificado en Mongo** (recon pre-código: `count({selected:true})` va de 1 a 0 tras cambios entre los 9 devices ajenos). El fix `be9d799` (`:key` compuesto `dId+variable` + watcher LiveValue + siembra UI-7) atacaba un eje distinto — nunca se llegó a ejercer porque este bug tapa el flujo antes. **(iv) ALCANCE.** Backend `app/api/routes/devices.js`: se elimina el endpoint `PUT /device`, el helper `selectDevice()`, y sus TRES llamadores — `POST /device:119` (alta), `DELETE /device:193-211` (bloque de re-selección tras borrado), y el endpoint `PUT /device:233-254` propio. Frontend `app/store/index.js`: `getDevices()` reemplaza la lectura de `device.selected` por lectura de `localStorage.getItem('lastSelectedDId:<userId>')` con **fallback determinístico al primero de la lista ORDENADA por `_id`** (campo estable y determinístico del servidor, no orden de llegada del array). Frontend `app/pages/dashboard-admin.vue`: `selectDevice()` reemplaza `PUT + dispatch getDevices` por `commit setSelectedDevice + localStorage.setItem` — sin round-trip a Mongo por selección. **(v) CASOS RAROS ESPECIFICADOS.** (a) **Scope vacío** (usuario sin devices por `buildReadFilter`) → pantalla con el mensaje ya presente en `dashboard-admin.vue:41-43` ("Elegí un dispositivo…"); **NUNCA TypeError** sobre `selectedDevice.template.widgets` (misma familia que el crash de `selectedDevice.dId` cazado en C5.1 vía `fixWidget` defensivo, DEC-REF-76 §v(a)). (b) **Sin preferencia guardada** → primer device de la lista **ORDENADA por `_id`** (campo estable del servidor, no orden accidental de `Device.find(...)`); el backend garantiza el orden estable con `.sort({_id: 1})` en `GET /device`. (c) **Device borrado que era el activo** → `lastSelectedDId:<userId>` no matchea ningún device de la lista fresca → cae al fallback (primer device); el frontend hace `commit` al store al mount siguiente, cerrando el hueco sin round-trip. (d) **Alta de device desde `devices.vue`** → hoy el POST llama `selectDevice(userId, newDevice.dId)` como side-effect; con la eliminación del helper, el frontend de `devices.vue` setea `localStorage.setItem('lastSelectedDId:<userId>', newDevice.dId)` tras el POST — misma experiencia UI que hoy, del lado cliente. **(vi) `Device.selected` DEPRECADO.** El campo (`app/api/models/device.js:12`, `Boolean required default:false`) queda por back-compat; los documentos existentes conservan valores que ya no significan nada. **Nada lo lee.** No confiar en él para ninguna decisión. **Se registra pendiente de removerlo del schema** en próxima limpieza (candidato: nueva familia `BACKLOG-SCHEMA-1` si aparecen más pendientes de cleanup del schema Device; hoy va como nota sin id porque es el primer pendiente de su familia y crear una familia por un item aislado es prematuro). Filas madre DEC-REF-75-B / -76-A intactas (append-only); DEC-REF-77 vecino intacto (append tras 77). |
| DEC-REF-78-A | 2026-07-28 | **Adenda DEC-REF-78 — misma raíz apareció en `simulator.js` (5 endpoints), no fue coincidencia (#53/Fase 3).** El patrón *"filtrar por `userId: req.userData._id` en operaciones sobre devices"* que DEC-REF-78 cerró en `app/api/routes/devices.js` (bug de tenant destapado en Fase 1) reaparece en **5 endpoints** de `app/api/routes/simulator.js`: (1) `GET /simulator/devices:140` filtra la lista por `userId`; (2) `POST /simulator/trigger` via helper `resolveDeviceAndTemplate(userId, dId):116`; (3) `POST /simulator/set` via mismo helper; (4) `POST /simulator/scenario:293` hardcoded `Device.findOne({ userId, dId, firmwareType:'wanomi-sim' })`; (5) `POST /simulator/reset:321` hardcoded igual. **Consecuencia para la demo Claro (Fase 3):** admin@wanomi.com sólo puede disparar escenarios sobre los devices que le pertenecen (ATS + 3 ELTEK del site CR00061). **BLOQUEA** la demostración de `fuel_drawdown` (autonomía), `service_due` (mantenimiento), `cummins_setpoint_lost` (auto-calibración, acto 3), `fuel_siphon`, `genset_no_start`, `genset_vibration_anomaly` y `battery_degraded` — todos escenarios sobre el Cummins/GEN/SEC de CR00061 que son de `operator-claro`. **NO ES OBSERVACIÓN — es bloqueante del guion.** **Fix aplicado en el mismo turno de esta adenda** (misma técnica que DEC-REF-78 §iv): reemplazar `{ userId, ... }` por `{ ...buildWriteFilter(req, 'Device'), ... }` en los 5 sitios; el helper `resolveDeviceAndTemplate` cambia firma de `(userId, dId)` a `(req, dId)`. El filter de Template también se libera del userId — los templates no están particionados por userId en el resto del código productivo (verificado con `devices.js:60` que usa `getTemplates(tplIds)` sin filtro userId). **Familia con DEC-REF-78, no coincidencia:** el patrón *"filtrar por el dueño en operaciones sobre entidades del site"* está esparcido por el backend y va a volver a aparecer (candidatos: endpoints del edge, alarms sobre devices ajenos). Cada instancia se registra como adenda a DEC-REF-78 para que el patrón quede visible como familia, no requiere nueva DEC por instancia. Filas madre DEC-REF-75-B / -76-A / -78 intactas (append-only); DEC-REF-79 vecino intacto. |
| DEC-REF-79 | 2026-07-27 | **Parámetros físicos reales de CR00061 para las reglas de combustible y el ciclo de ejercicio del simulador (#53/Fase 3, precede al código por DEC-PROC-6).** **(i) Capacidad y forma del tanque: 250 L, rectangular.** Dato aportado por Franco en la apertura de Fase 3, **NO estaba en el corpus**. Va al campo `tankCapacity` del sub-schema de widget que existe desde DEC-REF-75 §1 y **nunca se pobló** en producción. **Rectangular ⇒ altura proporcional a volumen** ⇒ el porcentaje que reporta la sonda es lineal respecto del volumen — la linealidad de la lectura queda garantizada por la geometría del tanque, no por la tecnología del sensor. **(ii) Consumo de referencia: 3,46 L/h — DERIVADO DE EVIDENCIA DE CAMPO del propio parque de Claro.** CR00070 Ituzaingó consumió **1.410 L en 17 días** con el grupo operando 24/7 tras perder suministro comercial (registrado en el estudio de conectividad del Hub). `1410 / (17×24) = 3,4558...` ≈ **3,46 L/h**. **NO es estimación, es medición** de un sitio real de la misma región con la misma clase de equipo. **Consecuencia aritmética:** tanque lleno ≈ 72 h ≈ 3 días de corte. **El umbral de 48 h dispara al 66% del tanque; el de 12 h al 17%. Ambos DENTRO del rango del tanque** — los umbrales del diseño de reglas son viables. La preocupación de "alarma permanente" queda descartada por aritmética. **(iii) EL GRUPO DE CR00061 ES DE RESPALDO, NO CONTINUO — evidencia del relevamiento de campo (sesión #15).** Motor frío (64 °F), aceite 0 PSI (standby), **2969,1 h acumuladas** en ~2-3 h/día promedio sobre 3-4 años de instalación. **Consecuencia de especificación** — la autonomía tiene DOS lecturas: (a) motor **CORRIENDO** → horas restantes al consumo actual → **ALARMA**; (b) motor **PARADO** → *"aguantaría X h si arrancara ahora"* → **INFORMATIVO, no alarma** (con consumo cero la división es indefinida). **La regla de autonomía sólo evalúa con el motor corriendo.** El valor informativo se muestra sin alarmar. Trazo esperado del ciclo `weekly_exercise` (DEC-REF-77-A) sobre las variables: `rpm` sube a régimen (~1800 rpm nominal Cummins PowerCommand), `oil_pressure` sube a rango operativo (~40-60 psi), `coolant_temp` sube gradual hacia ~85 °C, `battery_voltage` sube por alternador cargando (~13,8-14,4 V), `fuel_level` baja a 1,384 %/h, `run_hours` incrementa a razón real. **(iv) Unidad de `fuel_level`: PORCENTAJE.** La variable publicada se mantiene en porcentaje. La **autonomía se calcula sin capacidad** (nivel ÷ pendiente del nivel = horas; las unidades se cancelan). La capacidad se usa como CONFIGURACIÓN para (a) convertir la curva de consumo del fabricante (que viene en L/h) al porcentaje por hora que compara el motor, y (b) que la recomendación exprese litros (*"cargar 150 L para llegar a 48 h"* es una instrucción; *"el tanque está al 40%"* es un dato). **Fundamento de NO migrar a litros:** la regla productiva `cummins-G2-fuel-critical` (`seeds/cummins_pcc_v1.js`) compara contra 15 (porcentaje); migrar obligaría a tocarla y no aporta nada al cálculo. **(v) CONTRADICCIÓN DEL SENSOR — registrada para que la cierre Área 3.** **DEC-SENSOR-1** (`docs/wanomi.md:1076`) declara sonda **HIDROSTÁTICA por defecto (capacitiva opcional)** por retrofit sin perforar tanque + 2 casos documentados de sender de fábrica trabado (CR00058). El relevamiento R5 de esta sesión + `docs/informe_instalacion.md` §5.3 (líneas 532-568) + `docs/pcb/GEN.md:167,346,532` declaran sensor **ULTRASÓNICO JSN-SR04T** (cable 4 hilos, TRIG GPIO5, ECHO GPIO6, IP67, JST-PH 4 pines). Son tecnologías distintas y **ambas están escritas en el corpus**. **NO BLOQUEA Fase 3** — con tanque rectangular la linealidad se sostiene con cualquiera de las dos. **Pendiente de Área 3** junto con la decisión de **dónde vive la conversión distancia→volumen**. **Recomendación de sala:** el CORE publica la medición cruda y el Hub convierte con la geometría como configuración — así un tanque distinto no obliga a reprogramar la placa en campo. **(vi) VALOR INICIAL DE `run_hours` PARA EL SIMULADOR: 2969,1** — el valor REAL del relevamiento, no una estimación. **Detalle con valor de demo:** 2969,1 son 11,88 ciclos de servicio de 250 h; **el próximo cae en 3000, o sea a 31 h de motor**. El sitio real está a ~un mes de su próximo servicio — habilita disparar la regla D1/D2 del catálogo (`run_hours` cerca de 250h next-service) sin fabricar datos. Filas madre DEC-REF-75 / -A / -B / -C / -C-A / -D / -76 / -76-A / -76-B / -76-C / -77 / -77-A / -78 intactas (append-only). |
| DEC-REF-79-A | 2026-07-27 | **Adenda DEC-REF-79 (iii) — corrección de `rpm` nominal Cummins (#53/Fase 3, pre-código).** DEC-REF-79 (iii) mencionó *"~1800 rpm nominal Cummins PowerCommand"* en el trazo esperado de `weekly_exercise`. **ES 1500 rpm, NO 1800.** El simulador ya usa `target 1500` (`sensor-engine.js:152`, DEC-REF-56-A) y publica `gen_freq: 50` Hz. **Alternador de 4 polos a 1500 rpm = 50 Hz = frecuencia de Argentina** (60 Hz sería 1800 rpm — mercados NEMA). Corrección aplicada por adenda; fila madre DEC-REF-79 intacta (append-only). |
| DEC-REF-79-B | 2026-07-28 | **Adenda DEC-REF-79 (vii) — dos ritmos de consumo, dos escenarios, dos propósitos (#53/Fase 3, pre-código).** El consumo realista de DEC-REF-79 (ii) — **1,384 %/h** — hace que un ejercicio de 5 min baje el tanque **0,115%** (invisible) y que el tramo 66%→15% tarde **37 horas**. **Sirve para la COHERENCIA del ciclo de ejercicio; NO sirve para demostrar la anticipación de autonomía.** **RESOLUCIÓN — dos escenarios separados con propósitos distintos:** **(a) `weekly_exercise`** → **ritmo REAL** (1,384 %/h del punto ii). Propósito: que `run_hours`, `oil_pressure`, `coolant_temp` y `battery_voltage` se muevan coherentes con el motor corriendo. **El combustible casi no baja, y es correcto.** **(b) `fuel_drawdown` (NUEVO)** → **ritmo ACELERADO Y DECLARADO en el config**, no escondido. Propósito: cruzar los umbrales de autonomía en minutos. Arranca el tanque por encima del umbral de 48 h y lo baja hasta cruzar el de 12 h. **Fundamento de que la aceleración NO es maquillaje:** el cálculo de autonomía es correcto para la pendiente observada (`nivel ÷ pendiente = horas`, la aritmética se sostiene). Lo que se comprime es el **TIEMPO**, que es la razón de ser del simulador. **Precedente vigente:** `fuel_siphon` baja el tanque en 15 s lo que en realidad tarda horas — mismo patrón, ya aceptado. **Nota para el guion (no es código):** el número de autonomía visible en la reunión es el de la simulación, no el del sitio real; se dice al pasar *"el simulador comprime el tiempo para que vean en minutos lo que en el sitio pasa en horas"*. Filas madre DEC-REF-79 / -A intactas (append-only). |
| DEC-REF-79-B-A | 2026-07-28 | **Adenda DEC-REF-79-B (b) — `fuel_drawdown` NO vive solo: secuencia obligatoria con motor corriendo previo (#53/Fase 3, doc-only).** V5 del test decisivo confirmó que `_runScenario` (`tools/device_simulator/lib/device.js:257,300-310`) llama `_cancelActiveTimers()` al inicio: **durante un escenario solo publican las variables que los steps setean**. `fuel_drawdown` setea únicamente `fuel_level` → `rpm`, `oil_pressure`, `coolant_temp`, `run_hours` y `battery_voltage` **quedan congelados en los valores de REPOSO** que tenían al disparo. **Efecto visual en `/sites/CR00061`:** el tanque baja 6 min **con el motor apagado** — **la firma del SIFONEO (`fuel_siphon`), no del consumo** — contradice DEC-REF-79 (iii) (la autonomía solo tiene sentido con el motor corriendo). **RESOLUCIÓN — secuencia obligatoria, sin código nuevo:** **(1)** `mains_failure_ats_transfer` sobre el ATS (`noCleanup: true` deja al grupo corriendo — steps propagan `gen_status='RUNNING'` por `sharedState.gen_running`, y los timers del Cummins evolucionan las 6 variables a valores de motor corriendo). **(2)** Esperar 2-3 min a que el Cummins llegue a régimen (`rpm ~1500`, `oil_pressure 35-55 psi`, `coolant_temp 75-95 °C`, `battery_voltage ~14,1 V`, `run_hours` incrementando). **(3)** `fuel_drawdown` sobre el Cummins → sus timers se apagan (V5) y las 6 variables **quedan congeladas en los valores de motor corriendo** que capturaron en el paso (2); el nivel de combustible baja según los steps del escenario, cruzando los umbrales 48h y 12h. **NO cambia el código del escenario** — se agrega un comentario en `sensor-engine.js:SCENARIOS.fuel_drawdown` con la secuencia y se registra acá para que la consola de la demo la respete. **RECORDATORIO (ya flagueado, sigue vigente):** `fuel_drawdown` **todavía no demuestra ANTICIPACIÓN de autonomía** — falta el cálculo de pendiente (`nivel ÷ pendiente = horas`) publicado como variable derivada, más las reglas de autonomía sembradas del catálogo (Fase posterior). Hoy solo dispara la regla existente `cummins-G2-fuel-critical` (`<15`) al cruzar el umbral final. **Es el orden acordado, no un error.** Filas madre DEC-REF-79 / -A / -B intactas (append-only). |
| DEC-REF-80 | 2026-07-29 | **Siembra Fase 4 del paréntesis pre-reunión Claro — 10 reglas del catálogo #18 con fuente aterrizada; dos packs (cummins-pcc-v1 ampliado + ats-inteliats-v1 NUEVO); fix inferenceId productivo; cita normativa pendiente (#53/Fase 4.2).** **(i) Alcance — 10 reglas nuevas con prefijo `cat-CR00061-*` (DEC-REF-77 v).** **Pack `cummins-pcc-v1` AMPLIADO (5 reglas):** **`cat-CR00061-C1`** temperatura de refrigerante alta · type C · `coolant_temp > coolant_temp_setpoint` (95°C publicado por el sim, DEC-REF-66.d) · deviceType CUMMINS · recom *"Temperatura de refrigerante fuera de rango. Verificar radiador, ventilador y nivel. Reducir carga inmediatamente. Detener el motor si no baja en 5 min."* · **`cat-CR00061-D1`** próximo servicio · type D · `run_hours > 2750` (aritmética pura: 250 h antes del threshold 3000) · deviceType CUMMINS · recom *"Próximo mantenimiento del motor en menos de 250 h. Programar servicio."* · **`cat-CR00061-D2`** servicio vencido · type D · `run_hours > 3000` (aritmética pura) · deviceType CUMMINS · recom *"Mantenimiento del motor VENCIDO. Contactar técnico. Operar aumenta riesgo de falla mayor."* · **`cat-CR00061-G2`** combustible crítico · type D · `fuel_level < 5` (fuente catálogo #18, misma que G1 productiva) · deviceType CUMMINS · recom *"Nivel de combustible CRÍTICO (<5%). Grupo puede pararse por corte de suministro. Reabastecer inmediatamente. Verificar sensor físico (CR00058 clavado, DEC-REF-56)."* · **`cat-CR00061-D4`** arranques excesivos · type D · `crank_attempts_failed >= 3` (fuente: sim modela hasta 3 en el escenario `genset_no_start`) · deviceType GEN (variable vive en template WN-SITE-GEN-v2, no Cummins) · recom *"3 intentos de arranque fallidos consecutivos. Motor no arranca. Verificar batería, combustible y sistema de arranque."* **Pack `ats-inteliats-v1` NUEVO (5 reglas — deviceType ATS):** **`cat-CR00061-F1a`** subtensión red · type D · `mains_voltage < 198` · recom *"Tensión de red por debajo de 198 V. Riesgo de daño a motores. Verificar suministro comercial."* · **`cat-CR00061-F1b`** sobretensión red · type D · `mains_voltage > 242` · recom *"Tensión de red por encima de 242 V. Riesgo de daño a electrónica. Verificar suministro comercial y protecciones."* · **`cat-CR00061-F2a`** subfrecuencia red · type D · `mains_freq < 49.5` · recom *"Frecuencia de red por debajo de 49,5 Hz. Verificar suministro comercial."* · **`cat-CR00061-F2b`** sobrefrecuencia red · type D · `mains_freq > 50.5` · recom *"Frecuencia de red por encima de 50,5 Hz. Verificar suministro comercial."* · **`cat-CR00061-F4`** gen corriendo sin generar · type cross (single-device del ATS) · `gen_status='RUNNING' AND gen_voltage < 100` · recom *"Grupo corriendo sin generar tensión. Verificar excitación del alternador o breaker del grupo."* **(ii) DESDOBLADO F1/F2.** Una regla type D expresa **una variable + una condición** (un solo `op` + `value`; no expresa OR). Sub y sobretensión son **fallas semánticamente distintas** — subtensión sostenida quema motores, sobretensión quema electrónica, acciones difieren. Se desdoblan en 4 reglas (F1a/F1b/F2a/F2b). **Total sembrables: 10, no 8** (contra reporte previo 4.1-TER). **(iii) FIX del `inferenceId` productivo — 1 línea, cero riesgo funcional.** Verificado con grep exhaustivo (C4 recon Fase 4.2): `inferenceId` es **solo trazabilidad**, ninguna condicional de dispatch/join/lookup en el motor lo consume — solo se copia hacia adelante en el payload (`ruleEngine.js:85,115,220,259`; `notificationRouter.js:18,101,131`). `correlationParent` apunta al `ruleId`, no al `inferenceId` (verificado: único uso no-null es `correlationParent: 'cummins-M1-mains-loss'`, apunta al ruleId). **Cambio:** `cummins-C1-mains-loss-gen-no-start.inferenceId` pasa de `'C1'` → `'H1'` en `seeds/cummins_pcc_v1.js:131` (H1 es lo que implementa según catálogo: cascada AC→gen no arranca). **Libera `'C1'`** para la nueva `cat-CR00061-C1` (temp refrigerante) con trazabilidad limpia al catálogo. Cero cambio de comportamiento runtime. **(iv) DOS PACKS, no uno — BACKLOG-RULE-5 disparador CUMPLIDO.** BACKLOG-RULE-5 declaró disparador: *"creación del pack ATS productivo (segundo equipo ATS con reglas propias)"* — se cumple con `ats-inteliats-v1` NUEVO. **Motor filtra por `rule.deviceType`, NO por `pack.deviceType`** (verificado #42-R4 B4, `ruleEngine.js:29-30`) — cero riesgo de comportamiento. **Migrar `cummins-M1-mains-loss` al pack ATS queda OPCIONAL, fuera del paréntesis.** **(v) CITA NORMATIVA PENDIENTE, NO se atribuye ahora.** Los textos de F1a/F1b/F2a/F2b mencionan las bandas `198-242 V` / `49,5-50,5 Hz` **SIN atribución** — no se verificó que AEA sea el organismo correcto (AEA norma **instalaciones**; calidad de suministro la regula el **ENRE** en AR). Una cita normativa errada en texto que lee un ingeniero de Claro nos deja mal. **Pendiente de Área 1:** confirmar la fuente normativa; cuando se confirme se agrega la cita al texto. **(vi) NOTAS (no bloquean la siembra):** (a) **D4 depende del kit SENSE** — `crank_attempts_failed` vive en template `WN-SITE-GEN-v2`, no en `WN-GEN-Cummins-PowerCommand`. El Cummins reporta el intento; el contador de fallos consecutivos lo mantiene SENSE. Funciona en la demo porque el sim lo publica; **en un sitio Connect puro (sin SENSE) no existiría — NO prometerla como capacidad Connect.** (b) **G2 hereda el sensor mentiroso** — DEC-REF-56 documenta el sender de fábrica de CR00058 clavado en 24% con tanque vacío. Si miente, la crítica <5% no dispara. El texto ya lo advierte (*"Verificar sensor físico"*). Consistente con G1 productiva. **(vii) Convención `cat-{siteCode}-{inferenceId}`** — todas las 10 nuevas llevan prefijo. NO se renombra ninguna productiva (fix del inferenceId de C1 productiva NO es rename, es corrección de trazabilidad — cambio de valor de un campo). **Subtotal Fase 4:** 5 productivas previas + 10 nuevas sembradas + fix inferenceId → 15 reglas activas totales tras siembra, 8 recomendaciones nuevas al operador. Fuera de siembra: A2, A3, C2, C3, C4, E1, E2, F3, H3, I1 (10 del catálogo sin fuente aterrizable hoy). Filas madre DEC-REF-77 / -A / -78 / -78-A / -79 / -A / -B / -B-A intactas (append-only). |
| DEC-REF-81 | 2026-07-29 | **Diagnóstico de plataforma del paréntesis pre-reunión Claro, aterrizado sobre recon F0/F0-bis (#53).** **(i) ATS invisible — UNA causa, TRES síntomas.** El device `59XYsglM` (CR00061-ATS) **no tiene campo `siteId`** en su documento (G1). Los otros 6 devices del sitio sí lo tienen. Consecuencias verificadas: (a) queda fuera de `Device.find({siteId:{$in:siteCodes}})` (`dashboard_noc.js:167-170`) → no aparece en el panel; (b) no aparece en `/sites/CR00061`; (c) **es invisible al cellowner** — `buildReadFilter` compone `{$or:[{userId:<cellowner>},{siteId:{$in:[4 codes]}}]}` y el ATS no matchea ninguna rama (G2: 6 de 7 dIds visibles). Además `site.devices` lista el dId viejo `6z4LN2md` — **referencia colgante, sin documento** — y NO lista `59XYsglM` (R5). **Alcance del fix: `device.siteId` + `site.devices`. NADA MÁS.** **(ii) `userId` del ATS y de los 3 ELTEK NO se toca.** Los cuatro están bajo `admin@wanomi.com`, no bajo `operator-claro` (G1). El topic MQTT lleva el `userId` como primer segmento: migrarlo invalida credenciales y **mata la publicación**, que es el incidente ya vivido en #52/Fase 2. Se registra como **BACKLOG-TENANT-10** (consolidar ownership de los 4 devices MVP de CR00061 bajo `operator-claro`, con rotación coordinada de credenciales MQTT). Disparador: despliegue productivo, NO el paréntesis. **(iii) `deviceType` del ATS es `""`** (G1) mientras las reglas vivas `cummins-M1-mains-loss` y `cummins-C1-mains-loss-gen-no-start` declaran `deviceType:'ATS'` (R1) y la cascada funciona. **El motor NO resuelve `deviceType` desde `db.devices`.** Verificación obligatoria de dónde lo resuelve ANTES de completar ese campo — completarlo es cambio de comportamiento potencial, no cosmético. **(iv) "Resuelto" NO es la carrera de BACKLOG-EDGE-5.** R7 prueba que `fire` y `resolve` existen ambos en Mongo, correctos, separados por 155 s. `recentAlarms` (`dashboard_noc.js:282-283`) es un `find().sort({time:-1}).limit(10)` **sin correlación**: devuelve los dos como ítems independientes y `NocRecentAlarms.vue:14` pinta el badge verde solo en el ítem `resolve`, dejando el `fire` rojo indefinidamente. **Fix: replicar el pipeline de `activeEpisodesPromise` (`dashboard_noc.js:193-204`), que ya colapsa por `(ruleId,siteId)` y ya expone `lastKind` — probado en producción.** Diferencia: `activeEpisodes` EXCLUYE resueltos; `recentAlarms` debe INCLUIRLOS marcados. **(v) HIPÓTESIS a validar visualmente antes de tocar el router:** el "retraso del panel" reportado por Franco puede ser el MISMO síntoma que (iv) — una alarma que nunca se apaga se lee como panel que no actualiza. **El fix de raíz de EDGE-5 (`await saveToMongo` antes de los dispatches en `notificationRouter.js:264-274`) NO se ejecuta hasta que Franco mire el panel con (iv) aplicado.** Toca el hot path de los 4 canales a 6 días de la reunión: no se paga ese riesgo sin evidencia de que hace falta. La mitigación de #49/R8 (doble refresh 1s+4s con `?fresh=1`, `dashboard.vue:161-173,184-186`) **sigue vigente en el código fuente** (R6) — no hubo regresión. **(vi) El select del navbar es OTRO componente, no la tarjeta.** `DashboardNavbar.vue:43-75` (`base-dropdown`, no `<select>` HTML) ← `store/index.js:84-100` ← `GET /notifications` ← `getNotifications(filter)` (`webhooks.js:400-408`) = `Notification.find({...filter, readed:false})` **sin `.sort()` y sin `.limit()`** → orden natural y TODAS las no leídas. **El patrón correcto ya existe en el mismo archivo:** `getNotificationsBySite` (`webhooks.js:410-421`) ordena `time:-1` y limita. Fix: aplicar el mismo criterio, conservando `readed:false` (es la semántica del dropdown, no un bug). **(vii) `transfer_state` sigue singleton `{'AUTO'}`** — 3 únicos sitios en el código, `evolve()` no muta strings (R4). **`ats-T1/T2` NO sembrables. BACKLOG-SIM-4 sigue abierto.** **(viii) `cummins_setpoint_lost` suprime AMBOS setpoints** — `coolant_temp_setpoint` Y `oil_pressure_setpoint` publican `null` en el mismo tick (G4, `sensor-engine.js:258-264`), con contra-escenario `cummins_setpoint_restore`. Habilita el acto 3 sobre cualquiera de los dos. **(ix) NO existe NINGUNA regla viva sobre `coolant_temp`** (R1, grep con 0 hits sobre las 5 reglas del único pack). La "C validada E2E en #48" fue un fixture, no quedó sembrada. **El acto 3 de la demostración no tiene regla que lo ejecute hasta que F4 siembre la escalera de refrigerante.** **(x) La siembra de DEC-REF-80 NUNCA se aplicó** — `db.rulepacks.count({"rules.ruleId":/cat-CR00061/}) = 0` (R2). No hay nada que retirar de Mongo; DEC-REF-80 se corrige por adenda documental y F4 siembra ya reconciliado. **(xi) CORRECCIONES DE LA SALA, registradas sin atenuar:** (a) la auditoría de #53 afirmó que `cummins-M1` tiene umbral `lt 50` — **es `lt 100`** (R1, dump de `db.rulepacks`); el error salió de tratar un fragmento de bitácora como evidencia de runtime. La colisión F1a↔M1 sobrevive igual (`< 198` incluye el 0). (b) la auditoría afirmó que el sim no publica bitmaps — **sí los publica** (`bitmap_42100/01/02/10`, R3), constantes en 0; quedan fuera de siembra por falta de movimiento y de operador de máscara, no por ausencia. (c) la sala propuso autenticar el loader como `operator-claro` — **cuenta SIN login** (DEC-REF-36); R10 confirma que `rule_pack.js` no tiene ningún campo de tenancy y que el gate es RBAC puro, por lo que `admin@wanomi.com` (superadmin) crea packs sin suplantar a nadie. **(xii) Deuda menor observada, no bloqueante:** `DeviceList.vue:50-54` tiene `SITE_LABELS` hardcodeado sin `CR00061` → el sitio de la demostración muestra etiqueta vacía; y `roleLabel` (l.104-109) solo mapea SEC y GEN, dejando CUMMINS/ATS/ELTEK con el `name` crudo. Se corrige en el bloque de la consola del simulador. Filas madre DEC-REF-77 / -A / -78 / -78-A / -79 / -A / -B / -B-A / -80 intactas (append-only). |
| DEC-REF-82 | 2026-07-29 | **Vocabulario del tipo de alarma en el panel y join de la regla al componer `recentAlarms` (#53/F1, pre-código).** **(i) El "tipo de alarma" es el TIPO DE REGLA — decisión ya tomada.** DEC-REF-77 (i) lo fija: *"tipo de alarma en la fila (D/C/S/cross visible al operador)"*. No se crea taxonomía nueva ni se deriva de `severity`. **(ii) Se muestra en criollo, no la letra.** Se aplica el patrón de DEC-REF-69 (labels en castellano + definición operativa). Mapa FIJO, única fuente de verdad para el frontend: · `D` → **"Umbral fijo"** · *el valor cruzó un límite declarado* · `C` → **"Calibrada al equipo"** · *el límite lo pone el propio equipo* · `S` → **"Sostenida en el tiempo"** · *la condición se mantuvo durante una ventana* · `cross` → **"Cruzada"** · *combina varias señales o equipos* Tipo desconocido o ausente ⇒ **no se muestra subtítulo de tipo**. NO se inventa un default ni se cae a "D". **(iii) Fundamento de producto, no cosmético:** *"Calibrada al equipo"* es el enunciado legible de la diferencia que el acto 3 de la demostración quiere mostrar — el umbral no lo puso Wanomi, lo puso el equipo. Sin esa etiqueta el operador no puede distinguir una regla auto-calibrada de una cableada. **(iv) HUECO DE DATO — la notificación NO transporta el tipo.** Campos reales del documento (R7): `ruleId, siteId, dId, severity, kind, mode, value, thresholdUsed, time, correlationParent`. **No existe `type`.** `mode` vale `cross`/`calibrated` en el `fire` pero `resolve-by-condition` en el `resolve` → **no es sustituto**. **(v) RESOLUCIÓN — join de lectura, cero cambios al write path.** Al componer `recentAlarms` (`dashboard_noc.js`), resolver por `ruleId` contra `db.rulepacks` (colección única, R1/R10) y proyectar al ítem: `type`, `label`, `recommendation`. **NO se agrega ningún campo a `Notification` y NO se toca `notificationRouter.js`** — queda fuera del alcance de F1 (DEC-REF-81 v). Regla huérfana (notificación cuyo `ruleId` ya no existe en ningún pack, caso real posible tras reconciliar packs en F4) ⇒ el ítem se muestra igual, sin subtítulo de tipo y con el `ruleId` crudo como label. **NO se descarta el ítem.** **(vi) Beneficio lateral:** el mismo join deja `recommendation` disponible en el panel, que es el insumo de F3. No se consume en F1. Filas madre DEC-REF-77 / -A / -78 / -78-A / -79 / -A / -B / -B-A / -80 / -81 intactas (append-only). |

> **Nota de implementación (sesión #37) — `window` en `rule_definition.js`:** al construir el evaluador tipo S, confirmar el campo `window` (el recon A–H lo rotuló "para typeC"; probable mislabel — es del tipo S). Verificar contra el código antes de usarlo.

> **Nota de implementación (sesión #39) — `crossExpr` ya presente en `rule_definition.js`:** el recon #39-R1 confirmó que el slot YA existe en el schema: `type` enum incluye `'cross'`; `crossExpr: Mixed, default null`, persistido en las 4 reglas vigentes del pack `cummins-pcc-v1`. El alcance real de A3.2 es el EVALUADOR + su despacho en `ruleEngine.js` (el gate actual por `deviceType`+`variable` en `ruleEngine.js:12-13` saltearía las reglas `cross`) + validación de forma del árbol; NO hay migración de schema. Precisa el "crossExpr 100% libre" del recon #37.

> **Nota de implementación (sesión #39) — validación de A3.2:** la mecánica del evaluador cross (árbol AND/OR, `graceSec`, idempotencia por episodio, resolución por éxito, scoping por site, persistencia `mode:'cross'`) quedó validada E2E por INYECCIÓN DIRECTA al broker con el banco de pruebas caído (BUG-SIM-6). NO constituye el E2E producto de DEC-REF-49, que queda en A3.3 precedido por el recon del simulador mudo. Registrado sin eufemismos por decisión de Franco.

> **Nota (sesión #39, posterior) — corrección:** la frase "banco de pruebas caído (BUG-SIM-6)" de la nota anterior quedó refutada por el recon #39-R2 (falso positivo de medición). La validación por inyección directa de A3.2 y su carácter de no-E2E-producto se sostienen igual; lo que cambia es la causa del pivote: escenario `genset_no_start` no disparable sobre el ATS (BACKLOG-SIM-3) + diagnóstico apurado con taps cortos. Lección de método: un tap de silencio MQTT debe durar ≥ 2-3× la cadencia máxima esperada, con expectativa teórica de mensajes calculada al lado.

> **RISK-SEC-2 (sesión #40) — credenciales de backend expuestas en transcript.** Durante la recuperación del simulador en A3.3 R5 (fallo de metodología: el recon del env del proceso original filtró por grep sin incluir `USER_EMAIL/USER_PASSWORD`), Franco proveyó las credenciales de autenticación del backend (`USER_EMAIL`/`USER_PASSWORD`) por chat para desbloquear el sim. El valor NO se reproduce en este registro (evitar el vector RISK-SEC-1: secreto en git history); referencia: transcript sesión #40, R5. Riesgo: transcript de la sesión contiene la credencial en texto plano. Mitigación: entorno es localhost/dev (mismo perfil que RISK-SEC-1); rotación diferida al mismo trigger de despliegue a producción que RISK-SEC-1. Lección de método (registrada en cierre #40): capturar `/proc/PID/environ` ENTERO antes de killear cualquier proceso, sin grep filtrante. Checklist de rotación producción heredado de RISK-SEC-1.

> **RISK-SEC-4 (sesión #50) — signup público abierto en `/register`.** `pages/register.vue` (endpoint `POST /register` en `users.js`) permite alta pública SIN gate de admin ni token de invitación. Cualquiera con acceso a la URL crea una cuenta en la plataforma. Aceptable hoy porque el entorno es localhost/dev sin exposición externa, pero **inaceptable en dos escenarios inminentes**: (a) demo a Claro (la URL será accesible desde el navegador del cliente durante la sesión de demo — un tercero podría crear cuentas); (b) deployment productivo (superficie de ataque directa, spam de registros, uso de recursos). **Decisión registrada en DEC-REF-70 (d): kill de `register.vue` diferido con trigger propio — antes de la demo a Claro O antes del deployment productivo, lo que llegue primero.** Reemplazo del signup público: alta por admin en la consola superadmin (BACKLOG futuro) o por CLI de operaciones (`scripts/create-user.js` o equivalente — a diseñar en la sesión de ejecución del kill). El route backend `POST /register` **también se cierra en el mismo trigger** (no basta con matar la página — un cliente HTTP podría llamarlo directo). Mitigación provisoria disponible si el trigger llega antes de la ejecución: comentar el `router.post('/register')` en `users.js` (10 minutos, reversible). Checklist de rotación de secretos NO aplica (no hay secreto expuesto — es una superficie funcional).

> **Nota de implementación (sesión #40) — DEC-REF-49 + DEC-REF-50 cumplidos por E2E producto.** A3.3 cerrado con escenario nuevo `mains_failure_gen_no_start` (mínimo: `mains_voltage=0` at 0, `noCleanup:true`; la falla del gen es la ausencia de setter — verificado en R4 que ningún mecanismo autónomo sube `gen_status`). Cadena madre→hija verificada en Mongo: madre `e2e-mains-loss-root` (D, `mains_voltage lt 100`, correlationParent null) disparó a T+530ms; hija `e2e-gen-no-start-child` (cross, AND de `mains_voltage lt 100` y `gen_status neq 'RUNNING'`, graceSec 90) disparó a T+100.1s con **`correlationParent:'e2e-mains-loss-root'` PERSISTIDO** — evidencia principal de DEC-REF-50. Unicidad respetada por cooldown 300s; resolución por éxito comprobada tras `mains_restore`. Commits: `e25fefa` (persist correlationParent) + `621691c` (scenario nuevo). **Detalle a formalizar en A4:** `graceSec` funcional en el evaluador cross pero **fuera del schema** `rule_definition.js`; entró vía `insertOne` directo (mongoose acepta con strict:false o el sub-schema embebido no lo rechaza en `RulePack.find().lean()`). Al armar el RulePack de la cascada productiva, sumar `graceSec: { type: Number, default: null }` al schema — evita depender de la coincidencia actual.

> **Acceso a datos históricos por jerarquía** (nota de alcance ARCH-1, no DEC propia): el histórico crudo vive en el edge (DEC-ARCH-2); el acceso desde NOC/cellowner es por consulta (no suscripción), gobernado por los mismos grants (DEC-REF-29) — el endpoint backend aplica grants server-side antes de responder (filtrado NO delegable al frontend). El mecanismo de traída desde el edge (pull bajo demanda / resúmenes / híbrido) y la vista de históricos son backlogs hijos. ARCH-1 solo garantiza que el modelo de grants contemple el acceso histórico.

> Las DEC-REF-* se agregan a este documento y NO se modifican retroactivamente. Cambios de criterio se registran como nuevas DEC-REF.

### 5a · Backlog de tenancy (BACKLOG-TENANT-*)

> **Nota de alcance:** los BACKLOG-TENANT-1 a -5 viven en `docs/wanomi.md` (registrados como bullets en bitácora durante sesiones #28-#31). **-5 está retirado** (línea-lápida en `wanomi.md:2052`, no se reutiliza). Esta tabla formaliza el registro desde -6 en adelante en el documento maestro; las entradas previas se consultan en la bitácora.

| ID | Fecha | Descripción |
|---|---|---|
| BACKLOG-TENANT-6 | 2026-06-22 | **Trío de ingesta — migración coordinada de `emqxauthrules` + identidad del device/simulador + matching del saver-webhook.** `emqxauthrules` embebe el `userId` en el topic/ACL (DEC-REF-30: `{userId}/{dId}/sdata`) y `saver-webhook:95` valida cruzado `Device.find({dId, userId: data.userId})`. Migrar `device.userId` (hecho en 33.1) sin coordinar los otros dos desincroniza el pipeline MQTT→webhook→Data: el `find` devuelve 0 y la muestra **se pierde en silencio**. HOY offline (sin ingest, simulador apagado) → safe. **Disparador: paso 4** (adaptación del simulador, UI-1/UI-2). **Invariante:** el día que algo publique, los tres deben llevar el `userId` nuevo o la ingesta cae muda. Incluye la decisión de matching: relajar a dId-solo (requiere confirmar unicidad GLOBAL de `dId` primero) vs. reprovisionar credenciales (toca BACKLOG-API-1). La bandera de `simulator.js` (filtra por `userId`; corriendo como Franco no vería los devices migrados a `operator-claro`) es parte de este mismo trío. **Actualización #35:** la nota de #34b que lo revisó "a la baja" fue errónea (corregida en bitácora). El gap es REAL y bloqueante: `emqxauthrules` con `userId` viejo + modelo de suscripción del frontend (escucha `userId` propio, no el del dueño del dato). Se ataca en #35. **Cierre #35:** las 3 piezas resueltas — pieza 1 (ACL device) auto-resuelta por self-heal de `getDeviceMqttCredentials` (10 ACL creadas bajo SERVICE en bootstrap, confirmado E2E); pieza 2 (ACL web-user B-narrow) y pieza 3 (suscripción frontend) implementadas (DEC-REF-38, commits `7111235`/`edc7bd1`). Dato vivo validado E2E (cellowner recibe delta MQTT del namespace SERVICE). La 4ta pieza (saver rules → persistencia) se desglosó como BACKLOG-TENANT-9. |
| BACKLOG-TENANT-7 | 2026-06-22 | **Fuga de tenancy en notificaciones del dashboard. ABIERTO — agujero de autorización.** La cuenta personal (sin grant; 0 sites / 0 devices tras TENANT-4) SÍ ve notificaciones de Claro **con contenido** en el select del dashboard. El endpoint que alimenta ese select NO gatea por grant — ve por el `userId` viejo que quedó pegado en las 24 `notifications` (no migradas en 33.1 a propósito: su lectura debía estar gateada por Site, DEC-REF-33). El Catch 1 de #33 auditó `GET /notifications` (`webhooks.js:190/376/387` → `buildReadFilter`, SAFE) pero el select del dashboard pega a OTRO endpoint que se escapó a esa auditoría. Detectado por el E2E visual de #33. Próximo a reconnear: identificar el endpoint exacto, por qué no gatea, y **barrer si hay otros endpoints en el mismo estado**. Severidad acotada en dev (localhost, sin cliente real conectado). Concern separado de 33.1 (que cerró validada). |
| BACKLOG-TENANT-8 | 2026-06-23 | **Dueño de datos de los templates = catálogo Wanomi (decisión de PRODUCTO, no de código).** Los templates son catálogo de Wanomi por TIPO DE EQUIPO (DEC-REF-15; `refactor_implementacion:38`: `tpl-cummins-pcc`, etc.), NO datos de un operador ni de un sitio. Hoy los 4 templates tienen `userId` = cuenta personal de Franco (herencia); TENANT-4 NO los migró (correcto: un template Cummins no es de Claro). Pendiente: definir a qué cuenta pertenecen como catálogo (¿`admin@wanomi.com`? ¿cuenta `catalog@wanomi` dedicada?) y si se modela un concepto de "catálogo" separado de ownership por `userId`. Independiente del fix de DEC-REF-37 (que cierra la fuga sacando ownership de la lectura, sin importar el dueño). Disparador: herramienta interna de alta de templates (Área 1/4) o 2º operador. NO bloquea el MVP. |
| BACKLOG-TENANT-9 | 2026-06-24 | **Saver rules de EMQX pegadas al userId viejo → persistencia rota post-TENANT-4.** Descubierto en el E2E de TENANT-6 (#35). Las 10 saver rules (`db.saverrules`, todas `userId=PERSONAL`) crearon reglas en el EMQX Rule Engine con SQL sobre `PERSONAL/{dId}/+/sdata` (cuando los devices vivían bajo PERSONAL, pre-TENANT-4). El simulador ahora publica a `SERVICE/{dId}/var/sdata` → la rule no matchea el topic → no dispara el saver-webhook → las muestras NO se persisten en `data`. Es la cuarta pieza del "trío" de ingesta de BACKLOG-TENANT-6 (su línea original ya mencionaba "matching del saver-webhook" como parte del problema; se desglosa recién ahora al publicar por primera vez post-migración). **NO afecta el dato vivo** (el cellowner recibe el delta MQTT directo del broker, sin pasar por las saver rules — validado E2E #35). **SÍ afecta:** persistencia en `data`, históricos (`/get-last-data`, `/get-small-charts-data` devolverían vacío), charts del dashboard que dependen del histórico. Patrón de fix: recrear las 10 saver rules con `userId=SERVICE` (`createSaverRule(SERVICE, dId, true)` por dId, ya existe en `POST /device`) + eliminar las 10 viejas de EMQX. Disparador: cuando se necesite persistencia/históricos del dato Claro (sub-paso de la vista de site que use históricos, o BACKLOG-UI-5). NO bloquea el dato vivo ni el sub-paso 2 de `_siteCode.vue`. |

> **BACKLOG-TENANT-7 — RESUELTO en #34** (DEC-REF-37, commit `00b397a`). La causa raíz no fue un endpoint olvidado sino la rama ownership del helper aplicada a derivados sin tenencia propia. Fix de raíz (opción 2 completa): Notification + Template + ForensicEvent a DENY.

> **BACKLOG-TENANT-3 — absorbido en bloque #43 (DEC-REF-54)**. Registrado como referencia dentro de DEC-REF-33/46; sin fila propia. Cierre efectivo en A6: `buildWriteFilter` genérico + gates activos en writes de Site/Device/Notification.

### 5b · Backlog de hardening de API (BACKLOG-API-*)

| ID | Fecha | Descripción |
|---|---|---|
| BACKLOG-API-1 | 2026-06-19 | **Hardening de los enrichments de `GET /device`.** Deferido de 31.3 (#31) por single-concern. (1) **Retornos de error inconsistentes:** los 4 helpers devuelven distinto en `catch` — `getSaverRules`→`false`, `getTemplates`→`false`, `getAlarmRules`→`"error"` (string), `getRules`→`[]`. El `forEach` del handler hace `.filter()` sobre el retorno → `false.filter()`/`"error".filter()` lanzan TypeError → 500. Latente (happy path siempre devuelve array). Fix: normalizar los 4 a `[]` en error. (2) **`==` flojo en el template join** (`devices.js:59-61`): `template._id` (ObjectId) `==` `device.templateId` (string hex) funciona por coerción accidental. Fix: conversión explícita (`ObjectId.isValid` + comparar como ObjectId, igual que la rama 'Template' de scope.js) + `===` en los 3 joins por `dId`. |

> **BACKLOG-API-1 — absorbido en bloque #43 (DEC-REF-54)**. Se atiende en A9 junto a OPS-1/OPS-2 como pull operacional único.

### 5c · Backlog de UI/UX (BACKLOG-UI-*)

| ID | Fecha | Descripción |
|---|---|---|
| BACKLOG-UI-4 | 2026-06-21 | **Link de navegación a `/history` en el sidebar.** Hoy se accede por URL directa (MVP). El sidebar/menú principal del cellowner queda como sub-paso aparte cuando se defina el layout completo. Cambio chico (1 entry en `SideBar.vue` o equivalente). Disparador: definición del menú principal del cellowner. |
| BACKLOG-UI-5 | 2026-06-21 | **Vista de análisis / export de largo plazo (90d+).** Ventanas largas (90d, 180d, 1y), MTBF, degradación multi-mes. Uso distinto del histórico operativo (que vive en `pages/history.vue` con presets 1h-30d): Confiabilidad (Área 1) pide export (CSV/PDF), no preset adicional en un chart de línea simple. Mezclarlo con el histórico operativo viola la separación de concerns y degrada la UX del cellowner. Disparador: cuando exista rol de confiabilidad consumiendo datos. |
| BACKLOG-UI-6 | 2026-06-24 | **Dashboard (`pages/dashboard.vue`, vista admin de DEC-DASH-1) no renderiza la grilla de widgets del device seleccionado.** Síntoma observado (#35, E2E visual): el área de widgets queda vacía y el select de devices solo muestra el device preestablecido (CR00061-ATS), no permite elegir otros, pese a que el backend devuelve los datos correctos al cellowner (verificado #34b: `GET /template` 4 con widgets, `GET /device` 10 con template embebido, ambos 200 con token cell). NO se cura con rebuild del bundle (descartado bundle viejo). NO es regresión de TENANT-7/DEC-REF-37 ni de la vista de site (sub-paso 1). **Segundo síntoma, posiblemente la misma causa (a confirmar, NO asumido):** en consola del navegador aparecen `SyntaxError: Unexpected token '<' ... is not valid JSON` — un fetch del frontend recibe HTML (`<!doctype...`, típicamente una página de error) donde espera JSON. Hipótesis a diagnosticar: el endpoint que poblaría el dashboard (¿el del select de devices? ¿el de templates?) devuelve HTML de error → el parse falla → el dashboard no puebla. Verificar si el SyntaxError y la grilla vacía comparten causa o son concerns separados. Distinto de BACKLOG-TENANT-9 (ese explica charts dependientes del histórico vacíos por saver rules; este es la grilla de widgets que no renderiza, otra capa). Distinto de BACKLOG-TENANT-7 (fuga de tenancy en el select de notificaciones, no la grilla de widgets). NO bloquea el dato vivo (validado #35 por consola, llega al navegador) ni la vista de site. Disparador: cuando se priorice el dashboard, o si bloquea la demo a Claro. |
| BACKLOG-UI-7 | 2026-06-25 | **Siembra instantánea del valor vivo al montar la vista de site.** `LiveValue.vue` (sub-paso 2, #36) sólo muestra deltas MQTT en vivo, sin valor inicial → cada variable aparece recién en su próximo publish (`variableSendFreq`: 30-60s). Al abrir `/sites/:siteCode` el operador ve spinners de espera hasta 60s antes de que las cards se pueblen. Correcto como modelo (el spinner cumple su función), pero feo para demo. Fix: en `mounted` de `LiveValue`, pedir el último valor conocido (patrón de `Rtnumberchart.getChartData` → `/get-last-data` por `dId`+`variable`) y pintarlo de entrada; el MQTT en vivo lo actualiza. **Depende de BACKLOG-TENANT-9** (sin saver rules bajo SERVICE, `data` está vacía y `/get-last-data` devolvería nada). Disparador: prioritario para la demo a Claro (cards pobladas al instante). Sube de hecho la prioridad de TENANT-9. |
| BACKLOG-UI-8 | 2026-07-16 | **Pasada de consistencia visual — consola de Reglas de monitoreo (#47).** La página de Reglas no quedó alineada visualmente con el resto de la app. Alcance: auditar tablas, íconos, widgets y espaciado contra el patrón de Dashboard/Devices/Alarms/Templates (referencia: `app/components`, `plugins/globalComponents.js`, `app/.nuxt`) y unificar componentes CSS compartidos. NO es hotfix — momento de diseño propio, encaja con UI-polish previo a UI-3. ID: se usa UI-8 porque UI-1/2/3 viven en bitácora histórica (#25-#32) y el corpus formaliza desde UI-4. Disparador: sesión visual dedicada que abra Franco. Voces: Camila (UX) + Lucía (Frontend Vue). |
| BACKLOG-UI-10 | 2026-07-20 | **Rediseño de `/demo/simulator` como consola de demo (mandato Franco #50).** La página existente (`pages/demo/simulator.vue`, 202 líneas, master-detail con `DeviceList` + `DevicePanel`) sirve para control operativo del sim en dev, pero NO tiene diseño de consola de demo: falta narrativa (para qué se abre en una demo Claro), agrupación por site/rol operativo, story-mode (secuencia de eventos que muestren el motor edge en vivo), y visualización de estado del sim (qué está publicando, qué está escuchando el edge). En #50 la página **solo gana link de sidebar `v-if="isSuperadmin"`** sobre la implementación existente (DEC-REF-70, punto (a)) — el rediseño de consola queda para sesión dedicada. Voces: Camila (UX) + Lucía (Frontend) + Franco (narrativa de demo). Disparador: cuando exista fecha firme de demo Claro con audiencia definida, o cuando Franco quiera armar la sesión de branding/marketing con Área 4 (referencia identidad visual azul de DEC-REF-70 punto (g)). |
| BACKLOG-UI-12 | 2026-07-23 | **Higiene de unidad en dos consumidores cosméticos post-C4 (#52).** Post-C4 dos rutas de UI muestran el label del widget sin el sufijo `(<unit>)`, sin pérdida funcional pero con lectura menos completa: **(a) `app/components/Widgets/Rtnumberchart.vue:12`** — subtítulo del card (`{{ config.selectedDevice.name }} - {{ config.variableFullName }}`) queda como *"Home - Temperatura shelter"*; la unidad **sigue visible en la serie y el eje del propio chart** (L134/L170: `config.variableFullName + " " + config.unit`), es información redundante en el card. Fix simple: concatenar `config.unit` en L12 cuando exista, mismo patrón que HistoryChart. **(b) `app/pages/templates.vue:337`** — columna "Variable" de la tabla de widgets guardados (`<el-table-column prop="variableFullName" label="Variable" />`) muestra el label crudo sin unit al lado. Es el editor, no la vista de operador. **C5 dará a `unit` campo propio en el mini-form** de `valueStatus` y permitirá mostrar `unit` como columna aparte en la tabla, lo que haría descartable cualquier fix hecho ahora sobre `variableFullName`. Recomendación: esperar a C5 para (b); (a) se puede resolver en cualquier sesión de UI polish. Disparador: sesión visual dedicada, o al cerrar C5 si el patrón de la tabla se resuelve ahí. Voces: Camila (UX) + Lucía (Frontend). |

> **BACKLOG-UI-4 — CERRADO en #50 (DEC-REF-70 punto (a), commit `6c2a0e6`).** El link `Histórico` a `/history` está en el sidebar operador (`layouts/default.vue`), orden fijo Panel · Sitios · Histórico · Devices · Templates · Reglas de monitoreo · Simulador. Verificado por smoke del Paso 7 (`/history` HTTP 200 accesible por sidebar). Fila conservada como precedente inmutable (convención de la sesión #43 / DEC-REF-54).

> **BACKLOG-UI-11 — branding visual del template pendiente (post-cierre #50, Franco).** Franco cerró #50 con `chore(branding)` `76f1d75` cambiando `nuxt.config.js` `head.title` de `"IoTiX"` a `"wanomi"` (verificado en el bundle post-restart: `<title>wanomi</title>`). Quedan pendientes 3 puntos de branding del template Now-UI-Dashboard que arrastran identidad IoTiX/Creative Tim: **(a) favicon** en `app/static/favicon.ico` + `favicon.png` + `icon.png` (hoy con branding heredado); **(b) sidebar brand** en `layouts/default.vue:7-8` — `short-title="IX"` + `title="IoTix"` (visible en la esquina superior izquierda del sidebar cuando está expandido y colapsado); **(c) assets restantes** — revisión de otros hardcodes visuales del template (imágenes en `static/img/`, avatares del navbar como `img/mike.jpg` en `DashboardNavbar.vue:88`, splash de login/register si aplica). Coherente con la identidad visual azul de DEC-REF-70 (g) — el logo debe cerrar el conjunto sidebar-azul + título wanomi + favicon wanomi. Voces: Franco (dirección de identidad) + Área 4 (branding, ver referencia identidad en DEC-REF-70 g). Disparador: sesión visual dedicada, o previa a demo Claro / material de presentación.

### 5d · Backlog del simulador (BACKLOG-SIM-*)

> Nota: **BACKLOG-SIM-1** (race condition `createSaverRule`, sesiones #14-#15) **CERRADO** con DEC-REF-17. ID no se reutiliza (convención del repo, precedente: TENANT-5 retirado).

| ID | Fecha | Descripción |
|---|---|---|
| BACKLOG-SIM-2 | 2026-06-23 | **Re-seedeo del simulador roto post multi-tenant (TENANT-4 + DEC-REF-37).** `tools/device_simulator/seed.js` está roto en dos niveles: (1) **por DENY** — corre como `fsugamiele@gmail.com` (sin grants); las lecturas de pre-existencia (`getTemplates`/`getSites`/`getDevices`) ven 0 → no detecta el piloto ya cargado → recrearía duplicados; (2) **por destino** — crea assets con `userId = caller`, pero post-TENANT-4 el piloto vive bajo `operator-claro` (service account, DEC-REF-36). Ninguna cuenta existente lo arregla: `operator-claro` no tiene login (DEC-REF-36, password random descartada), `admin@wanomi.com` puede loguear y leer pero crea bajo `userId=admin` no SERVICE. El seed asume el modelo pre-tenancy "yo creo, yo soy dueño", incompatible con el multi-tenant actual donde el dueño es la cuenta de servicio. **NO bloquea el piloto actual** (`devices_state.json` cacheado, `run.js` funciona vía `getdevicecredentials` que resuelve `userId` fresco desde el device — recon O1 de #34b). **Bloquea:** re-seedeo desde cero + alta de nuevos sites/devices por el simulador. **Requiere diseño:** operación admin-only que asigne `userId` arbitrario al crear (crear-a-nombre-de la service account), o un mecanismo de identidad para la service account distinto del login normal. NO urgente. Disparador: cuando se necesite re-seedear o dar de alta sitios nuevos por simulador. |
| BACKLOG-SIM-3 | 2026-06-28 | **Extender escenarios del simulador para disparar umbrales en el E2E de la cascada.** `genset_no_start` (corte AC + cranks fallidos) es la base de la cascada; `fuel_siphon` se queda en 30% y no cruza G2<15 (`fuel_level_pct lt 15`). Tunear escenarios para que crucen umbrales reales del RulePack vigente (`cummins-pcc-v1`: A1 `oil_pressure_psi lt 15`, G2 `fuel_level_pct lt 15`). Disparador: paso A8 (E2E de cascada). **Actualización #39:** `genset_no_start` NO es disparable sobre el ATS (aborta por variables ajenas al device — verificado E2E A3.2); el escenario coordinado de cascada (DEC-REF-49) debe diseñarse contemplando qué device recibe el trigger. |
| BUG-SIM-1 | 2026-06-29 | **`sharedState` no compartido entre devices del mismo site (defecto real, verificado #37).** `tools/device_simulator/run.js:60-80` construye los devices en lista plana sin agrupar por site y NO pasa `sharedState` al constructor → cada `SimulatedDevice` crea el suyo vacío (`device.js:39`). Efecto: el ATS escribe `gen_running` en su instancia, el Cummins lo lee de la suya (siempre `undefined`) → la coordinación ATS↔Cummins nunca ocurre, y el Cummins simulado queda roto de fondo (`oil_pressure` 0, `rpm`→0, `run_hours` sin incremento, `fuel_level` sin consumo). No hay otra vía de coordinación (grep confirmó: sin singleton global, sin MQTT cross-device, sin archivo compartido en runtime). Fix: agrupar por `siteCode`, crear estado compartido por site (`Map<siteCode, {}>`), pasarlo a cada device del site. Se arregla como parte de A3 (DEC-REF-49). Verificado read-only #37 (`run.js`, `device.js`, `sensor-engine.js`). |
| BUG-SIM-4 | 2026-06-29 | **Restauración de state itera la pizarra cruda en vez de las variables del template — un bug, dos sitios (verificado #38).** Raíz: el código que restaura estado del device itera `Object.keys(this._state)` (incluye metadata interna como `deviceType`) en vez de iterar `this._variables` (lista del template, frontera natural real-vs-metadata; `deviceType` confirmado ausente del template ATS/CUMMINS en recon #38). **Manifestación (a) — cleanup post-scenario** (sesión #38 original): `tools/device_simulator/lib/device.js:251-262` usa un ternario que solo conoce 2 roles — `this._role === 'SEC' ? initialSecState() : initialGenState()`. Para ATS y CUMMINS cae por defecto en `initialGenState()`, que NO contiene sus variables (ATS: `transfer_state`, `gen_status`, `gen_voltage`, `gen_freq`, `load_kw`, `mains_freq`, `deviceType`; CUMMINS: `rpm`, `oil_pressure`, etc.). Cadena del daño: cleanup itera `Object.keys(this._state)` → para cada var no presente en `initialGenState`, `initial[varName]===undefined` → `_set(var, undefined)` asigna `undefined` al state → `_publish` skip ("invalid value") → de ahí en más `_tick` ve `state[var]===undefined` y retorna temprano (device mudo permanentemente). Efecto: el 1er scenario corre OK; tras su cleanup el ATS y el CUMMINS quedan mudos; scenarios 2+ abortan ("variables not in device"). Síntoma en browser: spinners eternos en las vars del ATS/CUMMINS post-1er scenario. ROMPE el banco de pruebas de cascada: solo se puede correr la cascada UNA vez sin relanzar el simulador → bloquea la validación repetible de A3 (DEC-REF-49). **Manifestación (b) — comando `reset`, LATENTE** (recon #38 ampliado): `device.js:181-190` (rama `command === 'reset'` de `applyCommand`) reasigna `this._state = this._initialState(this._role)` y luego itera `Object.keys(this._state)` llamando `_publish` para cada key — sobre ATS/CUMMINS publicaría `deviceType='ATS'`/`'CUMMINS'` a MQTT (`_publish` no filtra por template; el guard "value undefined/null" no atrapa porque value es string real). NO se ha manifestado en producción porque NO hay UI ni trigger automático para `reset`; queda latente esperando el primer caller. Misma raíz que (a). **Fix de raíz (ambos sitios):** reemplazar `Object.keys(this._state)` por iterar `this._variables` (la lista del template, ya cableada en el constructor y usada por `startPublishing`), y usar `this._initialState(this._role)` por rol (cubre los 4: `device.js:46-53`). Así `deviceType` queda intacto por construcción, no por la coincidencia de la comparación de igualdad. NO refuta A3.1 (BUG-SIM-1, fix en commit `ce8a895`): la coordinación `sharedState` quedó probada (Cummins despertó post-RUNNING). El fix del `reset` es un ENSANCHAMIENTO respecto del registro original (que era solo cleanup), incorporado por ser la misma raíz — decisión #38. Verificado read-only #38 (`device.js`, `sensor-engine.js`, templates ATS/CUMMINS en Mongo). Fix = Commit 2 de A3.1 (aplicado en `cf5f6c7`). |
| BUG-SIM-5 | 2026-06-29 | **Timers de publicación no se reinician tras un scenario — device congelado (verificado #38).** Raíz: `_runScenario` (`tools/device_simulator/lib/device.js:211+`) llama `_cancelActiveTimers()` al inicio (`device.js:206-208`), que mata los `setInterval` de `startPublishing` (pusheados a `this._timers`). NINGUNA de las dos ramas de fin de scenario los reinicia: **(a) rama cleanup** (`device.js:251-261`) restaura state pero NO llama `startPublishing`; **(b) rama noCleanup** (`device.js:262-267`) solo imprime un log. Efecto: tras cualquier scenario, el device deja de publicar periódicamente — sus LiveValue quedan congelados en el último valor publicado por los steps o el cleanup. Afecta a TODO device que corra un scenario (ATS y CUMMINS por igual). Se manifestó en la validación de BUG-SIM-4 (doble corrida de cascada): el ATS quedó silencioso entre `t=73.5s` (cleanup #1) y `t=121s` (trigger #2) — se interpretó como "OK", era este bug. Estaba **ENMASCARADO por BUG-SIM-4**: antes del fix, el ATS era mudo por `state=undefined`; con BUG-SIM-4 fixeado (commit `cf5f6c7`) quedó expuesto que además es mudo por timers muertos. El comando `reset` (`device.js:181-190`) NO sufre esto porque llama `this.startPublishing()` al final — el fix replica ese patrón. **Fix:** llamar `this.startPublishing()` **dentro del callback del `setTimeout` final** de AMBAS ramas (cleanup y noCleanup), tras restaurar/preservar state. +2 líneas. NO duplica timers: `_cancelActiveTimers` ya corrió al inicio del scenario y los `setInterval` viejos no tienen refs vivas; los nuevos se pushean a un `_timers` que solo contiene refs a Timeouts ya expirados (leak trivial, preexistente, no regresión). Verificado read-only #38. **NOTA — distinto de fidelidad física del ATS:** el recon #38 también identificó que `mains_freq` (sigue oscilando 50Hz durante corte, sin condicional a red apagada) y `load_kw` (jitter aleatorio sin responder a contexto de corte/gen/carga real del site) son **placeholders**, no física. Ese es trabajo separado de Área 1 (modelo físico de carga telco y frecuencia en corte) → registrable como backlog de fidelidad, NO en BUG-SIM-5. BUG-SIM-5 es solo el congelamiento de ticks, no la calidad del evolve. |
| BUG-SIM-6 | 2026-07-03 | **Simulador conectado pero mudo — no publica sdata periódicos (detectado #39, causa SIN diagnosticar).** `run.js` (PID vivo desde Jul01) figura conectado a EMQX pero no emite telemetría periódica; tap de `+/+/+/sdata` durante 5s = vacío. El canal de CONTROL está vivo: comandos `set_sensor` recibidos y procesados (log del sim, cleanup #39). Publish muerto, control vivo — recorta hipótesis (¿timers muertos post-scenarios de la corrida larga? ¿ACL? ¿estado interno?). NO especular: requiere recon dirigido read-only. BLOQUEA el E2E producto de A3.3 (DEC-REF-49) y es demo-relevante (LiveValues congelados). Disparador: apertura de A3.3. **RETIRADO #39 (mismo día) — FALSO POSITIVO.** Recon #39-R2: tap pasivo de 150s = 321 msgs sdata, 10/10 devices publicando (CR00061: ATS 25, GEN 27, CUMMINS 28, SEC 40); sesiones EMQX continuas desde 2026-07-01T17:34Z sin reconexión ni ACL deny; log del proceso limpio. La evidencia original (taps de 3-5s) era insuficiente frente a cadencias de 30-120s por variable. A3.3 NO está bloqueado. ID no se reutiliza (precedente TENANT-5). Siguen vigentes e independientes: lección `set_sensor` y actualización #39 de BACKLOG-SIM-3. |
| BACKLOG-SIM-4 | 2026-07-04 | **Modelar dominio real de `transfer_state` del ATS.** Recon #40-R4: el dominio actual observable es singleton `{'AUTO'}` — `sensor-engine.js:44` (initial), `:307` (paso redundante en `mains_failure_ats_transfer` que asigna el mismo 'AUTO'), sin evolve dinámico. El sim no modela los estados intermedios reales del InteliATS PWR (transferencia en curso, timeout de arranque, etc.). Consecuencia inmediata: el escenario `mains_failure_gen_no_start` (A3.3) no puede cambiar `transfer_state` de forma semánticamente correcta y quedó minimalista (solo `mains_voltage=0`). Falta también reposición de `transfer_state` en `mains_restore` si el dominio se abre. **Requiere criterio de Área 3** sobre el dominio real del PLC. Disparador: cuando A4 o la demo Claro requieran diferenciar visualmente estados del ATS más allá del `mains_voltage`. |
| BACKLOG-SIM-5 | 2026-07-28 | **Dos listas de escenarios que divergen — bug de arquitectura recurrente.** El simulador (`tools/device_simulator/lib/sensor-engine.js:254`, constante `SCENARIOS`) y el backend (`app/api/routes/simulator.js:30`, constante `VALID_SCENARIOS`) mantienen **dos listas hardcodeadas separadas** del catálogo de escenarios. **Ha mordido dos veces documentadas:** (i) en **#7**, escenarios nuevos del sim fueron rechazados por la API porque la whitelist del backend no los conocía; (ii) en **#53/Fase 3 (2026-07-27/28)**, 7 de 14 escenarios del sim quedaron fuera de la whitelist del backend (`mains_failure_ats_transfer`, `mains_failure_gen_no_start`, `mains_restore`, `cummins_setpoint_lost`, `cummins_setpoint_restore`, `eltek_load_high`, `eltek_load_restore`), imposibilitando la consola del paréntesis para dispararlos vía UI hasta que se sincronizara manualmente. **La solución real es UNA sola fuente de verdad:** (a) el backend deriva la lista del propio módulo del simulador via import compartido, o (b) un archivo compartido (JSON/JS) que ambos consumen, o (c) el backend consulta el sim vía MQTT al arrancar y cachea. **NO entra en el paréntesis pre-reunión Claro** — la sincronización manual del #53/Fase 3 (17 escenarios en ambas listas) cubre la demo. **Se registra para que no vuelva una tercera vez** (patrón recurrente = deuda arquitectónica, no accidente). **Disparador:** próxima aparición de escenarios nuevos (nueva ronda de features del sim). Familia con BACKLOG-SIM-* (deuda de contrato del simulador con el resto del stack). |

### 5e · Backlog de reglas (BACKLOG-RULE-*)

> Nota: **BACKLOG-RULE-1** vive en bitácora `docs/wanomi.md:1654` (pack productivo `cummins-pcc-v1` 100% tipo D, features tipo C sin cablear a ninguna regla viva). Esta tabla formaliza el registro desde -2 en adelante en el documento maestro.

| ID | Fecha | Descripción |
|---|---|---|
| BACKLOG-RULE-2 | 2026-07-02 | **Catálogo de RulePacks sin vínculo al árbol de tenancy.** Recon #39-R1: `rule_pack.js` existe y está sano (`packId`, `deviceType`, `version`, `canary`, `rules` embebidas; colección `rulepacks`), pero sin `operatorId` ni `overrides` por Zone — el pack se ata a `deviceType`, no cuelga de un operador. DEC-REF-31 ("schema del catálogo + pack colgado de Claro") queda cumplida a medias: schema sí, jerarquía no. Con 1 operador el catálogo global es indistinguible del de Claro (no-hardening especulativo aplica). Disparadores: 2º operador, pull de packs (BACKLOG-ARCH-2), consola multi-operador. |
| BACKLOG-RULE-3 | 2026-07-04 | **Mismatch nomenclatura pack `cummins-pcc-v1` vs template `WN-GEN-Cummins-PowerCommand`.** Recon #40-R1 C8: las reglas del pack productivo referencian `oil_pressure_psi` (A1) y `fuel_level_pct` (G2), pero el template publica `oil_pressure` y `fuel_level` (sin sufijos). Consecuencia: las reglas A1/G2 NO disparan sobre datos reales del sim — solo con inyección manual (así funcionó A3.2 con `oil_pressure_psi=0` fabricado). Decisión pendiente: (a) bug de seed → renombrar la referencia en el pack para alinearla con el template; (b) nombres de driver Modbus futuro → dejar el pack como está y agregar mapping en el bridge Modbus; (c) reglas duales durante transición. Bloquea validación E2E de reglas D productivas contra dato del sim; NO bloquea A3.3 (que usó reglas propias). Disparador: al armar A4 (RulePack cascada) o al reconciliar sim ↔ pack productivo. **RESUELTO #42 por DEC-REF-53** — opción (a) para clase connect (A1/G2 rename), + retiro del pack para clase inferred (D1/D2, diseño preservado en BACKLOG-RULE-4). Recon #42-R1 refinó el problema de 2 mismatches a 4 en 2 clases (connect: rename simple; inferred: variable derivada aún no publicada). Entrada conservada por precedente (los backlog se cierran con referencia, no se borran). |
| BACKLOG-RULE-4 | 2026-07-06 | **Reglas de mantenimiento preventivo por horas de uso (service 250 h).** Nace del retiro de D1/D2 del pack productivo por DEC-REF-53 (clase inferred de RULE-3). Diseño preservado: reglas D sobre variable derivada `hours_to_next_service_250` (base biblioteca de campo Cummins/ComAp: 250 h aceite / 500 h filtro), calculada a partir de `run_hours` (registro ComAp 40XXX / Cummins Run Hours). D1 warning `<=20h restantes`, D2 critical `<0h` (vencido). Disparador: **BACKLOG-EDGE-1** (driver Modbus real que exponga `run_hours` + derivación en edge) **O** decisión explícita de publicar `run_hours` en el sim para valor demo (alternativa D2 de sala #42 descartada por scope, recuperable acá). NO bloquea A4 ni MVP — es hardening de valor de mantenimiento predictivo, se activa cuando exista la fuente real de horas de operación. |
| BACKLOG-RULE-5 | 2026-07-06 | **Migrar regla madre `cummins-M1-mains-loss` a pack ATS propio.** Nace de DEC-REF-53 D4 opción (b): la regla D `cummins-M1-mains-loss` (dominio ATS, `deviceType:'ATS'`, `variable:'mains_voltage'`) vive transitoriamente en el pack `cummins-pcc-v1` porque la vista de cascada (DEC-REF-41/50) requería una madre para el `correlationParent` de C1, y no existía pack ATS. Funciona correctamente en runtime — el motor gate por `rule.deviceType` no por `pack.deviceType` (ruleEngine.js:29-30, verificado #42-R4 B4) — pero semánticamente pertenece a un pack `ats-inteliats-pwr-v1` o similar. Disparador: creación del pack ATS productivo (segundo equipo ATS con reglas propias, o refactor cuando A5-A8 aterrice el modelo de packs por deviceType). NO urgente: el pack cummins funciona con M1 embebida sin efectos secundarios. |
| BACKLOG-RULE-6 | 2026-07-07 | **Tenancy full de RulePack.** Hereda la porción diferida de BACKLOG-RULE-2. Trigger: firma del 2º operador. Alcance conocido: agregar `operatorId` al schema, migración marcando packs existentes como Claro, extensión de la rama `'RulePack'` en scope, diseño de overrides por Zone contra requisitos reales del cliente. Estimación ~1 sesión. Ver DEC-REF-60. |
| BACKLOG-RULE-7 | 2026-07-22 | **Escala numérica de umbrales vs. escala de telemetría del driver (apertura #52; ID renumerado desde RULE-5 propuesto por la sala — RULE-5 ya ocupado por la migración M1→pack ATS, DEC-REF-53-A).** El motor compara sin unidad (`ruleEngine` → `typeD.js`): el umbral crítico de `cummins-A1` está redactado en **Bar** (`< 1.0`, DEC-REF-53/D1) mientras el sim publica **psi** (rango operativo 35–55, `sensor-engine.js:130-132`) y el HMI211 real de CR00061 reporta **PSI** (evidencia de campo #15). Con telemetría en psi la alarma no dispara nunca; hoy dispara solo porque el sim publica `0` permanente en reposo. **NO es problema de etiqueta** — la unidad de presentación se resuelve en la preconfiguración del widget (decisión Franco #52); esto es **escala numérica** y ningún campo de UI la convierte. Observado sin ID en bitácora #4x (*"observación limpia"*, declarado fuera de alcance de A5-A7). **Alcance del fix:** definir dónde ocurre la conversión — driver Modbus (normalización en el Hub, coherente con el blueprint §2: escalas ×0.1, °F→°C, splits 32-bit) **o** umbrales redactados en la unidad que emite el driver. **Disparador:** definición del driver Modbus real (Área 3, CORE Rev B congelado). **NO bloquea #52** — #52 no toca reglas. |

> **BACKLOG-RULE-2 — absorbido en bloque #43 (DEC-REF-54)**. Se atiende en A8 (consola superadmin): catálogo↔tenancy (`operatorId` + overrides por Zone) queda cableado como parte del CRUD HTTP de RulePack.

### 5f · Backlog de operaciones (BACKLOG-OPS-*)

| ID | Fecha | Descripción |
|---|---|---|
| BACKLOG-OPS-1 | 2026-07-05 | **Durabilidad de reglas/config EMQX ante ciclo de vida del contenedor.** Recon #41-R2 evidenció que las reglas del EMQX Rule Engine NO sobrevivieron (`GET /api/v4/rules` = `data:[]`; 10 `emqxRuleId` de Mongo → 404); los resources sí (3 web_hook vivos). Causa probable: volumen EMQX sin persistencia de la sección rules del engine, o purga por versión/restart del contenedor. `reconcileSaverRules` mitiga el caso saver rules al arrancar el backend (DEC-REF-52), pero verificar: (a) qué persiste realmente el volumen EMQX en `docker_compose_production.yml`; (b) si otros objetos (`resources`, ACLs de `emqxauthrules`, reglas `alarm/actuator` — con sus propios reconcile en `emqxapi.js:329,389`) tienen el mismo riesgo o ya están cubiertos. Disparador: checklist de deployment a producción (junto a RISK-SEC-1/2). NO urgente: el self-heal cubre el gap operativo en dev y en el bootstrap real. **Adenda #41-R5:** la cadena de fallo completa se descubrió en R4. La pérdida de rules ~06-01 no rompió nada en el momento porque el reconcile del bootstrap iba a repararlas; pero el resource fue recreado en algún reboot posterior con `WEBHOOKS_HOST=localhost` (deriva `.env` vs. CLAUDE.md, ver BACKLOG-OPS-2), quedó `is_alive:false`, `waitForSaverResource` timeout → `reconcileRules` nunca corrió → rules quedaron en 0 semi-permanente. El self-heal existe pero es sensible a la config del resource — sumar al checklist: probar el pipeline completo `restart-node → resources vivos → rules creadas` como paso de aceptación de deployment. **Adenda #48/R1:** 3ª recurrencia observada — corte de persistencia `db.data` desde 2026-07-15T11:50Z, coincide con el cold-start del host WSL2 al inicio de #45. **Diagnóstico corregido por sala #48 (corpus-first):** NO es BACKLOG-TENANT-9 (cerrado en #41-R5) — `db.emqxsaverrules` es colección legacy no usada; `reconcileSaverRules` lee `db.saverrules`. Es el mismo patrón DEC-REF-52-A (rules perdidas + resources potencialmente con URL inalcanzable → self-heal bloqueado). **Episodio 2:** ~44h de gap descubierto ~2026-07-12, fix por el mismo procedimiento del episodio 1 (DELETE resources muertos + `docker restart node` → reconcile). **Episodio 3 (actual):** corte 2026-07-15T11:50Z sin fix aún; #48 lo encadena al rebuild del bundle PROD post-R23 en un solo restart. **Patrón firme: cada cold-start del host WSL2 mata la persistencia en silencio.** **Sube prioridad del hardening del volumen EMQX** de cara al Hub de campo (Orange Pi Zero 3 con power-cycle real, DEC-REF-68): en producción no habrá un dev disponible para el DELETE resources + restart cada vez que el sitio corte energía. Candidatos de hardening para la sesión visual dedicada: (i) verificar qué persiste realmente el bind mount `vol-emqx-data` para la sección rules del engine (punto (a) heredado del recon original); (ii) bootstrap con auto-DELETE de resources con `is_alive:false` (convierte el fix manual del episodio 1 en política); (iii) `waitForSaverResource` con retry más agresivo + fallback a DELETE-and-recreate; (iv) alerta operativa cuando el saver-webhook lleva N minutos sin ingesta (evita gaps silentes de 44h+ como los episodios 2 y 3). **Adenda #48/R2 — corrección de diagnóstico de #48/R1 (clase DEC-PROC-2, sin suavizar):** durante el Paso 3 (recon EMQX management API con `EMQX_DEFAULT_APPLICATION_SECRET`, autorizado por Franco) apareció evidencia que refuta el diagnóstico de #48/R1. **Cuadro EMQX real (recon):** 3 resources con URL correcta `http://node:3001/api/{rule,alarm,saver}-webhook`, 13 rules del engine `enabled=true`, alineación exacta con `db.saverrules` (13 documentos, mismos `emqxRuleId`). EMQX **NO tenía rules perdidas ni resources muertos** — el fix histórico de OPS-1 (DELETE + reconcile) NO aplicaba. **Causa raíz real (verificada por canary línea-por-línea del `index.js:26-38`):** el backend `node` arrancó el 2026-07-15 sin `.nuxt/dist/server/` (verificado ausente en el filesystem del container); el serverMiddleware quedó registrado a MEDIAS — los routes `webhooks.js` (línea 34), `emqxapi.js` (35), `alarms.js` (36) y `rules.js` (37) responden 404 mientras el resto del chain (`devices/sites/forensic/simulator/users/templates/rulepacks/dataprovider`) responde 400/401 normal. `POST /api/saver-webhook → 404` → EMQX manda mensajes con `save:1` pero el backend rebota → `db.data` sin ingesta desde 2026-07-15T11:50Z. `webhooks.js` en isolation carga OK (`@babel/register` + `dotenv.config()` con `FORENSIC_HMAC_SECRET` presente en `app/.env` con length 64), confirmando que **NO es error de código** sino de **estado del bundle Nuxt al arrancar el container**. **Reencuadre del patrón — lo que sobrevive:** el reporte de "3 interrupciones tras cold-start WSL2 en 2 meses" sigue vigente como observación empírica, pero con **dos mecanismos distintos, no uno**: (episodios 1 y 2) rules perdidas + resources con URL inalcanzable, patrón OPS-1 clásico; (episodio 3, actual) bundle server Nuxt ausente → serverMiddleware parcial. **Síntoma idéntico** (persistencia `db.data` sin ingesta), mecanismos distintos. **Refuerza el candidato de hardening (iv)** (alerta operativa post N min sin ingesta): una alerta agnóstica del mecanismo interno hubiera detectado ambos episodios 2 y 3 sin gap de 44h+/49h. **Error de sala #48 registrado sin suavizar (clase DEC-PROC-2, patrón recon con inferencia por analogía sin cruzar implementación con evidencia — parientes DEC-REF-66-A/#44-R21 y las lecciones de método arrastradas):** la sala hizo el diagnóstico OPS-1 por analogía con los episodios 1 y 2 (mismo síntoma, mismo momento — cold-start) SIN correr el recon del cuadro EMQX antes de commitear la adenda #48/R1. El corpus-first se aplicó a la versión y a los IDs, pero NO al mecanismo. **Lección explícita:** *"registro antes que código" aplica a **decisiones firmadas por la sala**; los **diagnósticos sin recon completo no son decisiones, son hipótesis** y deben etiquetarse como tales antes de commitear (ej. "hipótesis a validar en Paso 3", sin fila propia hasta la validación)*. #48/R1 no se elimina — queda como precedente del error, con esta adenda como corrección. **RISK-SEC (colateral registrado):** durante el recon Paso 3, `emqx_ctl mgmt list` volcó el secret de la app management (`EMQX_DEFAULT_APPLICATION_SECRET`) al output del terminal (fuga leve — dev local, sin push del transcript). **Agregar `EMQX_DEFAULT_APPLICATION_SECRET` al checklist de rotación de secretos de RISK-SEC-1** (junto a `TELEGRAM_BOT_TOKEN`, MongoDB, MQTT, `FORENSIC_HMAC_SECRET`). No urgente hoy; obligatorio antes de deployment productivo o antes de compartir el repo. **Fix operativo autorizado por Franco (paso 4-6 del plan #48):** `docker compose -f docker_nuxt_build.yml up` (genera `.nuxt/dist/server/`) + `docker restart node` (bundle completo → serverMiddleware carga los 4 routes ausentes). Sin DELETE de rules/resources EMQX (sano). Un solo restart resuelve el `db.data` sin ingesta Y hace visible el bundle R23 con los mini-forms C/S. **Adenda #48/R3 — DOBLE corrección de diagnóstico (clase DEC-PROC-2, sin suavizar):** el fix operativo de #48/R2 (rebuild + restart) NO resolvió la persistencia. Recon dirigido post-restart reveló DOS errores encadenados de método — la sala convalidó ambos. **(1) Error de sala en #48/R1:** diagnóstico OPS-1 emitido por analogía con episodios 1 y 2 (mismo síntoma, mismo momento — cold-start), **sin correr el recon del cuadro EMQX** (management API v4 con `EMQX_DEFAULT_APPLICATION_SECRET`) antes de commitear. **(2) Error de agente en #48/R2:** al ejecutar el recon EMQX, el canary de los 4 routes de `webhooks.js` respondió 404 porque el handler `/api/saver-webhook` (`webhooks.js:82-85`) responde 404 explícito ante token no matcheado — mi canary llamó con `token:x` bogus. Interpreté el 404 como "route no registrado" y construí sobre eso la teoría del "bundle server ausente → serverMiddleware parcial". El chain reproducido con `require + @babel/register` cargó las 13 routes OK (mostrando que la teoría nunca tuvo base), pero commiteé #48/R2 antes de esa verificación. La sala convalidó la teoría sin auditar el canary. **Parientes DEC-PROC-2:** DEC-REF-66-A (sala #44/R20: recon con `grep` insuficiente que no cruzó implementación con letra canónica → premisa de "field desconectado" era falsa) y DEC-REF-66-B (Franco #45: primer daño accidental real desde consola, no detectado por diagnóstico automático — regla del smoke visual). Familia de errores: **recon con inferencia por analogía o por canary superficial, sin cruce con evidencia atómica**. **Diagnóstico restaurado (con evidencia atómica esta vez):** OPS-1 recurrencia 3 confirmada. `docker logs emqx | grep resource_not_initialized` devuelve **10+ líneas críticas**: `Can not re-build rule <<"rule:X">>: {resource_not_initialized,<<"resource:9730c636">>}. The rule is disabled. Fix the issue and enable it manually.` con timestamp **2026-07-15T12:49:39Z** — 1h después del cold-start #45 (2026-07-15T11:50:40Z, verificado en bitácora al abrir #45). Rules quedaron enabled en la API v4 pero con `matched:23865, failed:23865` porque el resource `saver-webhook` (`resource:9730c636`) quedó en estado `is_alive:false` post-cold-start. El `is_alive:undefined` que #48/R2 desestimó era la firma real. **Lección de método incorporada al checklist de diagnóstico de persistencia:** ante `db.data` sin ingesta post cold-start, la verificación primera y barata que confirma o descarta OPS-1 recurrencia es `docker logs emqx | grep resource_not_initialized`. Si aparece — es OPS-1, aplicar fix DEC-REF-52-A (DELETE resources + `docker restart node`). Si NO aparece — investigar por otra rama. El estado `is_alive:undefined/false` en el JSON del resource es evidencia coherente adicional; **no descartarlo por interpretarlo como "bug de API v4"**. **CABO NO RESUELTO (etiquetado como tal, SIN teoría):** el rebuild + restart de #48/R2 provocó un flip observable de 404→401 en los endpoints de `alarms.js` (`/api/alarm-rule`) y `rules.js` (`/api/rule`) sobre :3001 respecto del canary pre-rebuild. Esto NO se explica con el diagnóstico OPS-1 restaurado (que solo afecta la persistencia, no la carga de routes). **Causa no determinada.** Registrado como observación sin teoría — abre investigación futura si vuelve a manifestarse o si bloquea algo. **RISK-SEC (colateral):** durante este recon apareció en output del terminal el valor de `EMQX_API_TOKEN` (`app/.env`) al hacer cross-check contra el header del resource EMQX. **Agregar `EMQX_API_TOKEN` al checklist de rotación** junto a `EMQX_DEFAULT_APPLICATION_SECRET` (fuga leve de #48/R1), `TELEGRAM_BOT_TOKEN`, MongoDB, MQTT y `FORENSIC_HMAC_SECRET`. No urgente hoy; obligatorio antes de deployment productivo. **#48/R2 queda como precedente del error de agente** — corregida por esta fila, no eliminada. La sala reconoce el doble fallo sin suavizar: la analogía sin recon (R1) y el canary superficial que fundó la teoría del bundle (R2). **Adenda #49/A9 — 2ª instancia clase R5/#41 durante apertura #49:** el sim quedó sin publicar post-reboot del host WSL2 y el diagnóstico inicial pasó por el playbook DEC-REF-52-A (DELETE resources + `docker restart node`), resolvió sin intervención especial. Precedente adicional del patrón (3ª recurrencia total contando #41-R5, #48/R3 y esta), **NO escala prioridad** — el mecanismo del fix está probado, sigue vigente el hardening del volumen EMQX como candidato para el Hub de campo (Orange Pi Zero 3, DEC-REF-68). **Concern ortogonal registrado en el mismo A9 — simulador sin reconexión MQTT ni supervisión (dev-only):** el sim `tools/device_simulator/run.js` no tiene reconexión al broker ni watchdog externo. Cuando el host WSL2 cortó al inicio de #49, el sim quedó dormido y hasta que Franco lo reactivó manualmente el pipeline estuvo mudo — mismo síntoma agregado que el gap EMQX, mecanismo distinto. En producción NO hay simulador, pero para el ciclo dev cae bajo el mismo checklist operativo: candidato de hardening es (a) reconnect MQTT en el `on('error')` del sim, o (b) watchdog externo (init.d / systemd / pm2). Deferido a la sesión #50 (auditoría/inventario) — decidir primero si el sim queda como parte del entorno dev estándar o migra a sistema aparte. |
| BACKLOG-OPS-2 | 2026-07-05 | **Deriva `.env` real ↔ `CLAUDE.md`: clase de bug silenciosa.** Detectado en #41-R4: `app/.env` contiene `WEBHOOKS_HOST=localhost` mientras `CLAUDE.md` documenta el valor correcto `WEBHOOKS_HOST=node` (hostname del contenedor node en la red docker, para que EMQX pueda llamar los webhooks). Consecuencia observada: URL del resource inalcanzable desde EMQX → `is_alive:false` → self-heal bloqueado (ver adenda BACKLOG-OPS-1). Cero errores visibles en el momento — la deriva se descubre solo cuando algo ejercita el path afectado (aquí: pérdida de rules + reconcile). Auditar TODAS las variables del `.env` real contra lo documentado: MONGO_*, EMQX_*, MQTT_*, SSLREDIRECT, etc. Los `.env` están gitignored (correcto para secretos), pero la sombra documentada en CLAUDE.md debe reflejar los valores operativos verdaderos, no los aspiracionales. Disparador: checklist de deployment (junto a OPS-1, RISK-SEC-1/2). NO urgente: cada deriva se manifiesta solo si algo la ejercita — pero cuando se manifiesta, el diagnóstico es caro. |
| BACKLOG-OPS-3 | 2026-07-27 | **Consolidación de histórico huérfano del ATS de CR00061 (`6z4LN2md` → `59XYsglM`) con conflicto de tenencia — NO ejecutar sin resolver dueño.** Fase 2 del paréntesis (DEC-REF-77) reveló que el ATS de CR00061 cambió de `dId` entre el 24-jul (última muestra bajo `6z4LN2md`, `2026-07-24T02:53Z`) y el 27-jul (primera muestra bajo `59XYsglM`, `2026-07-27T01:42Z`), con **gap de 3 días silencioso** por credenciales EMQX inválidas post-borrado por UI (el `DELETE /device` ejecuta `deleteMqttDeviceCredentials(dId)` — se borró el `emqxauthrule` del viejo con el device; el sim quedó intentando conectar con creds inválidas hasta que se actualizó `devices_state.json` con el `dId` nuevo). **162.194 documentos históricos quedaron bajo `6z4LN2md` sin device ni auth rule** (verificado: `db.devices.findOne({dId:"6z4LN2md"})` = null; `db.emqxauthrules.findOne({dId:"6z4LN2md"})` = null). **CONFLICTO DE TENENCIA CONFIRMADO (find atómico):** `db.data` guarda `userId` en cada documento (`printjson(findOne({dId:"59XYsglM", variable:"mains_voltage"}))` = `{userId:"6a32e105be5ca779169754af", dId:"59XYsglM", variable:"mains_voltage", value:225.19..., time:...}`). Los 162.194 documentos huérfanos tienen `userId` en **DOS valores distintos** (`db.data.distinct("userId", {dId:"6z4LN2md"})`): (a) `fsugamielecinetiksrl@gmail.com` (`6a1ddc27442190ad13f1da4a`) y (b) `operator-claro@wanomi.platform` (`6a3992b435afd807a7f992fe`). El actual `59XYsglM` está bajo (c) `admin@wanomi.com` (`6a32e105be5ca779169754af`). **Un `updateMany({dId:"6z4LN2md"}, {$set:{dId:"59XYsglM"}})` ciego rompería `buildReadFilter` en TRES direcciones** — documentos con `userId=(a)` o `(b)` bajo `dId` que ahora pertenece a `(c)`. **DISPARADOR de la consolidación:** solo si alguna vista **necesita** continuidad histórica del ATS **Y** se resuelve antes quién es el dueño canónico de los documentos huérfanos (decisión no es técnica sino de gobernanza — quién es el propietario del site en el modelo de tenants final). **Fundamento de NO ejecutar hoy:** (i) write destructivo sobre 162k docs; (ii) pregunta de tenencia abierta que no bloquea el miércoles; (iii) el ATS acumula historia propia desde hoy bajo `59XYsglM`, la operación no depende del histórico previo. Familia con BACKLOG-TENANT-* (conflicto de scope multi-user). Registrado durante Fase 2 (2026-07-27) del paréntesis pre-reunión Claro. |

> **BACKLOG-OPS-1, BACKLOG-OPS-2 — absorbidos en bloque #43 (DEC-REF-54)**. Se atienden en A9 como pull operacional único junto a BACKLOG-API-1 (hardening enrichments `GET /device`).

> **BACKLOG-OPS-2 — subitem nuevo (#51/R1):** divergencia CLAUDE.md ↔ realidad en el nombre de la DB — la app opera contra `iotix` (5 templates, 13 devices, 1 rulepack verificados) mientras CLAUDE.md §"App Node.js" declara `ioticos_god_level` (0 documentos en las 3 colecciones). Corregir CLAUDE.md dentro de la auditoría .env↔CLAUDE.md ya pendiente en este ítem. No urgente; costó un intento de auth fallido + búsqueda en el recon R1.

> **BACKLOG-EDGE-2 — absorbido en bloque #43 (DEC-REF-54)**. Referenciado únicamente dentro de DEC-REF-26; sin fila propia. La escalada temporal del fallback (`fallback`/`no-ref` → warning tras `escalateAfterMinutes`) queda cubierta como parte del hot-reload del motor edge que instrumenta A8 (consola superadmin).

### 5g · Backlog de notificaciones (BACKLOG-NOTIF-*)

| ID | Fecha | Descripción |
|---|---|---|
| BACKLOG-NOTIF-1 | 2026-07-15 | **Outbox durable de notificaciones (diferido de DEC-REF-67, #45/R22).** Garantía de entrega para el camino real LTE-M → NOC Claro: colección `notifications` en el Mongo local por Hub (estado pending/sent/failed) + reconciliador que reintenta failures; **dedup server-side** por `${ruleId}:${ts}:${kind}`. Sobrevive la muerte del proceso edge (el gap que la Fase 1 de DEC-REF-67 no cubre). Alineado con **DEC-ARCH-1** (soberanía del dato, Mongo local por Hub) y **DEC-ARCH-2** (entrega de eventos/estados/alarmas al NOC) — arquitectónicamente es el **mismo** problema de entrega garantizada. Trigger: integración del enlace celular real / diseño de entrega al NOC. Requiere opinión del **Asesor Telco** (las garantías de entrega al NOC son estándar de telco). NO es hotfix — es feature con diseño propio. |

### 5h · Backlog del motor edge (BACKLOG-EDGE-*)

> Nota: BACKLOG-EDGE-1/2/3/4 fueron declarados dentro de DEC-REF-24/26/48/54 sin fila propia (convención heredada). BACKLOG-EDGE-2 quedó **cerrado** (tombstone del doble uso en DEC-REF-57 · hot-reload diseñado en DEC-REF-58 y refinado en DEC-REF-61 · escalada del fallback dentro de DEC-REF-26). Los -1/-3/-4 siguen abiertos (driver Modbus real, silencio total del stream, heartbeat/staleness) — recuperables como filas propias si se activan. Esta tabla formaliza el registro desde -5 en adelante.

| ID | Fecha | Descripción |
|---|---|---|
| BACKLOG-EDGE-5 | 2026-07-20 | **Orden `persist → publish` en `notificationRouter.notify()`. CAUSA RAÍZ del race MQTT/Mongo observado en #49/R8.** `edge-engine/notificationRouter.js` (función `notify()`) ejecuta hoy: `sendMqttNotif(alarm)` sincrónico inmediato → `sendNocEvent(alarm)` → `saveToMongo(alarm).catch(...)` async **sin `await`** → `sendTelegram(alarm)`. El MQTT llega al browser antes que Mongo persista el doc. Cualquier consumer server-side que reaccione al MQTT (dashboard NOC, sistemas de reporting futuros, agregadores de estado) y consulte Mongo dentro de la ventana ~10-300ms lee un estado donde el evento **aún no está persistido**. **Impacto observado #49/R8:** el Panel (dashboard NOC) mostraba status crítico "pegado" post-resolve porque la aggregation `activeEpisodes` en `/dashboard/noc` NO veía el resolve → `$first: kind = 'fire'` → regla contaba como activa → site status crítico hasta el próximo poll natural (60s) o refresh manual. **Mitigación aplicada en #49/R8 (frontend puro):** doble refresh 1s+4s con `?fresh=1` en `dashboard.vue`. El segundo refresh (4s) cubre el lag de Mongo bajo carga o jitter WSL2 en el 99.9% de casos. Costo: 2 recomputes del backend (~4s c/u) por ráfaga de notifs — aceptable en operación normal (frecuencia baja de alarmas). **NO ataca el race en el origen.** Fix de raíz propuesto: `notify()` debe hacer `await saveToMongo(alarm)` **ANTES** de `sendMqttNotif(alarm)`. Costo: agrega latencia Mongo (~10-20ms bajo carga normal, hasta ~200ms en jitter) a la ventana de publicación MQTT. Trade-off aceptable — todos los consumers server-side que reaccionan al MQTT operan sobre estado consistente. Consideraciones adicionales del diseño: (i) `sendNocEvent(alarm)` — a verificar si consume Mongo agua abajo en el diseño de Fase 2 NOC Claro; (ii) `sendTelegram(alarm)` — fire-and-forget con retry (DEC-REF-67), NO consume Mongo, seguro después del await; (iii) política ante Mongo error — `notify()` debe decidir fallar duro (romper el pipeline) o log-and-continue (Mongo fail no debería silenciar MQTT/Telegram). Definir en el diseño del fix. **Disparador:** sesión dedicada al backend del NOC Claro (Fase 2) — cuando aparezcan más consumers que consulten Mongo post-MQTT y la mitigación #49/R8 deje de ser suficiente. Mitigación sostiene la responsividad del Panel v1 hasta entonces. |

### 5i · Backlog de la cadena de autoría (BACKLOG-CHAIN-*)

| ID | Fecha | Descripción |
|---|---|---|
| BACKLOG-CHAIN-1 | 2026-07-21 | **Implementación de la cadena Template→Device→Regla (diseño firmado en DEC-REF-72 + DEC-REF-73, sesión #51).** Bloque propio, tamaño comparable a A8. Alcance: (a) sub-schema formal de widget + `unit` como campo formal + migración backfill mínimo de unit (corrige psi/Bar productivo); (b) `PUT /template` canónico con warnings[] por reglas huérfanas; (c) selector de variable con herencia read-only en los mini-forms + fallback con badge para reglas legacy; (d) denormalización de variableLabel/unit resuelta en el PUT del pack (edge sigue ciego a templates); (e) recommendation v2 editable en los 4 mini-forms con placeholder por severidad; (f) asistente de creación por intención como capa de entrada (4 pasos, modo simple/avanzado). **Subitems absorbidos:** `correlationParent` sin UI (R1 #51, va en el avanzado del cross); subitems de BACKLOG-UI-8 relativos a campos faltantes en mini-forms — verificar solapamiento antes de ejecutar para no duplicar. **Secuencia sugerida:** (a)→(b)→(c)/(d)→(e)→(f); el asistente (f) es lo último porque se apoya en todo lo anterior. Disparador: palabra de Franco al abrir el bloque. |

> **BACKLOG-CHAIN-1 — ampliación de alcance (#51/R4, DEC-REF-74).** El punto (a) del alcance original ("sub-schema formal de widget + unit + backfill") se amplía a **frente único "sub-schema + catálogo de widgets Familia A"**: el catálogo define qué metadata debe soportar el sub-schema (umbrales, tankCapacity, enumValues, diccionario de bitmaps, cadencia esperada), por lo que no pueden diseñarse en secuencia. **Alcance por widget = 3 piezas** (componente + mini-form de configuración en la página de templates + campo de sub-schema) — once widgets son once tríos, no once componentes. **La migración del punto (a) suma una tarea:** asignar tipo de widget a los 42 widgets legacy (decisión de Franco, #51/R4) además del backfill de `unit`. **Familia B** (widgets de sitio: autonomía, recomendación activa, planta DC, cascada de energía) se secuencia después de Familia A; autonomía depende además de BACKLOG-INTEL-1.

### 5j · Backlog de inteligencia (BACKLOG-INTEL-*)

| ID | Fecha | Descripción |
|---|---|---|
| BACKLOG-INTEL-1 | 2026-07-21 | **Motor de sugerencias — reglas y situaciones propuestas desde datos históricos y eventos (visión de Franco, #51/R3).** Salto de "formulario inteligente" (DEC-REF-73, ayuda a escribir la regla que el operador YA tiene en la cabeza) a "sugerencia basada en datos" (propone reglas que el operador NO pensó). **Tres niveles jerarquizados, deliberadamente separados:** **Nivel 1 — desde lo que el equipo ES (sin historia):** el template declara qué mide cada equipo; se sugiere un catálogo base de alarmas típicas por familia de equipo. Sólido, no requiere historia, encaja con el método falla-primero de #17/#18. Diseñable pronto. **Nivel 2 — umbrales desde el historial (EL OBJETIVO REAL):** con historia del sitio se propone el umbral que ESE equipo pide, no un default genérico ("la presión de aceite de este sitio vive entre X e Y → sugerencia de alarma en Z"); con los eventos registrados (2225 notificaciones históricas verificadas en #49/R1) se suma afinado por recurrencia y patrones que precedieron incidentes. **DEPENDENCIA DURA: requiere BACKLOG-CHAIN-1 implementado** — proponer umbrales sobre datos sin unidad formal ni label confiable reproduce exactamente el bug psi/Bar que la cadena vino a matar. **Nivel 3 — descubrimiento automático de patrones (ML):** horizonte SIN promesa. Coherente con DEC-PRED-1/#17: no se promete ML en el pitch porque sería mentir; requiere baseline de meses. Disparador: cierre de BACKLOG-CHAIN-1 (Franco: "ni bien lo implementemos, iniciamos el diseño de los tres niveles"). |

### 5k · Backlog de onboarding (BACKLOG-ONBOARD-*)

| ID | Fecha | Descripción |
|---|---|---|
| BACKLOG-ONBOARD-1 | 2026-07-21 | **Onboarding por autodescubrimiento — el template nace leyendo el sitio (visión de Franco, #51/R3). SALTO DE DISEÑO ENCADENADO AL HARDWARE.** Premisa: al instalar Wanomi por primera vez en un sitio, el WN-SITE-CORE se engancha al controlador existente (ComAp / Cummins / Eltek / el que haya), descubre qué variables emite, y de ahí nace el BORRADOR del template — el instalador revisa y confirma en vez de cargar 13 variables a mano. Cierra el eslabón que DEC-REF-71 dejaba implícito: el diccionario se autocompleta leyendo la realidad, no se escribe siempre a mano. **Corrección técnica de la sala (Ing. Hardware) — el descubrimiento da la MITAD:** un equipo Modbus NO se autodescribe en criollo; devuelve "registro 40004 = 87", no "presión de aceite en Bar". El significado (label, unidad, tipo, escala) vive en el mapa del fabricante, no en el equipo. **Tres piezas con dependencias distintas:** (1) **catálogo de perfiles Modbus por familia** de controlador (registro→variable→label→unidad→tipo) — NO depende de fierro, es documentación y está medio hecho (el informe de equipamiento Claro del proyecto mapeó ComAp InteliATS NT a nivel registro; mapas de InteliGen NT y Cummins PowerCommand aportados por Franco en #14); **se puede adelantar cuando Franco lo disponga**; (2) **descubrimiento en el CORE** = perfil ∩ registros vivos — REQUIERE FIERRO; (3) **canal CORE→Hub para dictar el template** — NO requiere mecanismo nuevo: reusa el canal de control MQTT del hot-reload (DEC-REF-58/61), único que sobrevive a NAT en topología de Hub remoto. **DEPENDENCIA DURA: hardware.** El WN-SITE-CORE está congelado en Rev B (Área 3 frozen), existe en Gerbers, no instalado en ningún sitio; la estrategia vigente (DEC-STRAT-4, Camino A — pilar primero sin hardware) lo empuja adelante deliberadamente. Se puede DISEÑAR; no se puede VALIDAR hasta que haya CORE contra un controlador real. **Nota anti-demo (DEC-STRAT-2):** simular el descubrimiento contra el simulador propio NO probaría nada — probaría que sabemos leer nuestro simulador. La validación honesta exige fierro y controlador real; queda escrito ANTES de que la tentación aparezca. **Voces para la sesión futura:** Área 3 (Ing. electrónico industrial + Ing. potencia DC) + Área 2 (Backend + motor edge) + Asesor Telco. Disparador: fierro Rev B instalado, O decisión de Franco de adelantar la pieza (1). |

### 5l · Registro de fallas de proceso (DEC-PROC-*)

> **Nota de origen (#51).** DEC-PROC-1 y DEC-PROC-2 nacieron como referencias textuales en la bitácora y como categoría citada dentro de filas DEC-REF (`DEC-REF-66-A`, `-68`, `-70-A`, adendas de BACKLOG-OPS-1) — nunca tuvieron fila propia. **NO se retro-registran** (no se reescribe historia, convención append-only). La familia se formaliza tabularmente desde DEC-PROC-3.

| ID | Fecha | Descripción |
|---|---|---|
| DEC-PROC-3 | 2026-07-21 | **Una auditoría que no rastrea el origen de las piezas del eslabón que audita, no es una auditoría (señalamiento de Franco, #51/R4).** Cita textual: *"solicité una auditoría para que entiendan realmente el funcionamiento de la app, toqué en varias sesiones el tema y ninguno advirtió de dónde saldrán los widgets necesarios que llenarán el select en template y nos permitirán obtener la funcionalidad necesaria para recibir y emitir los datos de diferentes equipos existentes en un sitio"*. **Qué falló, sin suavizar:** el recon R1 auditó el Template como eslabón (schema, rutas, UI, consumidores) y produjo evidencia correcta — pero NO preguntó de dónde salen las piezas con las que se construye un template. La evidencia del hueco ESTABA en el propio reporte (sorpresas 4 y 5: `variableType` hardcodeado en los `configXxx` de templates.vue; `unit` presente solo en el widget numberchart) y la sala la clasificó como "asimetría menor" en vez de reconocer dos síntomas de un hueco estructural. **No fue falta de datos: fue falta de lectura.** Agravante: el pedido de Franco era explícito y repetido a lo largo de varias sesiones. **Familia de errores:** misma raíz que DEC-REF-70-A (decidir sin auditar la vecindad completa del componente afectado), escalada — allá fue una decisión visual reversible, acá fue un frente de trabajo entero no visto. **Regla operativa que queda:** al auditar cualquier eslabón de una cadena, el recon debe incluir explícitamente la pregunta *"¿de dónde salen las piezas con las que se construye esto, y son configurables o hardcodeadas?"* — y toda respuesta "hardcodeado" es hallazgo estructural, nunca asimetría menor. |
| DEC-PROC-4 | 2026-07-21 | **Higiene de edición del corpus y de encadenamiento de pasos — cuatro incidentes de proceso en #51 (sin daño, registrados por recurrencia).** **(a) Ancla destructiva:** en el PASO 5 se sobreescribió la fila vigente `BACKLOG-EDGE-5` con un placeholder (`| BACKLOG-EDGE-5-PLACEHOLDER | ignore | placeholder for edit anchor |`) para usarla como ancla de edición, y se revirtió en el edit siguiente. La reversión fue limpia (verificado: el diff final del corpus mostró 21 inserciones / 1 deleción, siendo la única deleción la línea de versión — una fila alterada habría aparecido como modificada). **Pero el riesgo era desproporcionado:** una interrupción entre ambos edits dejaba una fila de gobernanza destruida en un documento append-only cuya integridad ES la memoria del proyecto. **Regla:** para insertar al final de una sección, anclar en el separador `---` o en el header siguiente; NUNCA sobreescribir contenido vigente como paso intermedio, ni siquiera con intención de revertir. **(b) Error de orden recurrente:** dos veces en la misma sesión (PASO 3 con DEC-REF-71-A, PASO 5 con DEC-REF-73) la fila nueva se insertó ANTES de su vecina en lugar de después. Detectado y corregido por el propio agente antes del commit en ambos casos — el reflejo funciona — pero la recurrencia indica que el patrón de edición carecía de verificación de posición PREVIA a escribir. **Regla:** verificar posición relativa a la fila vecina antes del edit, no después. Aplica en particular a las convenciones vigentes: adenda contigua a su fila madre (DEC-REF-70/70-A), fila nueva al final de su tabla. **(c) La sala incurrió en la misma falla que DEC-PROC-3 mientras la registraba (#51/PASO 7).** Dos asunciones no verificadas en las instrucciones del paso de cierre: (i) se redactó el PASO 7 dando por ejecutado el PASO 6 (registro de R4), que Franco había aprobado pero que **al momento de redactarse el PASO 7 aún no se había ejecutado** — el corpus estaba entonces en v0.59, no v0.60, y `DEC-REF-74` no existía todavía; **resolución:** el PASO 6 se ejecutó a continuación (commit `efb08ee`, bump a v0.60) y el PASO 7 se corrió después, sobre v0.60; (ii) se instruyó "insertar en la misma tabla de DEC-PROC" sin verificar que esa tabla existiera — no existía, y `DEC-REF-70-A` lo decía explícitamente (*"Sin nueva DEC-PROC porque…"*); de ahí nace esta sub-sección 5l. Ambas las detuvo el freno del agente con evidencia, antes de escribir. **Valor del incidente:** demuestra que la regla de DEC-PROC-3 aplica a la sala tanto como al recon, y que el gate funciona en las dos direcciones — el agente frena a la sala, no solo la sala al agente. **Regla:** ninguna instrucción de un paso N puede asumir el estado resultante del paso N−1 sin verificación explícita al abrir; y ninguna instrucción puede referirse a una estructura del corpus sin confirmar que existe. **(d) Un paso por turno.** El PASO 6 y el PASO 7 se pasaron encadenados en un mismo turno, con dos bumps de versión distintos (v0.59→v0.60 y v0.60→v0.61); el agente lo leyó como contradicción aritmética y frenó — correctamente, porque el estado intermedio era ambiguo. **Regla:** un paso por turno, con su verificación de apertura, y el turno siguiente recién con el reporte del anterior en mano. Encadenar pasos fuerza al agente a interpretar, que es precisamente lo que los gates existen para evitar. |
| DEC-PROC-5 | 2026-07-23 | **Un smoke debe ejercer el MISMO path que producción, no uno equivalente (dos incidentes en #52/C1/C1-BIS/C1-TER).** `mongoose.connection.db.collection('templates').insertOne(...)` (driver crudo) y `Template.create(...)` (constructor Mongoose que ejecuta el sub-schema) terminan en la misma colección y son objetos **distintos** desde la óptica del sub-schema con `strict:true`: el primero prueba **read-path** (Mongoose no toca el widget al escribir; al leer preserva keys existentes en Mongo); el segundo prueba **write-path** (el constructor construye cada sub-doc pasando por el schema y `strict:true` descarta keys no declaradas ANTES de serializar). **Evidencia de los dos incidentes de #52:** (i) el smoke original de P1 (C1) usó 8 keys, todas declaradas por el sub-schema propuesto — cero ejercicio del `strict`; registrado en DEC-REF-75-A(iii); (ii) el smoke P1-real (C1-BIS) corrigió a 5 keys no-declaradas pero por el read-path (`insertOne` raw + `findById`) — certificó RAMA-1a con evidencia del path equivocado; falsado por el TEST-CREATE de C1-TER que ejerció el constructor y reveló pérdida de `chartTimeAgo`/`demo`/`selectedDevice`/`userId` en `numberchart` y de `message`/`text`/`selectedDevice`/`userId` en `button`; registrado en DEC-REF-75-B(i)/(v). **Regla operativa:** antes de escribir un smoke, **identificar el path del consumidor REAL** — grep del endpoint/router que ejecuta la operación en producción, mirar si usa `.create()`/`.save()` (write via constructor) o `insertOne`/`updateOne` (driver directo) — y ejercer **exactamente ese path** en el smoke. `insertOne` no valida como `.save()`; `findById` no lee como una route productiva que hidrata con `.lean(false)` vs `.lean(true)`. **Familia de errores:** misma raíz que DEC-PROC-3 (auditar el eslabón sin rastrear el origen de las piezas): acá el "origen" es el path de escritura del consumidor real, no una decisión de diseño equivalente. **Consecuencia positiva:** el patrón se registra ahora; futuros smokes de schema/validación deben incluir un comentario `// path real del consumidor: <archivo:línea>` que declara cuál es la ruta ejercida. Sin ese ancla, el smoke prueba un contrato distinto y su veredicto no es válido para producción. |
| DEC-PROC-6 | 2026-07-26 | **Implementar sobre una DIRECCIÓN firmada no es implementar sobre una ESPECIFICACIÓN (nueve adendas correctivas en #52 son la medida del hueco).** #51 firmó rumbo (DEC-REF-71 a -74: cadena de autoría + vocabulario + intención + catálogo de widgets); #52 entró a código con eso y cada pregunta que el rumbo NO contestaba se resolvió sobre la marcha y se registró como adenda. **DEC-REF-74-A** (conteo real de widgets), **-75-A** (retiro enum `variableType` post-smoke real), **-75-B** (write-path del constructor descartaba keys de dominio — bug latente del widget button destapado por TEST-CREATE), **-75-C** / **-75-C-A** (unit en `/history` + falsación de cierre UI-8), **-75-D** (unit en contrato `/dashboard/noc/trend` — habría perdido la unidad en la pantalla del NOC), **-76-A** (resolver con contexto + cáscara aparte), **-76-B** (mini-form con `variable` editable no `makeid`, dedupe por variable, `column`, degradación previewConfig), **-76-C** (los 4 umbrales SALEN del mini-form por objeción de Franco — doble fuente con reglas reproduce psi/Bar). **Nueve adendas correctivas en una sesión.** Tres de las correcciones mayores las hizo Franco (variable editable, umbrales fuera del widget, decimalPlaces al liveConfig), no la sala. **Familia de errores:** misma raíz que DEC-PROC-3 (auditar sin rastrear origen) y DEC-PROC-5 (smoke con path equivocado): entrar a código con una dirección aprobada pero sin especificación operativa. **Regla operativa:** antes de tocar código, el bloque necesita **especificación** que responda: (a) qué pantallas se afectan (grep exhaustivo de consumidores); (b) qué campos entran/salen/mutan en cada pantalla; (c) qué hace cada control (edit/reset/validación) y qué pasa en cada caso raro (widget sin variable, config `undefined`, campo no declarado en schema `strict`, dedupe key ambigua); (d) cuál es el path real del consumidor (DEC-PROC-5 lo declara para smokes; se generaliza acá al bloque completo). **Si aparece una pregunta que la especificación no contesta, SE FRENA EL BLOQUE y vuelve a diseño** — no se toma la decisión sobre la marcha, no se registra como adenda post-hoc. Adenda ≠ especificación: la adenda es reparación después del hecho, la especificación es contrato antes del hecho. **Consecuencia positiva:** los nueve incidentes de #52 quedan como material didáctico — la relectura del corpus permite estimar el costo real de saltar la fase de especificación (una sesión de 9 bumps de versión que debió ser una sesión de 3-4). **Aplica desde el siguiente bloque de implementación:** el paréntesis pre-reunión Claro (DEC-REF-77) requiere especificación por bloque antes de tocar código; la sala verifica que cada pregunta operativa esté contestada; si falta una, se cierra la especificación primero. Franco es dueño único del criterio "esta especificación alcanza / vuelve a diseño". |

---

## 6 · Próxima reunión — Sesión #15

**Objetivo:** revisión de iteración 1 — pipeline contra simulador funcionando + sitio confirmado.

**Disparador:** cuando Estrategia confirme sitio ComAp o se cumpla T0+3.

**Entradas esperadas:**
1. **Software:** simulador ComAp emitiendo registros + pipeline e2e básico corriendo.
2. **Estrategia:** pre-filtrado (T0+1) + avance hacia confirmación de sitio.
3. **Hardware:** estado del Hub funcional.
4. **Marketing:** pitch draft avanzado + renders corregidos.

---

## 7 · Convenciones del refactor

- **Lengua:** español (alineado con todo el proyecto).
- **Formato:** Markdown.
- **Versionado:** este documento sube de minor (0.1 → 0.2 → ...) cuando se agregan DEC-REF nuevas, de major (1.0) cuando se cierra una fase del roadmap.
- **Trazabilidad cruzada:** cuando una decisión del refactor cite una decisión heredada, usar el ID original (DEC-STRAT-1, DEC-ARCH-2, etc.) — sin renombrar.
- **Cierre de sesión:** cada sesión que toque `docsRefactor/` registra su entrada en `docs/wanomi.md` (track legacy mantiene el log histórico unificado) Y agrega resumen en este documento §5 si introduce DEC-REF nueva.

---

## 8 · Glosario rápido

| Término | Definición |
|---|---|
| **Connect** | Modo de operación: lee equipo existente por software (Modbus/SNMP/contacto seco). Sin fierro nuevo |
| **Sense** | Modo de operación: sensor físico Wanomi propio para variables que el equipo no expone |
| **Hub** | Unidad física por sitio: Orange Pi Zero 3 + BG95-M3 + switch KSZ8794CNX + UPS supercaps. Edge compute + agregación local |
| **CORE** | WN-SITE-CORE: PCB Wanomi propia con ESP32-S3, sensores físicos integrados, conecta al Hub por RJ45 |
| **Add-on** | Módulo opcional que se conecta al CORE: SURGE (rayos) y ENV+ (clima extendido) |
| **NOC** | Centro de operaciones del cliente (ej. Claro). Recibe eventos agregados, no telemetría cruda |
| **Cellowner** | Dueño de zona del cliente, responsable de 15-30 sites. Usuario principal de las alertas |
| **DEC-*** | Decisión registrada. Inmutable una vez tomada. Cambios = nueva DEC, no edición de la vieja |
| **RISK-*** | Riesgo abierto. Se cierra con evidencia o se documenta su mitigación |
